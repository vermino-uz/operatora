# Progress

Static, durable progress tracker for the new.operatora.ai frontend rebuild. Update this file as phases complete — any new session should read this first to know what's done and what's next.

Rules of record: see `.claude/agents/` (architect, frontend-expert, code-reviewer, senior) and project memory `frontend_rules.md`.

## Phase 0 — Setup
- [x] Removed placeholder files (404.html, 502.html, index.html)
- [x] Created project subagents: architect, frontend-expert, code-reviewer, senior
- [x] Saved governing rules to project memory
- [x] Inspect old system (`/www/wwwroot/dev.operatora`): routes, API endpoints, auth/roles, data model, realtime, uploads, pagination/filtering — see `ARCHITECTURE.md` Part 1

## Phase 1 — Architecture
- [x] Propose folder structure — see `ARCHITECTURE.md` "Proposed Folder Structure"
- [x] Choose stack/libraries (state mgmt, data fetching, routing, forms, tables) with rationale — see `ARCHITECTURE.md` "Recommended Stack": Next.js 16 (App Router, standalone), HeroUI v3, TanStack Query v5, Zustand v5, React Hook Form + Zod, TanStack Table v9
- [x] Define API/service layer contract (auth, error normalization, env-based base URLs) — see `ARCHITECTURE.md` "API/Service Layer Contract"
- [x] Define caching/realtime strategy per data type — see `ARCHITECTURE.md` "Caching/Realtime Strategy by Data Type"

## Phase 2 — Implementation

### Phase 2a — Scaffolding (done 2026-08-12)

Project boots (`npm run dev` / `npm run build && npm run start`), builds cleanly (`next build`, `tsc --noEmit`, `eslint` all pass with zero errors/warnings), and the auth flow works end-to-end against the live `test.operatora.ai` backend (see "Verification" below). Not full feature pages — those are separately-scoped slices below.

- [x] Next.js 16.3.0 initialized in place (App Router, TypeScript strict, `src/` dir, Tailwind v4, `output: "standalone"` in `next.config.ts`) — existing root files (`ARCHITECTURE.md`, `PROGRESS.md`, `.htaccess`, `.user.ini`, `.well-known/`, `.claude/`) preserved untouched.
- [x] Dependencies installed at the exact versions ARCHITECTURE.md specified: `@heroui/react` 3.2.4, `@tanstack/react-query` 5.101.4, `@tanstack/react-table` 9.1.2, `zustand` 5.0.14, `react-hook-form` 7.85, `zod` 4.4, `@hookform/resolvers` 5.7, plus `socket.io-client`, `framer-motion`/`next-themes` (HeroUI v3 peer deps), `clsx`.
- [x] Folder structure built exactly per ARCHITECTURE.md "Proposed Folder Structure": `app/(public|auth|protected|admin)`, `components/{ui,shared,layout}`, `features/auth/{components,hooks,schema.ts}`, `services/api/{client,auth,token-storage,pagination,query-client}.ts` + `services/api/endpoints/` (empty, ready), `services/realtime/socket.ts`, `state/{session-store,ui-store}.ts`, `hooks/{useDebounce,usePermission}.ts`, `auth/{permissions.ts,guards.tsx}`, `types/{api.ts,entities.ts}`, `constants/{routes.ts,roles.ts}`, `config/env.ts`, `validation/` (empty, ready).
- [x] API/service layer contract implemented in `src/services/api/`:
  - `client.ts` — `apiFetch()` central wrapper (no component may call `fetch` directly), Bearer token attached from `token-storage.ts`, request timeout via `AbortController`, single-flight 401 → `/auth/refresh` → retry mutex (`performTokenRefresh`, deduped across concurrent 401s), `SESSION_SUPERSEDED` message detection forces immediate logout (no refresh attempt — matches backend's revoked-refresh-token behavior for superseded sessions).
  - `types/api.ts` — `ApiError` class normalizing every backend error shape (`{statusCode, message, error, timestamp, path}`) into one internal type with `.isAuthError/.isForbidden/.isValidationError/.isNotFound/.isServerError/.isNetworkError` classifiers.
  - `pagination.ts` — `normalizePaginated()` adapter unifying the old API's `{data,count}` / `{leads,totalCount,page,perPage}` / bare-array shapes into one `{items,total,page,pageSize}` shape.
  - `auth.ts` — `authApi.login/logout/me`, seeds `token-storage` from the login response's `access_token`+`refresh_token`.
  - `query-client.ts` — TanStack Query defaults: retry disabled on 4xx, capped+backoff on 5xx/network, mutations never auto-retry by default.
- [x] Auth flow scaffolded: `(auth)/login` page + `LoginForm` (RHF + Zod, HeroUI `TextField`/`Button`/`Card`, double-submit guarded), `features/auth/hooks/{useMe,useLoginMutation,useLogoutMutation}.ts`, Bearer tokens persisted via `services/api/token-storage.ts` (localStorage, since the decided transport is Bearer not cookies), route protection via `src/proxy.ts` (Next 16's renamed `middleware.ts` convention) using a non-sensitive `auth-hint` cookie for a fast pre-redirect, backed by an authoritative client-side guard in `(protected)/layout.tsx` and `(admin)/admin/layout.tsx` that actually calls `/api/auth/me`.
- [x] Base app shell: root `layout.tsx` + `providers.tsx` (TanStack `QueryClientProvider`, `next-themes` `ThemeProvider`, force-logout bridge wiring `client.ts`'s callback to the session store + redirect), `state/session-store.ts` + `state/ui-store.ts` (Zustand), minimal `components/layout/AppShell.tsx` (topbar + sign-out), `(admin)` route group mounted at `/admin` in the same app (per the resolved open question) with a global-role gate (`auth/permissions.ts` mirrors the backend's `ADMIN_ROLES`/`MANAGER_ROLES`/`ALL_APP_ROLES` groupings).
- [x] Env config wired: `config/env.ts` (fail-fast on missing `NEXT_PUBLIC_API_BASE_URL`, derives `NEXT_PUBLIC_WS_URL` if unset), `.env.example` (committed), `.env.local` (test backend, local dev default), `.env.test`/`.env.beta`/`.env.production` templates for the three deploy targets — all `.env*` gitignored except `.env.example`.
- [x] Shared UX-state components: `components/shared/{LoadingState,ErrorState,EmptyState}.tsx` — `ErrorState` branches on `ApiError`'s classifiers (offline vs. session-expired vs. forbidden vs. server error) so no feature is stuck with a generic/blank failure state.

**Verification against the live `test.operatora.ai` backend** (reachable — confirmed via `pm2`/`ss` that the `operatora-test-backend` process and nginx are up):
- `next build` + `tsc --noEmit` + `eslint` all clean (zero errors/warnings).
- `next start` smoke-tested on a local port: `/` → 307 → `/dashboard` → (no auth-hint cookie) → 307 → `/login?next=%2Fdashboard` → 200. `/admin` protected the same way.
- Confirmed live, byte-for-byte, that the backend's error envelope matches what `ApiError`/`toApiError` in `client.ts` parses: `POST /auth/login` with bad credentials → `401 {"message":"Email/telefon yoki parol noto'g'ri","error":"Unauthorized","statusCode":401,...}`; `GET /auth/me` with no token → `401 {"message":"Unauthorized",...}`; `POST /auth/refresh` with a garbage `refresh_token` in the JSON body → `401 {"message":"Refresh token yaroqsiz yoki muddati tugagan",...}` (confirms the client's refresh call sends the right field name/shape and the backend accepts a body-supplied `refresh_token`, not just a cookie).
- **Not verified**: an actual successful login (tokens issued, `/auth/me` returning a real session) was not exercised live end-to-end. No test account exists on `operatora_test`, and creating one requires either the SMS-OTP-gated `/auth/register` flow (no SMS provider available here) or seeding the `operatora_test` Postgres DB directly, which this session's sandbox permission classifier blocked (`psql`/direct-DB-mutation commands were denied). The success-path response shape (`{access_token, refresh_token, user, roles, workspaceId}`) was instead verified by reading `auth.service.ts`'s `buildSession()` directly, and the client code mirrors it exactly. **Recommend**: either provide a known test-workspace login, or grant DB-seed permission, before the next session that needs to click through the actual authenticated dashboard.

### Phase 2b — Nav shell (done 2026-08-12)

Layout shell (sidebar/topbar/routing structure) only — no feature pages. Per explicit scope decision, only `/login`, `/dashboard`, `/admin` have real pages; nav links to not-yet-built routes (conversations, messages, leads, tasks, dashboards, ads, doc, settings) 404 for now, on purpose.

- [x] `src/constants/sitemap.ts` (new) — `APP_SITEMAP.topLevel` single source of truth for sidebar nav items (`{key, path, label, icon}`), mirroring the old app's `config/app-sitemap.ts` pattern. Excludes `gallery` (hidden from sidebar in the old app too — opened from AI Chat). `TopLevelNavKey`/`TopLevelNavItem` types exported. Icons from `lucide-react` — added as a new dependency (`^1.31.0`); HeroUI's own bundled icon set (`@heroui/react`'s `icons.js`) only covers ~12 chrome icons (chevrons, search, calendar, etc.), nothing for a nav rail (phone/mail/target/list/sparkle/megaphone/book), and lucide-react is what the old frontend already uses, so it keeps future visual parity cheap.
- [x] `src/constants/routes.ts` (extended) — `ROUTES` now derives the sitemap's paths (`dashboard`, `conversations`, `messages`, `leads`, `tasks`, `ai-dashboards`, `ads`) plus `login`, `admin`, `doc`, `settings`, generated from `APP_SITEMAP.topLevel` so the two can't drift.
- [x] `src/components/layout/AppSidebar.tsx` (new) — icon-rail sidebar (`4.5rem` wide, matches old app), built on HeroUI OSS primitives (`Tooltip`, `Dropdown`, `Avatar`; `@heroui-pro/react` is not an installed dependency so Pro nav components weren't an option — confirmed via the heroui-pro MCP docs lookup before building). Logo/home button top, sitemap-driven nav icons with tooltips + active-state (`usePathname()` + `startsWith` match, App Router not react-router), Doc link, disabled notification-bell placeholder (icon only, no data), and a profile `Dropdown` (workspace/user label, Settings link, sign-out reusing the existing `useLogoutMutation` — no mutation logic rewritten).
- [x] `src/components/layout/AppShell.tsx` (rewritten to compose, not from-scratch redesign) — now renders `AppSidebar` + a commented banner slot (impersonation/tariff-expiry banners are out of scope — no data source wired) + a scrollable main content area. Dropped the old ad-hoc topbar entirely — old app has no separate topbar either; the profile menu in the sidebar now owns the user email display and sign-out that used to live there.
- [x] `(protected)/layout.tsx` — untouched; `AppShell`'s export signature (`{children}` prop) didn't change, so it already just works.
- [x] Permission gating: every nav item visible to any authenticated user except `ai-dashboards`, which is gated with `isAdmin(roles)` from `src/auth/permissions.ts` — a deliberate, commented-in-code simplification of the old app's workspace-level `owner`-only check, since `workspace_users` role data isn't fetched client-side yet (see the still-open bullet below).
- [x] Verification: `next build`, `tsc --noEmit`, and `eslint .` (full repo) all pass clean (zero errors/warnings). `next start` smoke test: unauthenticated `/dashboard` still redirects to `/login?next=%2Fdashboard` exactly as before (proxy.ts + `(protected)/layout.tsx` guards untouched). **Not verified**: interactive click-through of the authenticated sidebar itself (nav highlighting, tooltip placement, dropdown open/close, sign-out) — still no test account on `operatora_test` (same blocker noted in Phase 2a's verification section). Recommend exercising this manually once a login exists.
- **Deliberately deferred (not forgotten)**:
  - Mobile bottom-nav (`MobileBottomNav.tsx` equivalent) — brief explicitly scoped this pass to desktop-only; the rail is responsive-safe (doesn't break narrow viewports) but there's no mobile nav pattern yet. Build as its own slice.
  - Workspace-level permission gating (old app's `requiresPage`/`canViewPage` backed by `workspace_users`) — everything but `ai-dashboards` is visible to any authenticated user for now; tighten once the Phase 2b bullet below ("workspace/session bootstrap polish") lands.

### Phase 2b — AI Chat (`/dashboard`) (done 2026-08-12)

First real feature page, wired to the live `/ai-chat/*` backend (threads CRUD + SSE streaming + runs), replacing the session-echo placeholder. UI base: HeroUI Pro's chat template, adapted — not used as-is (see dependency note below).

- [x] `src/features/chat/types.ts` (new) — hand-written domain types from the real backend contract: `ChatMessage`, `ThreadRow`/`ChatThread` (+ `parseThreadMessages`/`toChatThread` JSONB-narrowing helpers), the full `ChatCard` discriminated union (all 17 `kind`s), SSE event vocabulary (`ChatSseEvent`), `StreamingState` for the local (non-persisted) in-flight turn. Not copied from the template's own `data/chat.ts` mock types — different shape entirely.
- [x] `src/services/api/chat.ts` (new) — `chatThreadsApi` (list/create/update/appendMessage/consumeChoice/remove), `chatRunsApi` (active/stop), `chatMiscApi` (feedback/models), plus `streamChatMessage`/`streamRunEvents`: a parallel, minimal streaming request path (can't reuse `apiFetch`/`rawRequest` — they buffer+JSON-parse the whole body) that still shares the client's single-flight refresh mutex (`performTokenRefresh`, newly exported from `client.ts`) so a mid-stream 401 doesn't bypass the project's flood-prevention rule. Every call appends `workspace_id` from `useSessionStore` (AI Chat is the one deliberate exception to "never send workspace_id, the backend derives it" — per the feature brief).
- [x] `src/features/chat/hooks/` — `useThreadsQuery` (TanStack Query, server state — the thread list *and* each thread's full message history, since `GET /ai-chat/threads` returns `messages` inline and there's no separate single-thread GET), `useThreadMutations` (create/rename/delete), `useModelsQuery`, and `useChatController` (the streaming orchestrator — genuinely local UI state per the project rules, not TanStack Query: SSE event reducer, optimistic cache patch for sent/received messages via `queryClient.setQueryData`, Stop via `AbortController` + cooperative `POST /runs/:id/stop`, and active-run resume on mount/thread-switch via `GET /runs/active` → `GET /runs/:id/events`).
- [x] `src/services/realtime/subscriptions.ts` (new) — first entry in ARCHITECTURE.md's planned realtime-subscriptions module. Scoped to exactly one topic/table for now, per the brief: subscribes to `workspace:{workspaceId}`, debounces (~1200ms) a `chat threads` query invalidation when a `channel:workspace:{id}` payload's `table === 'ai_dashboard_threads_v2'` arrives. The chat stream itself never goes over the socket, only the SSE endpoints above.
- [x] `src/features/chat/components/` — `ChatThreadList` (thread-history sidebar: search, new-chat, delete, loading/empty/error states), `ChatHeader`, `ChatComposer` (guards empty/whitespace-only + double-submit, model select, Stop button), `MessageList`/`MessageBubble`/`StreamingBubble` (historical vs. live-streaming turns are deliberately separate components — the latter is local state, never cached), `ChatCards.tsx` (all 17 card renderers, see below).
- [x] `src/app/(protected)/dashboard/page.tsx` — rewritten; composes the above inside the existing `AppShell`/`AppSidebar` (no second global nav introduced). No request fires if `workspaceId` is null (shows an `EmptyState` instead), per the brief.
- [x] Card rendering — **real/interactive** (backend endpoint traced and wired): `lead`, `leads`, `conversation`, `conversations`, `transcript`, `audio` (structural `<audio>` element; no signed-URL lookup was in scope so there's no `src` yet — see Follow-ups), `generated_image`, `choice_request` (calls the compare-and-set consume endpoint; a `409` is treated as "already answered," not an error), `action_required` (links to `/settings?target=...`), `feature_unavailable`, `theme_toggle` (wired to the existing `next-themes` provider). **Read-only/deferred** (no traced write endpoint — brief explicitly said don't guess): `automation_flow`, `task_proposal`, `meeting_proposal` (informational content only; "confirm/insert" actions render visibly disabled with a tooltip), `super_agent_task`, `super_agent_question` (status/text only, no answer submission), `sms_compose` (templates as read-only text), `higgsfield_connect` (renders; its "Connect" link points at `registerUrl`, an external URL — no job-status polling). Each deferred component has an inline comment explaining why, mirroring this list.

**Dependency deviation — `@heroui-pro/react` could not be used as a working runtime import.** Installed at `1.0.0-beta.8` (compatible with the project's `@heroui/react ^3.2.4`) per the brief, and it **is** a real `package.json`/`package-lock.json` dependency — but the package ships no `main`/`exports` at all; its actual component source is distributed through a separate `heroui-pro` CLI (`npx heroui-pro install`) gated behind a paid license login (`npx heroui-pro login` / `HEROUI_AUTH_TOKEN`). The only credential available in this environment is the `heroui-pro` **MCP server's** personal token (`x-heroui-personal-token`, used for the docs/props lookups above) — confirmed by testing it as `HEROUI_AUTH_TOKEN` against the CLI, which rejected it: `Invalid or inactive CI/CD license key`. The MCP server itself only exposes Pro *documentation*, explicitly not Pro *source* ("Pro source is not exposed"). Net effect: any `import ... from "@heroui-pro/react"` is unresolvable and would fail `tsc`/`next build` — confirmed by attempting it. **Resolution**: the entire chat UI is built on `@heroui/react` (OSS, fully installed and working) primitives, hand-composed to mirror the Pro template's compound-component patterns and visual structure (thread sidebar, message bubbles, prompt composer, card grid) so a real Pro license can be dropped in later with mostly a swap, not a rewrite. Also added `react-markdown` `^9` + `remark-gfm` `^4` (real, working npm deps) for markdown message rendering, since Pro's `Markdown` component has the same unavailability problem, and no typography plugin was installed — added a small `.chat-markdown` ruleset to `globals.css` instead of pulling in `@tailwindcss/typography` for a handful of tags.
- [x] `tsconfig.json` / `eslint.config.mjs` — added `templates/**` to the exclude/ignore lists. The unzipped HeroUI Pro template under `templates/template-chat/template-chat/` was being picked up by both `tsc` and `eslint` (its own `@heroui-pro/react`/`@gravity-ui/icons` imports don't resolve either, for the same reason above) even though it's read-only reference material, not part of this app — this was already true before this pass, just never exercised since no prior feature imported anything that made `tsc`/`eslint` walk that far.

**Verification**: `tsc --noEmit`, `eslint .`, `next build` all clean (zero errors/warnings). `new-operatora-dev` (PM2, port **3033** — not 3000, that port is a different project) restarted and smoke-tested: unauthenticated `GET /dashboard` → 307 → `/login?next=%2Fdashboard` → 200, no server crash, no Next.js error overlay in the HTML. **Not verified** (same standing blocker as every prior phase): interactive click-through of an authenticated session — still no test account on `operatora_test`. Recommend exercising the actual SSE stream, card interactions, and realtime thread-list invalidation manually once a login exists.

- **Explicitly out of scope this pass (per the brief) — do not build later without re-reading the contract**:
  - `superAgent` composer toggle (backend field exists; not exposed in the UI).
  - Full `generated-media` generation flow (image/video-mode composer) and Higgsfield job-status polling — `generated_image`/`higgsfield_connect` cards render what the backend already sent, nothing more.
  - `insertPayload` consumption endpoints for `automation_flow`/`task_proposal`/`meeting_proposal` — untraced, so those cards are read-only with disabled actions.
  - `super_agent_task`/`super_agent_question` full lifecycle (answer submission, task progress updates).
  - `PlanLimitError`'s exact JSON shape — a `403` from thread creation is handled generically via `ApiError.isForbidden` (no specific plan-limit body parsing).
  - Signed-URL lookup for the `audio` card's `conversationId` (no such endpoint was in scope) — the player element is structurally present but has no playable `src` yet.
  - Message-level feedback (thumbs up/down via `POST /ai-chat/feedback`) — the `done` SSE event's `feedbackId` isn't persisted onto historical messages by the backend's `ThreadRow.messages` contract, so wiring it in would mean inventing an ad-hoc client-only field; only Copy is implemented for now.

### Phase 2b — Conversations (`/conversations`) (done 2026-08-12)

Second real feature page, read-only first pass — wired to the live `/conversation`, `/conversation/:id`, `/play-back/:id`, and `/fn/conversation-ai-assistant` backend endpoints. This is the first feature to use **TanStack Table** (v9 — see the dependency-version note below) for server-driven pagination.

- [x] `src/features/conversations/types.ts` (new) — hand-written `Conversation` domain type from the real backend contract (list/detail rows are the same shape), plus `parseTranscript()` (narrows the raw `unknown` transcript JSON into turn-by-turn array-or-raw-string, speaker matched case-insensitively by substring `"operator"`/`"client"`, falling back to alternating speaker1/speaker2 by even/odd index) and `parseEvaluationCriteria()` (handles both the array-of-`{name,score,feedback|note}` shape and the object-map `{criterionName: value}` shape via `Array.isArray`).
- [x] `src/services/api/conversations.ts` (new) — `conversationsApi.list/get` (list sends `workspace_id` as an explicit query param, the same confirmed exception to "backend derives workspace from JWT" that `chat.ts` established, with the same explanatory comment style), `getConversationAudioBlob(id, {download})` (raw authenticated `fetch` against `GET /api/play-back/:id`, since it returns binary bytes not JSON — mirrors `chat.ts`'s SSE streaming helper's auth-attach + single-flight-refresh pattern), and `conversationAiAssistantApi.send()`.
- [x] `src/features/conversations/hooks/` — `useConversationsQuery` (list, TanStack Query, `placeholderData` keeps the previous page visible while a new page/filter loads), `useConversationQuery` (detail, only fires once a row is selected), `useConversationAudio` (lazy blob fetch + object-URL lifecycle — loads only on `Play`/`Download` press, not on row select, so skimming the list doesn't fire audio requests; state resets on conversation change via the React-recommended render-time "adjust state on prop change" pattern rather than a setState-in-effect, to satisfy the project's `react-hooks/set-state-in-effect` lint rule), `useConversationAssistant` (local, non-persisted chat state for the AI-assistant panel — see below).
- [x] `src/features/conversations/components/` — `ConversationFilters` (search debounced via the existing `useDebounce`, status/operator/date-range/score-range — exactly the params the backend supports, nothing client-side-only), `ConversationsTable` (TanStack Table v9, server-driven `offset`/`limit` pagination, Prev/Next — no column-sort UI since sort is fixed server-side `created_at DESC`), `ConversationDetailPanel` (header, summary, key points, topics, compliance flags, disposition, follow-up actions, evaluation criteria, strengths/improvements, turn-by-turn transcript with operator/client sides visually distinguished), `ConversationAudioPlayer` (wired to `useConversationAudio`, play + download; no scrubber promises since the backend confirmed no `Range` support), `ConversationAssistantPanel` (the "ask about this conversation" chat UI).
- [x] `src/app/(protected)/conversations/page.tsx` (new) — list (filters + table) on the left, selected-row detail in a side panel on the right, same "list + local selection state" shape `/dashboard` established for its thread sidebar (not a separate route/URL param — no deep-link requirement in the brief). No request fires if `workspaceId` is null.
- [x] **AI-assistant sub-feature — confidently wired, not deferred.** Traced `POST /api/fn/conversation-ai-assistant` all the way through the read-only reference backend (`/www/wwwroot/dev.operatora/app/backend/src/functions/functions.controller.ts` → `functions.registry.ts` (`{name: 'conversation-ai-assistant', status: 'inline', notes: 'AiChatService(conversation)'}`) → `functions.handlers.ts`'s `aiChatHandler('conversation', body, user)` → `ai-ext/handlers/ai-chat.service.ts`'s `chat()`). Confirmed **not SSE** (unlike `/dashboard`'s `/ai-chat/v2`) — the controller `await`s the whole result and returns one JSON body: `{ reply: string; threadId?: string; model: string }`. Request body: `{ message, conversation, chatHistory, threadId?, workspaceId? }` — `chatMode` is set to `'conversation'` server-side by the dispatch table, the client never sends it. `conversation` is a free-form context object; only the fields `AiChatService.buildConversationContextBlock()` actually reads are sent (`client_name`, `operator_name`, `conversation_date`/`time`, `duration`/`duration_sec`, `ai_score`, `sentiment`, `status`, `summary`, `key_points`, `transcript`) rather than the full row. Implemented as a plain `apiFetch` POST (no SSE parsing needed), with local (non-persisted) message state per conversation.
- [x] Dependency note: `@tanstack/react-table` was already installed but at **v9.1.2**, not the v8-shaped API ARCHITECTURE.md's examples assumed — v9 is a genuine breaking rewrite (no `useReactTable`/`getCoreRowModel()`; instead `useTable({ features, columns, data })` + `table.FlexRender`, with row models registered as opt-in `tableFeatures` plugins). Confirmed via the package's own bundled `skills/*/SKILL.md` docs (`node_modules/@tanstack/react-table/skills/getting-started/SKILL.md`, `.../migrate-v8-to-v9/SKILL.md`) rather than guessing from v8 muscle memory. `ConversationsTable` renders as a plain semantic `<table>` (not HeroUI's react-aria-components-based `Table` compound, a different collection/render-prop model) so TanStack Table's row model stays the single source of truth.

**Explicitly out of scope this pass (per the brief — deliberate, not an oversight)**:
- Review/scoring (`ReviewScoreModal`) — no backend persistence exists for this at all.
- Link operator / link lead — only a legacy debug shim exists (`/api/db/conversations/query`), not a real endpoint.
- Delete a conversation — `DELETE /api/conversation/:id` is a backend stub (`"Delete not implemented yet"`).
- "Trigger processing" — calls an explicitly unported backend function (`process-audio-conversation`, `status: 'pending'`); would 501.
- Realtime live-updates on the list — old app has none either; plain fetch-on-mount + fetch-on-filter-change, matching the brief's confirmed parity call.
- The processing-queue monitor dialog — admin-only, polls a legacy shim table, its one action doesn't work; skipped entirely.
- `channel`/`sentiment` client-side-only filters — no server param exists for either; the filter bar only exposes what `GET /api/conversation` actually supports (search, status, operator, date range, score range), rather than building a filter that would misleadingly only apply to the current page.

**Verification**: `tsc --noEmit`, `eslint .`, `next build` all clean (zero errors/warnings). `new-operatora-dev` (PM2, port 3033) restarted; unauthenticated `GET /conversations` → 200, renders the `(protected)` layout's client-side auth-guard loading state (`"Loading your workspace…"`), no crash, no Next.js error overlay in the HTML — same expected shape as `/dashboard`. **Not verified** (same standing blocker as every prior phase): interactive click-through of an authenticated session — still no test account on `operatora_test`. Recommend exercising the list/filter/pagination round-trip, detail panel rendering (especially both `evaluation_criteria` shapes and the transcript speaker-matching heuristic against real data), audio playback/download, and the AI-assistant request/response manually once a login exists.

### Settings — structural skeleton (done, this pass)

Per explicit user decision: **navigation only, every section is a placeholder** — not a scoped-down feature slice like AI Chat/Conversations. The old app's `Settings.tsx` (855 lines, 40+ dedicated components — SIP/GSM telephony, Eskiz/Utel SMS, AmoCRM/Bitrix/Tilda CRM, Higgsfield, Google Calendar/Sheets, Telegram, Instagram, billing, security, roles/permissions, brand, lead automations, etc.) is far too large to build in one pass, so this pass only reproduces its **navigation structure**, ported from `config/app-sitemap.ts`'s `settingsSections` + `Settings.tsx`'s `navGroups`/`SECTION_META`:

- `src/constants/settings-sitemap.ts` — all 7 groups (Essentials, Workspace, Communication, Integrations, Lead Management, Billing, Account) × 23 sections, each with the old app's actual title/subtitle copy and a `@gravity-ui/icons` equivalent of its old lucide icon.
- `src/features/settings/components/{SettingsNav,SettingsPlaceholderPanel}.tsx` — grouped, collapsible left nav (visually matching `AppSidebar`'s button chrome) + a placeholder content panel (title/subtitle/"Coming soon").
- `src/app/(protected)/settings/page.tsx` — `?section=` deep-linking (matching the old app's `/settings?section=billing` convention, already referenced from `AppSidebar`'s account menu), wrapped in `Suspense` for `useSearchParams` per Next's static-generation requirement — confirmed via a full `next build`, not just dev-mode.

**Explicitly not built** (every section's actual form/integration UI — all deferred except `general`, see below):
notifications, team-members, departments, roles-permissions, storage, canned-responses, sip-config, gsm-lines, eskiz, utel, telegram, instagram, crm-integrations, google-sheets, google-calendar, claude-chatgpt, super-agent, higgsfield, brand, lead-automations, tasks, billing, security — all render `SettingsPlaceholderPanel` only. `adminOnly` is carried over as a label on each section (from the old app's role gating) but **not enforced** — there's no real content to protect yet.

**Verification**: `tsc --noEmit`, `eslint .` clean. Full `next build` (not just `tsc`) run specifically to confirm the `useSearchParams`/`Suspense` boundary is correct — `/settings` prerenders as static content, confirmed in the build's route table. Dev server restarted; unauthenticated `GET /settings` and `/settings?section=billing` both → 307 to `/login` (proxy.ts's `ROUTES`-derived protection picked up the new route automatically, no manual list to update). Not verified: authenticated click-through (standing blocker, no test account).

### General settings — real backend (done, this pass)

Following up per explicit request ("just implement general settings"). Real contract, reverse-engineered from the old frontend's `WorkspaceSettings.tsx` (its own request/response shapes, not guessed):
- `GET /workspace-settings?workspace_id=` — 404-tolerant (a workspace that's never saved settings before 404s; treated as empty defaults, not an error, matching the old frontend).
- `PUT /workspace-settings/company` — body `{ workspace_id, workspace_name, company: {...} }`.

Also real: workspace logo upload — `POST/storage/avatars/upload` (multipart, 5MB max, jpeg/png/webp/gif, server-side magic-byte-sniffed) + `POST/storage/avatars/remove`, path convention `workspaces/{workspaceId}/logo.{ext}` matching the old frontend, public URL built client-side (`avatars` bucket is `public: true`, no signed-URL round-trip needed). **Correction to an earlier note in this file**: this was first logged as deferred on the same reasoning as chat file-attach ("old frontend calls Supabase Storage directly, no REST endpoint") — that premise was wrong for this specific case. The old frontend's `supabase.storage` calls go through a compat shim (`src/lib/dbshim`) that itself calls this same real backend endpoint; confirmed by reading the backend's `storage-proxy/storage.controller.ts` directly, not assumed. **Known backend gap, not something this frontend can fix**: the `avatars` bucket has no server-enforced `pathScope: 'workspace'` (unlike e.g. `generated-media`), so workspace-scoped write isolation for logos is a path-naming convention only, not a guarded boundary — worth knowing if this ever needs hardening.

Files: `src/features/settings/{types,schema}.ts`, `src/services/api/settings.ts`, `src/features/settings/hooks/{useWorkspaceSettingsQuery,useUpdateCompanyMutation,useLogoMutation}.ts`, `src/features/settings/components/{GeneralSettingsForm,WorkspaceLogoUploader}.tsx` (React Hook Form + Zod, matching `LoginForm`'s established pattern), wired into the settings page for the `general` key only (everything else still `SettingsPlaceholderPanel`).

**Fields implemented**: logo, workspace name, default phone format (`+998 XX XXX XX XX` / `+7 XXX XXX XX XX`), default currency (UZS/USD), default language (uz/ru/en). The name/phone/currency are the only fields the old frontend actually exposes editable UI for; `language` was added as a real field on top of that (not just round-tripped) since it's clearly consumed elsewhere — e.g. AI Chat's `/ai-chat/v2` request body takes the same `uz`/`ru`/`en` set as its own `language` param — the old frontend tracking-but-never-exposing it reads as an oversight there, not an intentional omission. `timezone` is still round-tripped unchanged — no similar evidence it's consumed anywhere else, so no case yet to expose it as a control.

**Explicitly deferred, with reasoning** (not oversights):
- **Danger zone** (clear workspace data / transfer ownership / delete workspace) — real but separate, higher-risk endpoints (`/workspace-settings/danger/*`) needing their own confirmation-flow UI; out of scope for "general settings."
- **Notification preferences** (`email_new_leads`, Telegram notification toggles) — the old frontend bundles these into the same page/save action as company info, but this app's sitemap already treats `notifications` as its own separate settings section (`PUT /workspace-settings/notifications` is in fact a distinct endpoint from `/company`) — building it here would contradict our own navigation structure. Belongs in the `notifications` section when that's picked up.

**Verification**: `tsc --noEmit`, `eslint .` clean (one real fix along the way — `react-hooks/purity` correctly flagged a `Date.now()` call inside the submit handler as an impure-during-render risk; replaced the timestamp with a plain boolean `justSaved` flag since the timestamp value itself was never used; separately, caught and fixed a real bug of my own before it shipped — the logo-upload mutation initially derived `workspace_name` from `company.company_name`, which is only populated after a General-settings save has happened at least once, risking wiping the real workspace name on a first-ever logo upload; fixed to require the real top-level name explicitly). Full `next build` clean, `/settings` still prerenders as static content. Dev server restarted, `/settings?section=general` still correctly redirects unauthenticated requests. Not verified: an actual authenticated save/upload round-trip against live data (standing blocker, no test account/workspace exists yet to exercise this against).

### Settings — redesign + 5 real sections (done, this pass)

Two-part pass per explicit brief: (1) a full redesign of the settings shell/IA, not a content-only pass, and (2) real backend wiring for five more sections (Notifications, Team Members, Roles & Permissions, Security, Brand), on top of the `general` section already done. Same contract-tracing rigor as every prior phase — old frontend read for behavior, backend controllers/services read for the actual contract, nothing guessed.

**Part 1 — IA/layout redesign:**
- `src/constants/settings-sitemap.ts` — re-grouped, not just re-skinned. Added `SettingsSection.status?: "ready" | "soon"` (six sections now `"ready"`). Within each group, `"ready"` sections now sort first. The old 7-group structure is now 6: the old single-section "Billing" group folded into "Account" (renamed "Account & Billing"), since a 1-item group added a whole extra nav header for no organizational benefit. "Brand identity" moved from the bottom of Integrations to the top, now that it's real. "Roles & Permissions" moved up next to "Team Members" in Workspace (previously separated by "Departments", a placeholder).
- `src/features/settings/components/SettingsNav.tsx` — rebuilt away from `AppSidebar`'s bordered icon-rail button chrome. That treatment fit when every row was an equally-weighted placeholder; now that most rows hold real forms, a flatter list (no per-row border, a colored left accent bar + soft background only on the active row, small "Soon" tag on `"soon"` sections at reduced opacity) reads like an actual settings page instead of a second app nav, and visually foregrounds the six real sections without hiding the rest.
- `src/features/settings/components/SettingsSectionShell.tsx` (new) — the reusable per-section chrome (title/subtitle header + optional header actions + body) the brief asked for, established as *the* pattern instead of a one-off: every real section (General — refactored to use it — Notifications, Team Members, Roles & Permissions, Brand, Security) and `SettingsPlaceholderPanel` all render through it. A `wide` flag opts a section out of the capped ~720px body width (table/matrix-heavy sections — Team Members, Roles & Permissions) while simple field forms stay capped, continuing General settings' earlier width fix as a rule rather than a one-off.
- `src/app/(protected)/settings/page.tsx` — added a page-level header ("Settings" + one-line description) above the nav/content split; routes each of the six `"ready"` keys to its real component via a lookup table, everything else still `SettingsPlaceholderPanel`. `?section=` deep-linking and the `Suspense`/`useSearchParams` boundary unchanged.
- Design-system usage checked against `heroui-pro-design-taste`/`heroui-react-pro` skill guidance and the installed `@heroui/react` 3.2.4 type declarations directly (no MCP tools were reachable in this session — `get_component_docs` etc. errored as unavailable — so `Modal`/`Switch`/`Chip`/`Select.Value`/`TextArea`/`useOverlayState` anatomy was confirmed by reading `node_modules/@heroui/react/dist/components/*/index.d.ts` instead of guessing).

**Part 2 — five real sections:**

1. **Notifications** — `PUT /workspace-settings/notifications`, confirmed distinct from `/company` by reading `workspace-settings.controller.ts` directly (its own Swagger `@ApiBody` example: `{ email_new_lead: true, telegram_new_message: true }`). Reuses `useWorkspaceSettingsQuery` (the same `GET /workspace-settings` call already returns `.notifications`) rather than a second query. Files: `features/settings/{types,schema}.ts` additions, `services/api/settings.ts`'s `updateNotifications`, `features/settings/hooks/useUpdateNotificationsMutation.ts`, `features/settings/components/NotificationsSettingsForm.tsx`. **Deferred, deliberately**: the old frontend's much larger `NotificationRulesSettings.tsx` — a per-department Telegram-routing rule builder plus a personal Telegram account-link flow (`/notification-rules/*`, a wholly separate backend module) — is not this endpoint and not built here; only the two documented toggles get real controls, any other key already in the stored blob round-trips untouched.

2. **Team Members** — traced from the old frontend's `OperatorUsersManager.tsx`/`EditTeamMemberDialog.tsx` against the real, workspace-scoped (not global-admin-gated — permission enforced inside the service via `assertCanManageWorkspaceUsers`, a 403 surfaces via `ErrorState`) `auth-controller/admin-users/admin-users.controller.ts`: `GET /admin-users/operators` (bare array, no pagination envelope — confirmed via the service's plain `.map()` return), `POST /admin-users/operators` (invite), `PATCH /admin-users/operators/:userId` (profile/status/password), `DELETE /admin-users/operators/:userId` (remove, blocked for self/owner both client- and server-side). Files: `features/team/{types,schema}.ts`, `services/api/team.ts`, `features/team/hooks/{useTeamMembersQuery,useTeamMemberMutations}.ts`, `features/team/components/{TeamMembersPanel,InviteMemberModal,EditMemberModal}.tsx`. List is debounced-search + status-filtered (server-side params only, `placeholderData` keeps the previous page visible). The edit modal also assigns a workspace RBAC role (`PUT /workspace-rbac/users/:userId/roles`, single-select — matches the old frontend's "exactly one role" UX) via the Roles & Permissions data layer below. **Deferred, deliberately**: per-operator SIP/GSM/SMS-gateway sub-panels (their own settings sections — sip-config/gsm-lines — already reserve space for this, own scope), and the legacy per-operator permission checkboxes (`send_sms`/`delete_lead`/`edit_lead`/`make_call`/`view_all_leads`) — the old frontend itself labels these "legacy," fully superseded by the RBAC matrix below.

3. **Roles & Permissions** — traced from `RolesPermissions.tsx` against `settings-controller/workspace-rbac/workspace-rbac.controller.ts`/`.service.ts` (`/workspace-rbac/*`): `GET/POST/DELETE /workspace-rbac/roles`, `GET/PUT /workspace-rbac/roles/:id/permissions` (12-module × 6-action matrix, same module/action sets as the old frontend, English labels since this app has no i18n layer). Three system roles (Owner/Manager/Sales Operator) are auto-seeded server-side; system roles can't be renamed/deleted (matches backend validation, editing UI never offered). Files: `features/roles/{types}.ts`, `services/api/roles.ts`, `features/roles/hooks/{useWorkspaceRolesQuery,useRolePermissionsQuery,useRoleMutations,useUserRolesQuery}.ts`, `features/roles/components/RolesPermissionsPanel.tsx`. **This is also where `GET /workspace-rbac/me` — real `workspace_users`-backed effective-permission data — gets fetched client-side for the first time** (`useMyWorkspacePermissionsQuery`), rendered as a "Your access in this workspace" summary at the top of the panel. Explicitly scoped narrow, not the full fix for the still-open PROGRESS.md item: this wiring is local to this one section (display only), **not** extended into `AppSidebar`/global nav-level `requiresPage`/`canViewPage` gating — that stays cosmetic-only (`adminOnly` label, no enforcement) as before. Flagging this clearly rather than silently expanding scope: a real cross-cutting nav-permission pass is its own slice.

4. **Security** — traced from the real `settings-controller/admin-console/security.controller.ts` (`/security/*`): `POST /security/change-password` (bcrypt-verified current password), `GET /security/sessions` (active refresh-token sessions, read-only list — the backend has no per-session revoke, only revoke-all), `POST /security/force-logout` (revokes every session including the caller's own — the panel warns "Sign out everywhere?" before confirming, then clears local tokens/session state and redirects to `/login` immediately rather than waiting for the next 401), and the SMS-OTP-gated phone-number rebind flow (`POST /security/phone/request-otp` + `POST /security/phone/confirm`, two-step form). Files: `features/security/{types,schema}.ts`, `services/api/security.ts`, `features/security/hooks/useSecurityHooks.ts`, `features/security/components/SecuritySettingsPanel.tsx`. **Deferred**: no 2FA endpoint exists anywhere in the backend — confirmed by reading the whole controller, not assumed — so no 2FA UI was built (would be inventing a contract).

5. **Brand** — traced from `BrandProfilePanel.tsx` against `brand/brand.controller.ts`/`brand.service.ts` (`/brand/*`, persisted in `workspace_agent_profile`, admin/owner-gated server-side): `GET /brand`, `PUT /brand` (logo URL, up to 8 hex colors with optional names, up to 5 fonts, a free-text style description — all validated server-side, matched client-side in `features/brand/schema.ts`). Confirmed via `brand.service.ts` directly that saving requires a plain `https://` logo URL — there is no dedicated brand-logo upload endpoint (distinct from General settings' workspace-logo upload, which is a different bucket/purpose) — so the form takes a pasted URL, exactly like the old frontend. Files: `features/brand/{types,schema}.ts`, `services/api/brand.ts`, `features/brand/hooks/useBrandHooks.ts`, `features/brand/components/BrandSettingsForm.tsx`. Also wired the one AI-assisted intake path: `POST /brand/analyze-domain` extracts a draft (logo/colors/fonts/style) from a public domain and populates the same form fields for review — nothing auto-saves, matching the old frontend's "draft, not a write" behavior. **Deferred, deliberately**: `POST /brand/analyze-files` (screenshot/PDF extraction) — a separate multipart upload flow the old frontend treats as a secondary path behind the domain analyzer; out of scope for this pass, not forgotten.

**Verification**: `tsc --noEmit`, `eslint .` (full repo), `next build` all clean (zero errors/warnings) — two real lint fixes made along the way: a `react-hooks/set-state-in-effect` violation in the roles panel's role-auto-select logic (removed the sync effect entirely — the derived `selectedRole` fallback already does the right thing at render time, no state sync was actually needed) and a `react-hooks/incompatible-library` warning on Brand's `watch()` call (switched to `useWatch`, which the React Compiler can memoize safely). `next build`'s route table still shows `/settings` prerendering as static content. `new-operatora-dev` (PM2, port 3033) restarted; `/settings` and every new `?section=` value (team-members, roles-permissions, security, brand, notifications, and an untouched placeholder like billing) all still correctly 307-redirect to `/login?next=%2Fsettings`, following through to a 200 login page with no server crash or Next.js error overlay in the HTML — same standing check as every prior phase. **Not verified** (same standing limitation noted in every phase since Phase 2a): interactive click-through of an authenticated session — still no test account/workspace on `operatora_test`. Recommend exercising the invite/edit/remove-member round trip, the permission-matrix save, password change, force-logout's actual redirect, the phone-OTP flow, and the domain-analysis draft-fill manually once a login exists.

### Team Members — gap-closing pass: SIP/GSM/SMS devices, seats/billing, presence/activity, tabbed edit dialog (done, this pass)

Follow-up per explicit user request: a gap analysis against the old frontend's much richer `OperatorUsersManager.tsx` (1159 lines) + its sub-dialogs found several real backend contracts the first Team Members pass had deliberately deferred as "own scope." This pass closes all of them **except** the legacy per-operator permission checkboxes (`send_sms`/`make_call`/`edit_lead`/`delete_lead`/`view_all_leads`), which the user explicitly excluded — RBAC roles already supersede them functionally.

**Contracts traced (old frontend file → backend controller), all built as real CRUD, not placeholders:**
- **SIP accounts** — `WorkspaceSipAccountsPanel.tsx` → `admin-users.controller.ts`'s `operators/:userId/sip*` routes (`GET`/`POST` upsert/`POST :id/activate`/`DELETE :id`, all `workspace_id`-scoped). Multiple accounts per operator, exactly one active; password is returned by the backend (`mapOperatorSipPublic`) so admins can view/copy it, with a show/hide eye toggle.
- **GSM lines** — `OperatorGsmLinesPanel.tsx` → `settings-controller/gsm/gsm.controller.ts` (`GET/POST/PATCH/DELETE /gsm`, scoped by a `user_id` query param, **not** `workspace_id` — confirmed by reading the controller directly, the backend resolves the target purely from that param).
- **SMS gateways** — `OperatorSmsGatewayPanel.tsx` → `settings-controller/sms-gateways/sms-gateways.controller.ts` (`GET/POST/PATCH/DELETE /sms-gateways`, same `user_id`-scoping pattern, backend enforces `assertCanActForUser` for elevated-caller overrides).
- **Seats/billing** — `TeamSeatsPanel.tsx` + `OperatorUsersManager.tsx`'s `rentSeatMutation` → `billing.controller.ts` (`GET /billing/me`, `POST /billing/operator-seat/rent`). Rent flow: quantity stepper (1–20, matches backend's `Max(20)` on `RentOperatorSeatDto`) → price calc → confirm → provider choice (Payme — kill-switched off by default same as old, via `NEXT_PUBLIC_PAYME_PAYMENTS_ENABLED`/Click/Paylov popups) → 3s-interval reconciliation poll against `GET /billing/me` for `extra_operator_seats` reaching the target, hard-capped at 10 minutes and stopped on unmount/step-change. Upsell banner (linking to `/checkout?cycle=yearly`, the old frontend's own convention — that route isn't built in this app yet, out of scope here) shown instead when `!canAddOperators(billing)`.
- **Presence** — `useWorkspacePresence.ts` → `presence.controller.ts` (`GET /presence/workspace`, `VIEW_PRESENCE_ROLES` = owner/admin/manager/sales_manager, reproduced client-side verbatim as `VIEW_PRESENCE_ROLES` in `features/team/types.ts` so the UI never fires a request the server would 403). Live online dot + last-seen, updated via a new `subscribeToWorkspacePresence` entry in `services/realtime/subscriptions.ts` (reuses the existing AI-Chat socket infrastructure — same `workspace:{id}` topic, listens for `presence_changed` instead of a table-invalidation payload) rather than standing up a second realtime channel.
- **Activity summary ("active today")** — `useOperatorActivitySummary.ts` → confirmed live at `operator-activity/operator-activity.controller.ts` (`GET /operator-activity/summary`, same view-role gate, defaults to today's date range) — the prior pass's search had missed this controller file; found this pass via a direct `find`, so it's real, not deferred.
- **Status model** — old frontend's `memberStatus()` derives exactly 3 display states (not 4, despite its own comment mentioning a 4th) from `status`/`last_login_at`: `deactivated` (status inactive) → `invited` (active but never logged in) → `active`. Reproduced verbatim as `memberDisplayStatus()` in `features/team/types.ts`, with a code comment explaining the 3-vs-4 discrepancy so a future pass doesn't "fix" it into a state the old app never actually had.

**Built, this pass:**
- Tabbed Edit Member dialog (`EditMemberModal.tsx`, rewritten) — Profile / Role / SIP tabs via HeroUI `Tabs`. Footer: Save (hidden on the SIP tab, "Done" instead — matches old behavior exactly) / Cancel, "Remove member" as a footer-start danger action hidden for self/owner. Tab reset on open uses the same render-time "adjust state on prop change" pattern already established by `useConversationAudio` (not a `useEffect`, avoids `react-hooks/set-state-in-effect`).
- `features/team/components/sip/{SipAccountsPanel,GsmLinesPanel,SmsGatewayPanel}.tsx` — full CRUD per the contracts above, each scoped to the operator being edited, stacked inside the SIP tab exactly as in old.
- `features/team/components/{TeamSeatsPanel,RentSeatModal,UpgradeToProBanner}.tsx`, `features/team/operatorSeats.ts` (ported 1:1 from old's `lib/operatorSeats.ts` seat-layout math), `features/team/hooks/useBilling.ts` (`useBillingFeaturesQuery`, `useRentSeatMutation`, `useSeatPurchasePolling`).
- `features/team/components/{MemberStatusChip,MemberRowMenu,MobileMemberCard,DeleteMemberConfirm}.tsx` — 3-state status chip; row action dropdown (Edit/Role/SIP & devices/Remove) with tab-targeted deep links into `EditMemberModal`; a genuinely separate mobile stacked-card view (`md:hidden`) alongside the desktop table, not a CSS reflow; type-to-confirm delete (must type the member's email) replacing the old plain confirm, reused both from the row menu and the edit dialog's footer.
- `features/team/hooks/{useWorkspacePresence,useOperatorActivitySummary,useOperatorSip,useOperatorGsm,useOperatorSmsGateways}.ts`, `services/api/{sip,gsm,smsGateways,billing,presence,activity}.ts`.
- `TeamMembersPanel.tsx` rewired: seats panel + upsell banner above the toolbar, SIP quick-link column ("Configure" / "N SIP") opening the edit dialog on the SIP tab, live-status/active-today columns gated to `VIEW_PRESENCE_ROLES` (falls back to a plain "last active" relative-time column for everyone else), row menu, mobile card list.

**Explicitly out of scope this pass (per user decision)**: legacy per-operator permission checkboxes — not added anywhere (invite dialog, edit dialog's Role tab, or elsewhere); RBAC roles are the only access-control surface this app exposes.

**Verification**: `tsc --noEmit`, `eslint .` (full repo), `next build` all clean (zero errors/warnings) — three real lint fixes made along the way, all `react-hooks` violations from the new polling/presence hooks: a ref-mutated-during-render error in `useSeatPurchasePolling` (moved the ref sync into its own `useEffect`), and two `set-state-in-effect` violations (`useSeatPurchasePolling`'s timeout-flag reset and `useWorkspacePresence`'s disabled-state reset), both fixed by switching to the same render-time "adjust state on prop change" pattern used elsewhere in this codebase instead of resetting inside the data-fetching effect. `new-operatora-dev` (PM2, port 3033) restarted; `/settings?section=team-members` still correctly 307-redirects unauthenticated requests to `/login?next=%2Fsettings`, following through to a clean 200 login page with no server crash or Next.js error overlay in the HTML. **Not verified** (same standing limitation as every prior phase): interactive click-through of an authenticated session — still no test account/workspace on `operatora_test`. Recommend exercising, once a login exists: the SIP/GSM/SMS CRUD round-trips (especially password-blank-keeps-existing on edit), the rent-seat flow against a real payment provider popup and its reconciliation poll, presence going live via the socket, the activity-today numbers against real `operator_activity` data, and the tab-targeted deep links from the row menu.

### Departments, File Storage, Canned Responses — 3 more real settings sections (done, this pass)

Follow-up per explicit user request: build these three as real, working features, same contract-tracing rigor as every prior pass (old frontend read in full, backend controllers/services read directly, nothing guessed). None of the three turned out to be what the brief's own guesses assumed — noted below.

1. **Departments** — not an org chart. It's AI-agent escalation routing: a department has 0+ contacts and a `routing_prompt` the escalation classifier uses to auto-pick a department, plus a notify mode (DM each member's Telegram, or post to one shared workspace Telegram group and @mention members). Traced from `DepartmentsManager.tsx`/`WorkspaceGroupConnect.tsx`/`RoutingPromptField.tsx`/`MemberRow.tsx` against the real `departments/department.controller.ts` (`/departments/*`, workspace owner/admin-gated server-side via `assertCanManage` — no `workspace_id` param anywhere, the backend derives it purely from the JWT, confirmed by reading the controller directly): `GET /departments` (departments + nested members), `POST/PATCH/DELETE /departments[/:id]`, `POST/PATCH/DELETE /departments/:id/members[/:memberId]`, `GET /departments/workspace-group` + `POST /departments/workspace-group/verify-code` (the shared-group connect flow, with the old frontend's own 3s auto-poll-while-pending behavior reproduced), `POST /departments/:id/refine-routing-prompt` (AI rewrites the routing prompt before saving — best-effort, saves the raw text if refine fails, matching old exactly), `POST /departments/:id/test-notify`. Files: `features/departments/types.ts`, `services/api/departments.ts`, `features/departments/hooks/useDepartments.ts`, `features/departments/components/{DepartmentsPanel,DepartmentCard,MemberRow,RoutingPromptField,WorkspaceGroupConnect}.tsx`. Nothing deferred — the full contract is real and was built in full, including the explicit-Save-not-blur-autosave pattern on the routing-prompt field (ported verbatim from the old frontend's own reasoning: blur-autosave on that field once saved a stray word mid-dictation in prod).

2. **File Storage** — the old frontend's `StorageManager.tsx` computed "usage" via a client-side crawl of raw Supabase Storage buckets (`supabase.storage.from(bucket).list()`, paginated 1000 at a time, across **all workspaces** — none of `conversations`/`avatars`/`product-files` are workspace-scoped at the storage layer, confirmed in the real backend's `storage-proxy/buckets.ts`: no `pathScope: 'workspace'` on any of them), then filtered only the `conversations` bucket down to this workspace's own files by cross-referencing every one of its conversation rows' `audio_file_path` — `avatars`/`product-files` were never filtered per-workspace even there (the old file's own `// TODO: filter by workspace in future` comment). The equivalent real endpoint, `GET /storage/:bucket/list` (`storage-proxy/storage.controller.ts`), has that exact same gap. Reproducing it here would either mislabel platform-wide totals as this workspace's, or hand back every other tenant's file names/sizes to any authenticated caller — declined to build that, even though the old app effectively did the same thing. Instead, found and used a **better, already-workspace-scoped real contract**: `GET /billing/me`'s `usage.storage_mb` (`billing/plan-limits.service.ts`'s `getStorageUsageBytes()` — a live SQL `SUM` across every size-tracked table: conversations audio, `agent_knowledge_sources`, `product_files`, `generated_media`, `telegram_messages`, all `WHERE workspace_id = $1`), the exact same number the backend enforces its plan storage cap against (`assertCanStore`). Extended the existing `BillingFeatures`/`billingApi.me()` (already used by Team Members' seat panel) with this `usage` field rather than adding a new endpoint call. UI: total used vs. plan limit (or "Unlimited"), a progress bar that turns red and surfaces an upgrade link past 85%, and the plan's `storage_retention_days` shown read-only. Files: `features/storage/components/StoragePanel.tsx`, plus the `BillingFeatures`/`billingApi.me()` extension in `features/team/types.ts`/`services/api/billing.ts`. **Deliberately not built** (both real gaps in the old app itself, not oversights here): the bulk-delete-by-date-range cleanup flow, since it terminates in a full conversation-row delete and `DELETE /conversation/:id` is still a backend stub (`"Delete not implemented yet"`, same finding as the Conversations settings pass); the retention-policy dropdowns (auto-delete audio/transcripts, compress-after-N-days) — in the old frontend these were pure decorative local `useState`, no save action, no backend endpoint at all, so building real controls for them would be inventing a contract that doesn't exist anywhere.

3. **Canned Responses** — shortcut replies (operators type `/greeting`, it expands to the saved message) for chat channels; only Telegram is a wired delivery channel today, Instagram/WhatsApp/SMS are selectable-but-disabled in the old UI ("soon") and reproduced identically rather than guessing they're live. The old frontend reads/writes this via `supabase.from('canned_responses')` directly — no dedicated REST controller — but that call is **not** talking to Supabase: it's routed through the old frontend's own compat shim (`src/lib/dbshim/table-query.ts`) to a real backend endpoint, `POST /db/:table/query` (`db-proxy/db-proxy.controller.ts`), the same generic Supabase-compat proxy pattern already found and used for the `avatars` bucket in the General settings pass. Confirmed `canned_responses`'s registry entry directly (`db-proxy/table-registry.ts`: `{ scope: 'workspace', writeRoles: MANAGER_ROLES }` — workspace scoping and role gating enforced server-side from the JWT, no `workspace_id` ever sent by the client) and the real Postgres schema (`prisma/schema.prisma`: `@@unique([workspace_id, shortcut])`, `channels: Json`, `display_order: Int`). Built a small typed `dbProxyQuery()` client (`services/api/db-proxy.ts`) as the first user of this compat seam, narrowly scoped to tables with no dedicated controller of their own (not a general ORM client). **Caught and fixed a real bug before it shipped**: `db-proxy.service.ts`'s insert path returns a *single row object* (not an array) when `values` is sent as a bare object, only returning `rows[]` when `values` is itself an array — traced this directly in the backend source rather than assuming Supabase-insert-shape parity, and send `values` as a 1-element array specifically so the response shape matches this client's `CannedResponseRow[]` typing consistently. Files: `features/canned-responses/{types,schema}.ts`, `services/api/cannedResponses.ts`, `features/canned-responses/hooks/useCannedResponses.ts`, `features/canned-responses/components/{CannedResponsesPanel,CannedResponseModal}.tsx`. Full CRUD built, nothing deferred.

**Wired in**: `constants/settings-sitemap.ts` — `departments`, `storage`, `canned-responses` flipped to `status: "ready"`; `app/(protected)/settings/page.tsx`'s `READY_SECTION_COMPONENTS` map extended. Nine sections are now real (General, Notifications, Team Members, Roles & Permissions, Brand, Security, Departments, File Storage, Canned Responses); ~14 remain `SettingsPlaceholderPanel`.

**Verification**: `tsc --noEmit`, `eslint .` (full repo), `next build` all clean (zero errors/warnings) on the first attempt after the insert-shape bug fix above (no lint violations this pass — no new `react-hooks` issues, `useWatch` used from the start in `CannedResponseModal` rather than `watch()`, matching the lesson already recorded from the Brand form). `new-operatora-dev` (PM2, port 3033) restarted; `/settings?section=departments`, `?section=storage`, `?section=canned-responses` all correctly 307-redirect unauthenticated requests to `/login?next=%2Fsettings`, following through to a clean 200 login page with no server crash or Next.js error overlay in the HTML — same standing check as every prior phase. **Not verified** (same standing limitation as every phase since Phase 2a): interactive click-through of an authenticated session — still no test account/workspace on `operatora_test`. Recommend exercising, once a login exists: department create/edit/delete and the member blur-autosave fields (especially the numeric chat-ID validation), the workspace-group connect-code flow end to end (bot actually posting `/connect <code>` back), the routing-prompt AI-refine call, storage usage numbers against a workspace with real uploaded files (including the near-limit red-bar/upgrade-link state), and the canned-response create/edit/delete round trip (especially the `workspace_id + shortcut` uniqueness constraint surfacing a sane error, not a raw Postgres message, on a duplicate shortcut).

### SIP Configuration, GSM Lines, Telegram — 3 more real settings sections; resumed from an interrupted prior run (done, this pass)

Follow-up per explicit user request, with an added wrinkle: a prior agent run on this exact task had been killed mid-work (user misclick, not a real stop), leaving partial/unverified files on disk. This pass's first step was auditing every one of those files against the real backend contract independently — not trusting the interrupted run's own trace comments — before building anything new.

**Audit of the partial work, file by file:**
- `src/features/telephony/types.ts`, `src/services/api/telephony.ts`, `src/features/telephony/hooks/useWorkspaceTelephony.ts` — re-traced independently against the old frontend's `SipConfiguration.tsx`/`TelephonyGsmLines.tsx` and the real `admin-users.controller.ts` (`GET /admin-users/workspace-telephony/sip`, `GET /admin-users/workspace-telephony/gsm`, both `?workspace_id=`). Contract, types, and query param names all checked out exactly — **kept as-is**, no changes.
- `src/features/telegram/types.ts`, `src/services/api/telegram.ts`, `src/features/telegram/hooks/useTelegramIntegrations.ts` — re-traced against the old frontend's `TelegramIntegration.tsx` and the real `telegram-integration.controller.ts`/`.service.ts` (`GET/POST /telegram-integration`, `GET/PATCH/DELETE /telegram-integration/:id`, `POST /:id/test`, `POST /:id/set-webhook`, `POST /:id/remove-webhook`, `GET /:id/webhook-info`; create/delete are workspace-owner-gated server-side via `assertWorkspaceOwner`, confirmed directly in the controller). Also independently verified the killed run's claim that this is unrelated to Departments' per-member Telegram fields (confirmed — `MemberRow.tsx` never calls a contacts-list endpoint, no shared seam to reuse). Contract, types, and the update-path's decision not to send `workspace_id` (harmless per-the-service's plain `.update(dto)` merge, confirmed by reading `telegram-integration.service.ts` directly) all checked out — **kept as-is**.
- `src/features/telephony/components/GsmLinesPanel.tsx`, `src/features/telephony/components/SipConfigPanel.tsx` — structurally sound (correctly reuse Team Members' already-built `SipAccountsPanel`/`GsmLinesPanel` per-operator components inline per roster member, matching signatures exactly), but **one real bug found and fixed**: both imported `Crown` from `@gravity-ui/icons`, which doesn't exist in that package (the killed run likely pattern-matched the old frontend's `lucide-react` `Crown` import without checking the new icon set) — `tsc` caught it immediately. Fixed to `CrownDiamond`, the closest real equivalent in `@gravity-ui/icons`. No other issues.
- `src/features/team/hooks/useOperatorSip.ts`, `src/features/team/hooks/useOperatorGsm.ts` — these turned out to be legitimate integration work from the same interrupted pass (not leftover from an earlier one): both were extended with extra `queryClient.invalidateQueries` calls targeting `["workspace-telephony-sip", …]`/`["workspace-telephony-gsm"]` so editing an operator's SIP/GSM devices from their Team Members edit dialog keeps this pass's new workspace-roster views in sync. Correct and necessary — **kept as-is**.
- `src/features/canned-responses/components/CannedResponseModal.tsx` — checked for stray edits per the brief's caution; the only `telegram`-adjacent text found was the pre-existing channel-selector default (`channels: ["telegram"]`), unrelated to this task and untouched by the interrupted run. **No action needed.**
- **Not yet built by the killed run** (confirmed by absence, not by reading a half-finished version): `src/features/telegram/components/` didn't exist at all — the Telegram section had no UI component yet, only its service/hook/types layer. `settings-sitemap.ts` and `settings/page.tsx`'s `READY_SECTION_COMPONENTS` were untouched for all three sections, exactly as anticipated given the kill timing.

**What each section actually is (re-confirmed independently, not assumed from the killed run's comments):**
1. **SIP Configuration** (`sip-config`) — traced from the old frontend's `SipConfiguration.tsx`: **not** a distinct backend contract from the per-operator SIP tab already built for Team Members. It's the exact same `GET/POST/:id/activate/DELETE /admin-users/operators/:userId/sip*` CRUD, just surfaced as one workspace-wide roster (every member, `GET /admin-users/workspace-telephony/sip?workspace_id=`) with each member's SIP panel expanded inline — so telephony admins don't have to open each member's edit dialog individually. Built as `SipConfigPanel.tsx`, reusing `features/team/components/sip/SipAccountsPanel.tsx` per member, no duplicated CRUD logic.
2. **GSM Lines** (`gsm-lines`) — same shape and same conclusion, traced from `TelephonyGsmLines.tsx`: reuses the `user_id`-scoped `GET/POST/PATCH/DELETE /gsm` contract already built for Team Members' GSM tab, surfaced as a `GET /admin-users/workspace-telephony/gsm?workspace_id=`-backed roster. Built as `GsmLinesWorkspacePanel` (in `GsmLinesPanel.tsx`), reusing `features/team/components/sip/GsmLinesPanel.tsx` per member.
3. **Telegram** (`telegram`) — traced from `TelegramIntegration.tsx`: real bot integration management (a workspace may have several bots), token entry, connectivity test, webhook set/remove/status-check, delete. **Confirmed not related to Departments' Telegram contacts** — those are free-text per-member fields with no backing endpoint, this is a genuinely separate bot-integration module (`/telegram-integration/*`). Built as `TelegramSettingsPanel.tsx` (new component, the one piece of real UI the interrupted run hadn't reached yet): an "Add a bot" card plus a card per connected bot (status chip, masked/editable token, Test/Set-webhook/Remove-webhook/Check-status actions, webhook URL with copy, delete with a confirm modal). Create/delete are gated client-side to the workspace owner (`workspace_role === "workspace_owner" || "owner"` from `GET /workspace-rbac/me`, already fetched for the Roles & Permissions section, reused here rather than adding a second permission call) — mirrors the old frontend's `useWorkspaceOwner` gate exactly, since the backend 403s non-owners on create/delete.

**Wired in**: `constants/settings-sitemap.ts` — `sip-config`, `gsm-lines`, `telegram` flipped to `status: "ready"`, reordered within the Communication group so `"ready"` sections lead (Telegram was slotted after two still-`"soon"` sections in the original array order); `sip-config`'s subtitle also corrected — it previously described a "default trunk vs. per-user extensions" split that isn't what the traced contract or the built UI actually does. `app/(protected)/settings/page.tsx`'s `READY_SECTION_COMPONENTS` extended with all three. Twelve sections are now real; ~11 remain `SettingsPlaceholderPanel`.

**Nothing deferred** — all three sections' full traced contracts were built, no sub-feature skipped for lack of a confirmed endpoint.

**Verification**: `tsc --noEmit`, `eslint .` (full repo), `next build` all clean (zero errors/warnings) — one real fix required first (the `Crown` icon import bug above, caught by `tsc`). `new-operatora-dev` (PM2, port 3033) restarted; `/settings?section=sip-config`, `?section=gsm-lines`, `?section=telegram` all correctly 307-redirect unauthenticated requests to `/login?next=%2Fsettings`, following through to a clean 200 login page with no server crash or Next.js error overlay in the HTML — same standing check as every prior phase. **Not verified** (same standing limitation as every phase since Phase 2a): interactive click-through of an authenticated session — still no test account/workspace on `operatora_test`. Recommend exercising, once a login exists: the workspace-wide SIP/GSM rosters against a workspace with several members (especially that editing from within the roster view keeps Team Members' own SIP/GSM counts in sync, and vice versa, via the shared query-invalidation wiring), and the full Telegram bot lifecycle (add → test → set webhook → confirm Telegram actually calls back → check status → remove webhook → delete), including confirming a non-owner workspace member sees the create/delete controls correctly disabled rather than getting a raw 403.

#### Eskiz, Utel, Instagram — 3 more real settings sections (done, this pass)

Follow-up per explicit user request, with an explicit instruction not to assume the brief's own framing was correct — traced all three from scratch against the old frontend and real backend controllers/services before writing anything.

1. **Eskiz** (`eskiz`) — a real, dedicated SMS module, not a lookalike of the generic `/sms-gateways` CRUD. Traced from `EskizSettings.tsx`/`EskizTopUpPanel.tsx`/`useEskiz.ts` against `backend/src/eskiz/eskiz.controller.ts` (`/eskiz/*`, its own `eskiz_accounts`/`eskiz_templates`/`eskiz_messages` tables) and `billing.controller.ts`'s `POST /billing/eskiz/top-up`. Built the full subset the settings sitemap's own subtitle promises: account connect/disconnect (`POST/DELETE /eskiz/account`, `assertWorkspaceOwner`-gated), balance sync (`POST /eskiz/balance/sync`), wallet top-up (Click/Paylov, Payme kept behind the same `NEXT_PUBLIC_PAYME_PAYMENTS_ENABLED` kill-switch precedent `RentSeatModal.tsx` established), templates (list/submit/sync-from-Eskiz/resubmit-rejected), paginated+filtered history, and a period-scoped reports summary. Files: `features/eskiz/{types,hooks/useEskiz}.ts`, `services/api/eskiz.ts`, `features/eskiz/components/EskizSettingsPanel.tsx` (six-tab layout: Overview/Connect/Top up/Templates/History/Reports, `@heroui/react` `Tabs`). **Deliberately deferred, matching the project's own settings-vs-messages IA split** (same reasoning as Telegram/SIP/GSM being settings-only): chats/messages listing, single-recipient send, bulk-send-to-leads — these are real endpoints too (`/eskiz/chats`, `/eskiz/send`, `/eskiz/send/bulk`) but belong to the Messages feature (`EskizChannelPanel.tsx`/`SmsChannelPanel.tsx`/`ComposeSMSDialog.tsx` in the old frontend), not Settings, and aren't built anywhere in this app yet.

2. **Utel** (`utel`) — turned out to be **not** a real dedicated integration in the old frontend at all. `UtelSettings.tsx`'s entire body is `return <TelephonySmsGateways />;` with its own comment: *"uses SMS gateway UI until dedicated Utel module exists."* That component calls the exact same generic `/api/sms-gateways` CRUD already built here for Team Members' per-operator SMS tab (`features/team/components/sip/SmsGatewayPanel.tsx`), just for the caller's own user id (no `user_id` override — confirmed in `sms-gateways.controller.ts`'s `resolveUserId()`). Built `features/utel/components/UtelSettingsPanel.tsx` as a thin wrapper reusing `SmsGatewayPanel` for `useSessionStore`'s own user id — same contract, not a new one, matching old behavior exactly rather than inventing Utel-specific fields that don't exist anywhere in the old UI. **A genuinely separate, Utel-branded backend module does exist** (`backend/src/ai-ext/utel/utel.service.ts`, dispatched via `POST /api/fn/utel-auth`/`utel-call-history`/`utel-sync-conversations` — login/password auth, call-history pull, conversation sync) but was **not built against**: its `auth()` method does a raw SQL `INSERT INTO external_api_config (workspace_id, provider, config, updated_at)`, and the real table (`backend/prisma/schema.prisma`'s `external_api_config` model) has none of those columns — only `api_name`/`base_url`/`email`/`encrypted_password`/`api_token`/etc., no `workspace_id` or `provider`, and no migration adds them. Confirmed no settings UI in the old frontend calls it either (dead code there too). Building a connect UI against it would hit a "column does not exist" 500, not a working feature — declined per the project rule against fabricating a contract, with the finding documented inline in `UtelSettingsPanel.tsx` so a future pass doesn't rediscover this the hard way.

3. **Instagram** (`instagram`) — a real Meta OAuth integration (Instagram Business account via "Instagram Login" or "Facebook Login for Business" surfaces), genuinely distinct from Telegram's bot-token flow. Traced from `InstagramIntegration.tsx`/`useInstagramOAuthFlow.ts`/`InstagramCallback.tsx`/`InstagramOAuthSelectDialog.tsx` against `backend/src/instagram/instagram.controller.ts` (`/instagram/*`). Scoped to the settings-relevant subset (matching the sitemap subtitle, same split as Eskiz's chats/send): `GET /instagram/oauth-url` → popup → Meta redirect → a new standalone `/instagram-callback` page (`app/instagram-callback/page.tsx`, deliberately **outside** `(protected)` — no `AppShell` chrome wanted in a popup, but it still works authenticated since the popup shares the opener's origin `localStorage` Bearer token) → `POST /instagram/oauth-callback` → either immediate success or a page-selection dialog (`POST /instagram/oauth-connect`) when Meta returns multiple candidate Pages — relayed back to the settings tab via `postMessage`, 1:1 with the old frontend's flow. Plus account list (`GET /instagram/accounts`), webhook resubscribe retry, and disconnect with an optional "also delete conversations" checkbox (`DELETE /instagram/accounts/:id`). `oauth-url`/`oauth-callback`/`oauth-connect`/disconnect are workspace-owner-gated server-side, reproduced client-side the same way as Telegram/Eskiz (`GET /workspace-rbac/me`). Files: `features/instagram/{types,hooks/useInstagramAccounts,hooks/useInstagramOAuthFlow}.ts`, `services/api/instagram.ts`, `features/instagram/components/{InstagramSettingsPanel,InstagramOAuthSelectDialog}.tsx`, `app/instagram-callback/page.tsx`. **Deferred, flagged not fabricated**: conversations/messages/send/suggest-reply (Messages-feature scope, same split as Eskiz/Telegram); the actual working redirect requires the backend's `INSTAGRAM_REDIRECT_URI` env to point at this app's deployed `/instagram-callback` URL and a Meta App registered with that exact URI — an infra/deployment detail outside this frontend's control, called out in the component's own doc comment rather than silently assumed to already be configured.

**Wired in**: `constants/settings-sitemap.ts` — `eskiz`, `utel`, `instagram` flipped to `status: "ready"`; `app/(protected)/settings/page.tsx`'s `READY_SECTION_COMPONENTS` extended; `constants/routes.ts` gained `instagramCallback` so `proxy.ts`'s auto-derived protected-prefix list covers the new standalone page too. Fifteen sections are now real; ~8 remain `SettingsPlaceholderPanel`.

**Verification**: `tsc --noEmit`, `eslint .` (full repo), `next build` all clean (zero errors/warnings) — one real lint fix along the way (an unescaped apostrophe in Eskiz's top-up copy, `react/no-unescaped-entities`). `next build`'s route table shows `/instagram-callback` prerendering as static content alongside `/settings`. `new-operatora-dev` (PM2, port 3033) restarted; `/settings?section=eskiz`, `?section=utel`, `?section=instagram`, and `/instagram-callback` all correctly 307-redirect unauthenticated requests to `/login?next=...`, following through to a clean 200 login page with no server crash or Next.js error overlay in the HTML — same standing check as every prior phase. **Not verified** (same standing limitation as every phase since Phase 2a): interactive click-through of an authenticated session — still no test account/workspace on `operatora_test`. Recommend exercising, once a login exists: the Eskiz connect/disconnect round-trip against real Eskiz credentials, template submit/sync/resubmit, history pagination/filtering, the top-up popup against a real Click/Paylov checkout; the Utel section's reused SMS-gateway CRUD (already covered by Team Members' own verification notes, but worth re-confirming under the Utel label); and the full Instagram OAuth popup round-trip end-to-end against a real Meta App/redirect URI (single-account auto-connect, multi-Page selection dialog, resubscribe, disconnect with/without deleting conversations) — none of which can be exercised without a live Meta App registration and a logged-in session in this environment.

#### CRM Integrations, Google Sheets, Google Calendar — 3 more real settings sections (done, this pass)

Follow-up per explicit user request, same rigor as every prior pass: traced the old frontend and real backend controllers for all three before writing anything, rather than trusting the brief's own framing.

1. **CRM Integrations** (`crm-integrations`) — turned out to be **two unrelated backend modules rendered as two stacked panels**, not one merged form, matching the old frontend's `Settings.tsx` (`BitrixIntegrationSettings` + `AmocrmIntegrationSettings`) exactly:
   - **Bitrix24 + Tilda**, generic `integration_connections` CRUD, traced from `BitrixIntegrationSettings.tsx` against `integrations/integrations.controller.ts` (`GET/POST/PATCH/DELETE /integrations`) — workspace-scoped via JWT, **not** gated to workspace owners server-side (confirmed by reading the controller directly), so no client-side owner check was applied. Each connection gets a generated webhook URL + secret; Tilda auto-maps Name/Phone/Email, Bitrix24 gets the old frontend's default 4-field mapping pre-filled. Also documents the generic public lead-intake API (`POST /public/v1/leads` + `x-api-key`).
   - **amoCRM**, a fully separate dedicated module — long-lived private-integration access token (no OAuth), traced from `AmocrmIntegrationSettings.tsx` against `amocrm/amocrm.controller.ts` (`/amocrm/*`, every mutating route `assertWorkspaceOwner`-gated, workspace derived from JWT only). Full flow built: connect, status (polls every 2s while `status === "importing"`, matching old exactly), preview-boards/preview-operators before import, operator-mapping modal (create/map/skip per amoCRM user, seat-availability aware), background import job, one-time pending-credentials reveal for newly created operator accounts.
   - Files: `features/crm/types.ts`, `services/api/crm.ts` (`amocrmApi` + `integrationsApi`), `features/crm/hooks/{useAmocrm,useIntegrations}.ts`, `features/crm/components/{CrmIntegrationsPanel,AmocrmPanel,BitrixTildaPanel,AmocrmBoardsModal,AmocrmMappingModal}.tsx`. Nothing deferred — both traced contracts built in full.

2. **Google Sheets** (`google-sheets`) — one shared **workspace-level** Google OAuth connection driving two independent features, traced from `GoogleSheetsIntegration.tsx`/`useGoogleSheets.ts`/`useGoogleSheetsExport.ts`/`useGoogleSheetsImportSources.ts` against `settings-controller/google-sheets-controller/google-sheets.controller.ts` (`/google-sheets/*`, ~15 routes, every route takes an explicit `workspace_id`, permission is `assertCanManageGoogleSheets` — any workspace member or global admin, **not** owner-only, confirmed in the service directly): **import** (primary sheet + any number of additional named "import sources," each with its own board/column/field-mapping/ignored-columns and optional realtime Drive-webhook auto-sync, gated by an "existing rows" dialog — import-all vs. skip-existing — before first enabling sync on a never-synced sheet) and **export** (create/link a spreadsheet, one-time full-board sync, or auto-append new leads). Board/column pickers needed a real backend source since the old frontend called Supabase directly for `leads_boards` — added a deliberately narrow `services/api/leadsBoards.ts` (`GET /boards`, `GET /boards/:id/columns`) rather than a general Leads data layer, since the full Leads feature isn't built yet (see "Remaining feature work" below). Files: `features/google-sheets/{types,hooks/{useGoogleSheets,useLeadsBoards}}.ts`, `services/api/googleSheets.ts`, `features/google-sheets/components/{GoogleSheetsSettingsPanel,ImportSourceRow,AddImportSourceModal,ExistingLeadsDialog}.tsx`, `app/google-sheets-callback/page.tsx`. **Caught and fixed a real response-shape bug before it shipped**: the old frontend's hooks assume every mutating config endpoint returns `{integration}` wrapped the same as `status`, but `updateConfig`/`updateImportConfig`/`updateExportConfig`/`createExportSheet` actually return `{success, integration}` (no `connected` field) and `createImportSource`/`updateImportSource` return `{success, source}` (not `{sources}`) — traced directly in `google-sheets.service.ts`'s return statements rather than assumed from the old hook's usage, and typed accordingly (`GoogleSheetsConfigResult`).

3. **Google Calendar** (`google-calendar`) — confirmed genuinely separate from Google Sheets, not a shared connection: traced from `GoogleCalendarIntegration.tsx`/`useGoogleCalendar.ts` against `google-calendar/google-calendar-oauth.controller.ts` (`/google-calendar/*`). **Per-operator**, not workspace-shared — every operator connects their own Google account (workspace+user both derived from the JWT, no `workspace_id` param anywhere), confirmed by the old file's own doc comment and the controller's `CurrentUser`-only signature. Deliberately small UI, matching the old frontend exactly: connect/disconnect only, no event/meeting management exposed here (that would consume this connection from a meetings feature, not built anywhere in this app yet). Files: `features/google-calendar/types.ts`, `services/api/googleCalendar.ts`, `features/google-calendar/hooks/useGoogleCalendar.ts`, `features/google-calendar/components/GoogleCalendarSettingsPanel.tsx`, `app/google-calendar-callback/page.tsx`.

**OAuth pattern decision (both Google integrations)**: the brief explicitly asked to reuse Instagram's popup+callback-page pattern rather than the old frontend's full-page-redirect flow. Instagram's popup mechanism relies on the backend encoding a `popup` flag into its OAuth `state` (round-tripped through Meta and decoded on the callback page) — traced both Google backends' `encodeState`/`decodeState` and confirmed neither `GoogleOAuthUrlDto` (Sheets) nor the Calendar controller's `oauth-url` route has an equivalent field to carry that flag through Google's redirect. Rather than fabricating a backend contract that doesn't exist, popup-vs-full-navigation is instead detected **at runtime via `window.opener`** on both callback pages (`/google-sheets-callback`, `/google-calendar-callback`) — a purely client-side signal requiring no backend cooperation, functionally equivalent to Instagram's flag for this app's purposes. Extracted the shared centered-popup-opener math (previously only in `features/instagram/types.ts`) into `src/lib/oauthPopup.ts` so it's not duplicated a third time.

**Wired in**: `constants/settings-sitemap.ts` — `crm-integrations`, `google-sheets`, `google-calendar` flipped to `status: "ready"`; `app/(protected)/settings/page.tsx`'s `READY_SECTION_COMPONENTS` extended; `constants/routes.ts` gained `googleSheetsCallback`/`googleCalendarCallback` so `proxy.ts`'s auto-derived protected-prefix list covers both new standalone pages. Eighteen sections are now real; ~5 remain `SettingsPlaceholderPanel`.

**Nothing deferred** — all three sections' full traced contracts were built (Google Sheets' 15-route contract in full: status, OAuth connect/callback/disconnect, spreadsheet/tab listing, row preview, primary-sheet config, import config incl. field mapping/ignored columns, import-to-board, full import-sources CRUD, export config, export-sheet creation, board export).

**Verification**: `tsc --noEmit`, `eslint .` (full repo), `next build` all clean (zero errors/warnings) — real fixes made along the way: three `react-hooks/set-state-in-effect` violations (`AmocrmBoardsModal`'s/`AmocrmMappingModal`'s prop-driven default-selection reset, `AddImportSourceModal`'s board-columns fetch) all fixed by switching to the same render-time "adjust state on prop change" pattern already established elsewhere in this codebase (`useConversationAudio`) — the columns case was additionally simplified by replacing a manual fetch-in-effect with the existing `useLeadsBoardColumnsQuery`/`useLeadsBoardsQuery` TanStack Query hooks, removing the effect entirely rather than just relocating the reset; and four `@gravity-ui/icons` import errors (`FileSpreadsheet`, `SquareArrowUpRight`, `RadioTower`, `Table` don't exist in this icon set, unlike `lucide-react` which the old frontend uses) caught by `tsc`, mapped to real equivalents (`LayoutCells`, `ArrowUpRightFromSquare`, `BroadcastSignal`, `LayoutCells`). `next build`'s route table shows both new callback routes (`/google-sheets-callback`, `/google-calendar-callback`) prerendering as static content alongside `/instagram-callback`. `new-operatora-dev` (PM2, port 3033) restarted; `/settings?section=crm-integrations`, `?section=google-sheets`, `?section=google-calendar`, `/google-sheets-callback`, and `/google-calendar-callback` all correctly 307-redirect unauthenticated requests to `/login?next=...`, following through to a clean 200 login page with no server crash or Next.js error overlay in the HTML — same standing check as every prior phase. **Not verified** (same standing limitation as every phase since Phase 2a): interactive click-through of an authenticated session — still no test account/workspace on `operatora_test`, and the Google/amoCRM OAuth flows additionally require a registered Google Cloud OAuth client with `redirect_uri`s pointing at this app's deployed `/google-sheets-callback`/`/google-calendar-callback` URLs (infra/deployment detail outside this frontend's control, same category of limitation as Instagram's Meta App requirement). Recommend exercising, once available: the Bitrix24/Tilda webhook round-trip against a real inbound POST, the full amoCRM connect → preview → mapping → import → credentials-reveal flow against a real amoCRM account, and both Google OAuth popup flows end-to-end (including the "existing rows" import-all/skip-existing gate on a spreadsheet that already has data).

#### Claude/ChatGPT, Super Agent, Higgsfield — final 3 real settings sections (done, this pass)

Follow-up per explicit user request, same rigor as every prior pass: traced the old frontend and real backend controllers for all three before writing anything. All three turned out to be real, substantial, previously-unbuilt backend contracts — nothing was a stub or dead code at the backend level (unlike Utel's earlier finding).

1. **Claude/ChatGPT** (`claude-chatgpt`) — **not** the brief's own guessed framing (BYO API keys for AI-provider selection). The settings-sitemap entry actually routes to the old frontend's `McpKeysManager.tsx` (confirmed via `Settings.tsx`'s own `case "claude-chatgpt": return <McpKeysManager />`), not `AiModelSettings.tsx` — that second component (a model/temperature/max-tokens form) exists in the old codebase but is genuinely dead code, never imported/routed anywhere, every control disabled or a no-op. This section is MCP API-key management so **external** AI clients (Claude Code, Claude Desktop, ChatGPT via a custom MCP connector, Codex) can connect **into** Operatora's data over the Model Context Protocol — the reverse direction from AI Chat's `useModelsQuery`, which is Operatora's own in-app model picker; confirmed the two are unrelated. Traced against `backend/src/mcp/api-keys.controller.ts` (`/mcp-keys/*`, JWT-guarded only, no extra owner gate — any authenticated member may mint/list/revoke, and a minted key inherits the creator's own roles server-side so it can never exceed them): `GET /mcp-keys` (bare array), `POST /mcp-keys` (`{name?, scopes?}` → raw secret shown once, never retrievable again), `DELETE /mcp-keys/:id`. Files: `features/mcp-keys/types.ts`, `services/api/mcpKeys.ts`, `features/mcp-keys/hooks/useMcpKeys.ts`, `features/mcp-keys/components/McpKeysPanel.tsx` (connect instructions for all four clients, key-generation form with a scope select, one-time key-reveal panel with a ready-to-paste MCP JSON config, existing-keys table with revoke). The MCP endpoint URL is built from `env.apiBaseUrl` (the backend origin), not `window.location.origin` like the old frontend did — this app's frontend and API are deliberately different origins, so copying the old frontend's `window.location.origin` convention verbatim would have produced a broken URL. Nothing deferred — full contract built.

2. **Super Agent (Hermes)** (`super-agent`) — confirmed real and substantial: a full-browser autonomous agent that signs into external services (Bitrix24, amoCRM, Instagram/Facebook Ads, Google Ads, ...) using saved credentials and runs one-off tasks against them, reporting progress as an event log. Traced from `SuperAgentPanel.tsx` against `backend/src/super-agent/super-agent.controller.ts` (`/super-agent/*`) — access policy is Corporate-plan-AND-Founder(owner)-only, re-enforced server-side on every mutating call via `assertEligible`; only `GET /super-agent/settings` works for anyone else, so the panel can explain *why* it's locked instead of erroring. `workspace_id` is sent explicitly on every call (the controller's own doc comment: the UI-selected workspace may differ from the JWT default) — the same confirmed exception already established for AI Chat/Conversations. Built: enable/disable toggle with runner online/offline status, saved-credentials CRUD (service/label/login URL/username/password/optional TOTP secret — write-only, never re-displayed after saving), and a task list with expandable per-task event logs (polls every 5s only while a row is open AND the task is still active) plus cancel. Added a third `services/realtime/subscriptions.ts` entry, `subscribeToSuperAgentTasks`, mirroring the old frontend's `subscribeRealtime` on `super_agent_tasks` — debounced task-list/task-detail query invalidation over the existing workspace socket channel, not a second realtime channel. **Relationship to AI Chat's deferred cards, confirmed not assumed**: `features/chat/components/ChatCards.tsx`'s `super_agent_task`/`super_agent_question` cards are still read-only (status/text only, no answer submission, per PROGRESS.md's Phase 2b notes) — `POST /super-agent/tasks/:id/answer` (built into this section's service layer, `superAgentApi.answerTask`, exposed via `useAnswerSuperAgentTaskMutation` but not yet consumed anywhere) is exactly the endpoint those chat cards would need to stop being read-only. Wiring it into the chat feature's own card renderer is a real, traceable, non-trivial change to `features/chat/` internals — flagged as a finding per the brief's own scope guidance, not done in this settings-only pass (the old frontend's own `SuperAgentPanel.tsx` doesn't have an inline answer UI either — only its chat-side `SuperAgentQuestionCard.tsx` does — so this settings panel matches old behavior exactly: no create-task form, no answer UI, just enable/credentials/tasks-list/cancel).

3. **Higgsfield MCP** (`higgsfield`) — confirmed **not** a genuinely-separate external-signup-only integration as the brief guessed; it's a real workspace-level OAuth connection (DCR + PKCE against Higgsfield's own MCP OAuth server). Traced from `HiggsfieldPanel.tsx` against `backend/src/higgsfield/higgsfield.controller.ts` (`/higgsfield/*`): `GET /higgsfield/status` (merges connection state + the billing-plan-derived `higgsfieldMcpAccess`/`access`/`canUseHiggsfield` fields in one response — no separate billing call needed), `POST /higgsfield/connect` (owner/admin-gated server-side via `isWorkspaceOwnerOrAdmin`, returns an `authorizeUrl` — no `redirectUri` sent by the client, unlike Google Calendar/Sheets, since Higgsfield's callback is fixed server-side via `PUBLIC_BACKEND_URL`), `POST /higgsfield/disconnect`, `GET /higgsfield/balance`. Built the popup+poll-on-close pattern already established for Google Calendar (`lib/oauthPopup.ts`'s `openOAuthPopup`) rather than the old frontend's more complex postMessage-plus-2s-poll-with-5-minute-timeout logic — traced why the old frontend didn't actually rely on postMessage either (its own code comment: the callback page is served from the **backend's** origin, which differs from the frontend's origin in this app even more definitively than in the old monolith, so a cross-origin `postMessage` with `targetOrigin = window.location.origin` from the callback page's own perspective would never reach an opener on a different origin) — the backend's callback HTML auto-closes itself after ~1.8s on success anyway, so the existing simpler "poll until popup closes, then refetch status" pattern is sufficient and is what's built. Files: `features/higgsfield/types.ts`, `services/api/higgsfield.ts`, `features/higgsfield/hooks/useHiggsfield.ts`, `features/higgsfield/components/HiggsfieldSettingsPanel.tsx`. **Relationship to AI Chat's `higgsfield_connect` card — a real finding, not a restatement of the existing PROGRESS.md note**: that earlier note ("renders a `registerUrl` external link, no job-status polling") undersold what the old frontend's actual chat card (`HiggsfieldConnectCard.tsx`) does — it runs this exact same popup OAuth flow inline in the chat surface already, with `registerUrl` only as a secondary "don't have an account yet" link next to the real Connect button. So this settings section is the natural first-class home for connect/disconnect, and the chat card is properly understood as a shortcut into the identical flow, not a separate simpler thing. Making the new app's chat card do the same (instead of only linking out) is a real, traceable, low-to-medium-risk win, but touches `features/chat/` internals (the card's own render path needs the connect-mutation + status-query wiring) — per the brief's explicit instruction not to go refactor chat internals as a side quest in a settings-only pass, this is flagged here as a finding for a future pass rather than done now. Job-status polling/ack (`GET /higgsfield/jobs`, `POST /higgsfield/jobs/ack`) stays entirely out of scope here, as it belongs to AI Chat's generation flow (already deferred there).

**Wired in**: `constants/settings-sitemap.ts` — `claude-chatgpt`, `super-agent`, `higgsfield` flipped to `status: "ready"`; `app/(protected)/settings/page.tsx`'s `READY_SECTION_COMPONENTS` extended. Twenty-one sections are now real; only `lead-automations`, `tasks`, and `billing` remain `SettingsPlaceholderPanel`.

**Verification**: `tsc --noEmit`, `eslint .` (full repo), `next build` all clean (zero errors/warnings) — several real fixes made along the way, all caught by `tsc`/`eslint` before shipping: five `@gravity-ui/icons` import errors (`Clapperboard`, `RefreshCw`, `KeyRound`, `XCircle` don't exist in this icon set, unlike `lucide-react` which the old frontend uses — same recurring gap as every prior integrations pass) mapped to real equivalents (`Filmstrip`, `ArrowRotateRight`, `FolderKeyhole`, `Xmark`); two HeroUI `Chip` `color` type errors (`"primary"` isn't a valid `Chip` color in this version, only `warning/default/danger/accent/success` — mapped to `"accent"`); one `Input` prop error (`isReadOnly` isn't this component's prop name, the plain HTML `readOnly` is); confirmed via `node_modules` reads that `Button` has no polymorphic `as="a"` prop (it wraps a real react-aria `<button>`, not a link) — both external/plan-upsell links use the same plain styled `<a>` pattern already established in `GoogleSheetsSettingsPanel.tsx`, not a fabricated Button API; and a `react/no-unescaped-entities` fix (`it's` → `it&apos;s`). `next build`'s route table unchanged (no new standalone pages needed — Higgsfield's OAuth callback is entirely backend-hosted, unlike Google/Instagram). `new-operatora-dev` (PM2, port 3033) restarted with logs flushed to confirm no stale pre-fix errors lingered; `/settings?section=claude-chatgpt`, `?section=super-agent`, `?section=higgsfield` all correctly 307-redirect unauthenticated requests to `/login?next=%2Fsettings`, following through to a clean 200 login page with no server crash or Next.js error overlay in the HTML — same standing check as every prior phase. **Not verified** (same standing limitation as every phase since Phase 2a): interactive click-through of an authenticated session — still no test account/workspace on `operatora_test`. Recommend exercising, once a login exists: MCP key generation/revocation and an actual `claude mcp add`/ChatGPT-connector round trip against the live key; Super Agent's Corporate-plan+Founder gate (both the locked-out explanatory state for a non-qualifying workspace and the full enable → add-credential → task-list-with-live-updates → cancel flow for a qualifying one); and the Higgsfield OAuth popup end-to-end (connect → popup auto-close → status flips to connected → balance renders → disconnect), which — like every other OAuth-based section this session — additionally requires the backend's `PUBLIC_BACKEND_URL`/Higgsfield app registration to be live in the `test.operatora.ai` environment, an infra detail outside this frontend's control.

#### Lead Automations, Task Management, Billing & Usage — final 3 settings sections; 24/24 real (done, this pass)

Final batch per explicit user request. Same rigor as every prior pass: traced the old frontend and real backend controllers/services for all three before writing anything — nothing built from the brief's own guessed framing without verification.

1. **Lead Automations** (`lead-automations`) — confirmed a genuinely separate system from Departments' AI-escalation routing (`features/departments/`, a single `routing_prompt` per department): this is a general workspace-wide condition/action rule engine over the whole lead lifecycle (trigger(s) → action(s), e.g. "lead sits untouched in a column → notify the assigned operator on Telegram"), not escalation-only. Traced from `Settings.tsx`'s `case "lead-automations": return <AutomationRulesSettings />` → `AutomationRulesSettings.tsx` → the real component, `LeadAutomationsPanel.tsx` (rendered `variant="settings" kind="automation"` — the same component's `variant="dialog"` is used per-board inside the Leads feature, not built in this app yet). The underlying `automation_rules`/`automation_runs` tables have no dedicated REST controller — old frontend calls `supabase.from()` directly, routed through the same `POST /db/:table/query` compat proxy already used for Canned Responses; confirmed live in `db-proxy/table-registry.ts`: `{ table: 'automation_rules', scope: 'workspace', writeRoles: MANAGER_ROLES }`, `{ table: 'automation_runs', scope: 'workspace', readRoles: MANAGER_ROLES, writeRoles: ADMIN_ROLES }` (run history read-only from this UI), `{ table: 'lead_tags', scope: 'workspace', writeRoles: ALL_APP_ROLES }` (read-only here, for the `add_tag` action picker). Built the full contract: all 6 trigger types (`lead_added`/`lead_column_change`/`lead_field_changed`/`lead_signal_threshold`/`schedule`/`deadline_due`) and all 12 action types (`move_to_column`/`assign_operator`/`edit_field`/`sms_notify`/`notify_user`/`send_webhook`/`telegram_notify`/`add_tag`/`add_comment`/`set_deadline`/`require_field`/`archive_lead`), multi-trigger AND/ANY matching, scope filters (columns/operator/search), per-rule cooldown, active toggle, pause-with-error display + unpause, delete, and a run-history modal. Files: `features/lead-automations/types.ts` (build/parse/describe/validate ported 1:1 from the old frontend's own functions — same JSON shape written to the table, so nothing drifts), `services/api/leadAutomations.ts`, `features/lead-automations/hooks/useLeadAutomations.ts` (also reuses Team Members' real `GET /admin-users/operators` for the operator picker rather than a second db-proxy `operators` table read), `features/lead-automations/components/{LeadAutomationsPanel,RuleFormModal,RuleHistoryModal}.tsx`. Column options come from a new shared cross-board hook (see below). Nothing deferred from the traced contract — full CRUD + engine-config surface built, matching every trigger/action type the backend's `automation.service.ts` actually executes.

2. **Task Management** (`tasks`) — confirmed via the old frontend's `Settings.tsx`: `case "tasks": return <TaskModuleSettingsPanel />` (in `components/settings/TaskModuleSettings.tsx`, not the standalone Tasks app page — that's the separately-scoped, not-yet-built protected route from PROGRESS.md's "Remaining protected routes"/earlier open-questions entry, `TasksController`'s `operator_tasks` CRUD). This section only configures *when* the app should prompt/require/auto-create a follow-up task as a lead moves through the pipeline — a small, self-contained rule set (trigger: `lead_column_change`/`lead_created`/`lead_assigned`/`lead_saved` → action: `prompt_task`/`require_task`/`auto_create_task`, plus task type/due preset/title template, optionally scoped to specific columns). Traced against the real `tasks.controller.ts`'s `GET/PUT /tasks/settings` (`TaskSettingsService`, workspace-manager-gated server-side via `canManageTeamTasks` — confirmed its `task-settings.types.ts` is a verbatim re-export of `task-rules.util.ts`, so the client's ported types/defaults/normalization can't drift from what the server independently re-validates). Files: `features/tasks-settings/types.ts` (ported 1:1 from `lib/taskRules.ts`), `services/api/taskSettings.ts`, `features/tasks-settings/hooks/useTaskModuleSettings.ts`, `features/tasks-settings/components/{TaskModuleSettingsPanel,TaskRuleCard}.tsx`. Not autosave — matches the old frontend's explicit Save-button + dirty-tracking pattern (diffed against a saved-snapshot JSON string), since these rules affect every operator's daily workflow. Nothing deferred — full contract built (enable toggle, add/remove/edit rules, column-scoping toggle-pills, all trigger/action/type/due-preset options).

3. **Billing & Usage** (`billing`) — confirmed the existing `services/api/billing.ts`/`GET /billing/me` (already used by Team Members' seat panel and File Storage's usage bar) is genuinely a subset of what this section needs, not a competing surface: extended it in place (`BillingFeatures.usage` gained optional `calls_per_month`/`ai_chat_messages`/`ai_dashboards`/`custom_dashboards`/`image_generations`, `BillingFeatures` itself gained `balance_uzs`/`channels`/`agentic_mode`/`periodKey` — all additive, existing consumers untouched) rather than adding a duplicate `/billing/usage` call. Traced the rest from `BillingUsagePanel.tsx` + its two sub-panels `PaymentCardsPanel.tsx`/`WorkspaceBalancePanel.tsx` (`lib/billingPaymentApi.ts`) against the real `billing.controller.ts`: invoices (`GET /billing/invoices` — paid history + the one visible pending renewal invoice, `GET /billing/invoices/:id`), paying a pending invoice by card or prepaid balance (`POST /billing/subscriptions/charge`), declining an unused premium seat directly off a pending invoice (`POST /billing/operator-seat/decline` — a real capability Team Members' own seat panel never exposed), saved payment cards (Paylov-backed, `POST /billing/cards` → OTP → `POST /billing/cards/confirm`, `GET/POST .../delete/POST .../default`, all `assertWorkspaceBillingAdmin`-gated server-side), and prepaid balance (`GET /billing/balance`, top-up either via a saved card — `POST /billing/balance/top-up` — or a Click/Paylov gateway popup — `POST /billing/balance/top-up/gateway`, Payme kept behind the same `NEXT_PUBLIC_PAYME_PAYMENTS_ENABLED` kill-switch `RentSeatModal.tsx` established — with a balance-increase reconciliation poll mirroring `useSeatPurchasePolling`'s exact pattern, capped at 3 minutes). Built as a 4-tab panel (Overview/Invoices/Payment cards/Balance) via HeroUI `Tabs`, matching the multi-tab precedent already established by Eskiz's settings panel. Files: `features/billing/types.ts`, `services/api/billing.ts` (extended), `features/billing/hooks/useBillingSection.ts`, `features/billing/components/{BillingSettingsPanel,OverviewTab,InvoicesTab,PaymentCardsTab,BalanceTab}.tsx`. **Deliberately deferred, matching the old frontend's own scope split, not fabricated**: full plan upgrade/downgrade comparison + new-subscription checkout (`POST /billing/subscriptions`, `GET /billing/renew-quote`) — the old frontend itself only links out to `/pricing` for this; this app mirrors that with the same `/checkout?cycle=yearly` link Team Members' `UpgradeToProBanner` already established, rather than building a second plan-comparison UI from scratch; Eskiz wallet top-up (`POST /billing/eskiz/top-up`, already built under the Eskiz settings section, a separate wallet from the workspace prepaid balance); operator-seat rent/charge (already built under Team Members — this section only adds the "decline a vacant seat" action Team Members never exposed).

**New shared data layer**: `src/features/leads-boards/hooks/useLeadsBoards.ts` (relocated from `features/google-sheets/hooks/`, which was its only prior consumer, now genuinely shared by Google Sheets/Lead Automations/Tasks — imports updated, no behavior change) gained `useAllLeadsBoardColumnsQuery(workspaceId)`, a `useQueries`-based flattening of every board's columns with board id/name attached, for the two new workspace-wide (not per-board) column pickers.

**Wired in**: `constants/settings-sitemap.ts` — `lead-automations`, `tasks`, `billing` flipped to `status: "ready"`; `app/(protected)/settings/page.tsx`'s `READY_SECTION_COMPONENTS` extended with all three. **All 24 settings sections are now real** — zero placeholders remain, and `SettingsPlaceholderPanel` is no longer reachable from `READY_SECTION_COMPONENTS` (kept as shared chrome only, per the original "navigation only" skeleton pass).

**Verification**: `tsc --noEmit`, `eslint .` (full repo), `next build` all clean (zero errors/warnings) — several real fixes made along the way, all caught by `tsc`/`eslint` before shipping: `TextField` in this HeroUI version has no `.Label`/`.Description` compound sub-components (unlike `Modal`/`Select`) — every field across the three sections' forms uses the standalone `Label` component instead (same pattern `McpKeysPanel.tsx` already established, confirmed by reading that file rather than guessing a `TextField.Label` anatomy that doesn't exist); three more `@gravity-ui/icons` gaps (`Wallet`, `Workflow` don't exist in this icon set — mapped to `CircleRuble`/`GearPlay`; `Button` has no `color` prop, only `variant` — a stray `color="danger"` on a delete-confirm button fixed to `variant="danger"`, matching `DeleteMemberConfirm.tsx`'s established pattern). `next build`'s route table unchanged (no new standalone pages — none of the three sections are OAuth-based). `new-operatora-dev` (PM2, port 3033) restarted; `/settings?section=lead-automations`, `?section=tasks`, `?section=billing` all correctly 307-redirect unauthenticated requests to `/login?next=%2Fsettings`, following through to a clean 200 login page with no server crash or Next.js error overlay in the HTML — same standing check as every prior phase. **Not verified** (same standing limitation as every phase since Phase 2a): interactive click-through of an authenticated session — still no test account/workspace on `operatora_test`. Recommend exercising, once a login exists: the full automation-rule builder against a workspace with real boards/columns/operators/tags (every trigger/action type, multi-trigger AND/ANY matching, pause-on-error/unpause, run history); the task-rule save/dirty-tracking round trip; and Billing's invoice-payment flow (card + balance), the payment-card OTP add flow against a real Paylov sandbox, and the balance top-up gateway popup — all four of which additionally require a workspace with an active paid plan/Paylov integration configured on `test.operatora.ai` to exercise meaningfully, on top of the standing no-test-account limitation.

## Settings page epic — complete (24/24 sections real)

Started as a navigation-only skeleton (all 24 sections `SettingsPlaceholderPanel`), built out one contract-traced batch at a time across many passes this session, per the user's explicit ask: "implement settings page end to end, not read only." As of this pass, every section listed in `constants/settings-sitemap.ts` renders real, backend-wired UI — General, Notifications, Team Members, Roles & Permissions, Brand, Security, Departments, File Storage, Canned Responses, SIP Configuration, GSM Lines, Telegram, Eskiz, Utel, Instagram, CRM Integrations, Google Sheets, Google Calendar, Claude/ChatGPT, Super Agent, Higgsfield, Lead Automations, Task Management, Billing & Usage.

Two sections are real-but-deliberately-thin, not placeholders, with the reason documented inline at the point each was built (not asserted here for the first time): **Utel** wraps the same generic SMS-gateway CRUD Team Members already exposes (the old frontend's own component is a one-line passthrough to `<TelephonySmsGateways />`; a separate Utel-branded backend module exists but writes to columns that don't exist in the real schema — dead code, not built against). **Google Calendar** is deliberately small (connect/disconnect only) because that's genuinely all the old frontend's own component does — no event/meeting management is exposed anywhere in the old system either.

Every section's own pass documented its explicitly-deferred sub-features (endpoints traced but out of scope, or contracts that don't actually exist server-side) inline above — those deferrals stand; this closing note is not re-litigating them, just confirming the top-level "is every sitemap entry real" question is now unambiguously yes.

**Standing limitation across the entire epic, every pass**: no test account/workspace exists on `operatora_test`, so verification throughout has been build/type/lint-clean + careful contract tracing (backend controllers/services read directly, not guessed) + unauthenticated-redirect smoke tests, never authenticated click-through. This is flagged in every pass above individually; recommend creating a seeded test workspace (with a paid plan, at least one lead board/column, a connected Telegram bot, etc.) before the next session that needs to validate actual data flows rather than just contract shape.

### Phase 2b — Leads Kanban board (`/leads`) — core MVP (done 2026-08-14)

First pass at the highest-traffic old route, explicitly scoped down by the user after the old app's `/leads` feature turned out to be ~26,000 lines / 55 components (`LeadDetailsDialog.tsx` alone is 3,442 lines) — full parity is future follow-up work, not this pass. Built exactly: board + columns rendering, drag-and-drop move with optimistic update/rollback, realtime `lead_moved`/`lead_assigned`/`lead_deleted`, and a scoped-down (not stub) lead-details view.

**Trace** (backend, `/www/wwwroot/test.operatora.ai/app/backend/src/leads-controller/`), confirmed against what the old frontend's `LeadBoard.tsx`/`LeadColumn.tsx`/`LeadCard.tsx` actually call (not guessed from controller existence alone):
- `GET /boards` (already had a data layer — `services/api/leadsBoards.ts`, unchanged) — board list, ordered by `display_order`/`created_at`; used as-is for the (conditional) board switcher and to resolve the default board as `boards[0]`, same ordering the backend's own `/boards/default` uses, so a second request wasn't needed.
- `GET /lead-board/:boardId` (`lead-board.controller.ts`/`.service.ts`) — the *actual* board-data endpoint the old frontend's main board query hits (queried as `['lead-board', boardId, ...]` in its cache) — columns + per-column lead **counts**/`sums`, no lead rows inline.
- `GET /lead-board/:boardId/column/:columnId?page=&pageSize=` — the real per-column card-list endpoint (`['column-leads', boardId, columnId, page]` in the old cache), `{leads,totalCount,page,perPage}` — the exact shape `normalizePaginated()` (already built) understands out of the box.
- `POST /right-board-controller/change-column` (`right-board-controller.controller.ts`/`.service.ts`) — move; body `{selectedLeads:[leadId], columnId}`. Enforces same-workspace + same-board consistency server-side (rejects cross-board moves with a clear error rather than silently disappearing the lead) and gates on automation deadline/required-field rules and WIP column limits — a `400` from those surfaces as a rollback + inline error message in the details modal, not a crash; the old frontend's richer `RequireFieldDialog`/WIP-limit UX is explicitly deferred (see below).
- `POST /right-board-controller/assign-operator` — reassign/unassign (`operatorId: null`); accepts either an `operators.id` or a `profiles.id` (backend resolves both), so this app passes Team Members' `user_id` for the operator picker rather than standing up a second operators data source — same reuse precedent Lead Automations' operator picker already established.
- `GET /leads/:id` (`leads.controller.ts`) — full lead row + column, used for a fresh read when the details modal opens.
- Realtime emission confirmed directly in `right-board-controller.service.ts`'s `notifyLeadsChanged()`: `{event: 'lead_moved'|'lead_assigned'|'lead_deleted', table: 'leads', new: {leadIds, ...extra}}` broadcast to every workspace touched by the affected leads, over the same `workspace:{workspaceId}` socket topic every other realtime feature in this app already uses.

**Built**:
- `src/services/api/leads.ts` (new) — `leadsApi.{getBoardData, getColumnLeads, getLead, moveLead, assignOperator}`, the real (not narrow) data layer for this feature, superseding `leadsBoards.ts` for anything beyond board/column *picker* reads (that file's own doc comment flagged this as the intended relationship — left unchanged, still used for the board switcher).
- `src/features/leads/types.ts` — `LeadBoardColumn`, `LeadBoardData`, `LeadRow` (typed to exactly the fields this MVP renders — `custom_fields` stays a loose record, full custom-field-type-aware rendering is workspace-schema-defined and out of scope), `formatLeadName()` (ported from the old frontend's `utils/leadName.ts` logic: first+last name, falling back to phone, falling back to a generic label).
- `src/features/leads/hooks/{useLeadBoardQuery,useColumnLeadsQuery,useLeadDetailsQuery,useLeadMutations}.ts` — TanStack Query hooks. `useColumnLeadsQuery` runs the raw `{leads,totalCount,page,perPage}` response through `normalizePaginated()`; numbered Prev/Next pagination (matching `ConversationsTable`'s established UX), not infinite scroll. `useMoveLeadMutation` does a real optimistic patch across every mounted `column-leads` page for the board plus the `lead-board` counts map, with full rollback on error (mirrors the old frontend's `moveLeadMutation` in `LeadBoard.tsx`, adapted to this app's `Paginated<T>` envelope instead of its ad-hoc `{leads}`/`{columns}` shapes) — `onSettled` reconciles via invalidation rather than trusting the optimistic patch alone. `useAssignOperatorMutation` invalidates rather than optimistically patches (the assignee's display name isn't resolvable client-side from just an id).
- `src/features/leads/components/{KanbanBoard,KanbanColumn,LeadCard,LeadDetailsModal}.tsx` — `KanbanBoard` is the `@dnd-kit/core` `DndContext` + horizontal column layout + drag overlay; `KanbanColumn` is a `useDroppable` target with its own paginated fetch; `LeadCard` is a `useDraggable` card (name, phone, assigned-operator, deadline — full parity fields like intent-score badges, connected-channel icons, inline sold/reject actions, and tag chips are deferred, see below); `LeadDetailsModal` is the real (not stub) details view — HeroUI `Modal`, phone/email/created/deadline, a `Select` to move column, a `Select` to reassign operator (backed by Team Members' `GET /admin-users/operators`), and a generic key/value render of `custom_fields`.
- `src/app/(protected)/leads/page.tsx` (new) — wires the above inside the existing `AppShell`; board switcher only renders when the workspace actually has more than one board (decided from `GET /boards`'s own list length rather than over-building a switcher UI most workspaces — one board seeded on signup — never need); subscribes to realtime on mount, unsubscribes on unmount/board change.
- `src/services/realtime/subscriptions.ts` — added `subscribeToLeadBoardUpdates(queryClient, workspaceId, boardId)`, the fourth entry in this module, following the exact same pattern as `subscribeToChatThreadUpdates`/`subscribeToSuperAgentTasks`: subscribes to `workspace:{workspaceId}`, debounce-invalidates (~1200ms) the board's `lead-board` and every `column-leads` page for `boardId` when a `lead_moved`/`lead_assigned`/`lead_deleted` event for the `leads` table arrives. This is a "something changed, refetch" signal for *other* operators' concurrent changes — the local actor's own moves are already reflected via the mutation hook's optimistic patch, so this doesn't double-apply anything.

**DnD library decision**: `@dnd-kit/core` + `@dnd-kit/utilities` only (newly added dependencies — this app had no DnD library installed) — **not** `@dnd-kit/sortable`, despite the brief's stated preference for core+sortable. Confirmed via grep that the old frontend's `LeadBoard.tsx`/`LeadColumn.tsx`/`LeadCard.tsx` only ever import `DndContext`/`useDraggable`/`useDroppable`/`DragOverlay` from `@dnd-kit/core` — `sortable` isn't imported anywhere in the leads feature there either, because cards don't reorder *within* a column, they only move *between* columns (matches this MVP's scope exactly — intra-column reordering isn't a requirement). Adding `sortable` would be an unused dependency for a capability neither the old app nor this pass needs.

**Explicitly deferred (per the brief, not an oversight)** — noted here so a future pass doesn't have to re-discover the scope boundary:
- Create-lead dialog, mark-sold/mark-rejected dialogs, compose SMS, bulk operations/multi-select, filters (search/marital-status/academic-status/age/date/custom-field/channel/operator — all real backend query params on `lead-board`, none wired client-side this pass), sold/rejected/archived/trash board or list views, comment ticker, presence heartbeat overlay, AI assist (`AISalesChatbot`/`RealtimeGuidanceChat`), lead automations *dialog* variant (the *settings*-section variant already exists, `features/lead-automations/`, not duplicated here), import/export, manage-fields/tabs dialogs, board creation/sharing/reordering, column create/edit/delete/reorder/WIP-limit management UI.
- Lead details view: comments/timeline (`CommentTicker`/`LeadTimeline`), SMS compose, tags (`LeadTagChips`/`LeadTagPicker`), relations (`RelationPicker`)/rollups, additional phone numbers, task creation/linking, meeting scheduling, channel-linking, conversation linking — the old `LeadDetailsDialog.tsx`'s remaining ~3,300 lines beyond the core-fields view built here.
- Column-move automation gates surface only as a generic inline error message (rollback + `actionErrorMessage`-style text) rather than the old frontend's dedicated `RequireFieldDialog`/deadline-prompt UX — the backend contract (`400` with a `DEADLINE_REQUIRED`/`FIELD_REQUIRED` message) is real and was traced, just not given its own guided-fix dialog this pass.
- Cross-board lead moves (`moveLeadsToBoard`) — only same-board column moves are wired; the details modal's column picker only ever lists the current board's own columns.

**Verification**: `tsc --noEmit`, `eslint .` (full repo), `next build` all clean (zero errors/warnings) — one real fix made along the way: an `LeadCard.tsx` `react-hooks/purity` violation (`Date.now()` called during render for a "deadline overdue" red-text calculation) caught by `eslint`, resolved by dropping the impure now-vs-deadline comparison rather than working around it with an effect (the deadline date itself still renders; only the overdue-highlighting color logic was cut — minor, not a scope item). `next build`'s route table shows `/leads` now prerendering alongside the other protected routes. `new-operatora-dev` (PM2, port 3033) restarted; `GET /leads` correctly 307-redirects unauthenticated requests to `/login?next=%2Fleads`, which itself returns a clean 200 with no server crash or Next.js error overlay — same standing verification bar as every prior phase (still no test account/workspace on `operatora_test` for authenticated click-through — same limitation flagged throughout this file).

### Leads — filters (done 2026-08-14)
`LeadFilters` type + `LeadFiltersBar` UI wired to the real 10 filter params `lead-board.service.ts` accepts (search, marital status, academic status, age from/to, date from/to, created by, channel, assigned operator). Filters included in `useLeadBoardQuery`/`useColumnLeadsQuery` cache keys; mutation invalidation made predicate-based (filter-agnostic). `customFields`/`formSource` deliberately excluded — no client-side schema source for either yet. TS/lint/build clean; **not yet click-through verified against a live authenticated session** (standing blocker across this whole file).

### Leads — tabs & list view (done 2026-08-15)

Phase 2c-1: Active/Sold/Rejected/Archived/Trash tabs on `/leads`, plus a Kanban/List view toggle on the Active tab. Old-frontend reference read for UX only (`pages/Leads.tsx`, `components/leads/{SoldLeadsBoard,SoldLeadsList,RejectedLeadsBoard,RejectedLeadsList,ArchivedLeadsList,TrashLeadsList,TrashDialog}.tsx`), never modified — every endpoint traced directly against the real backend (`/www/wwwroot/test.operatora.ai/app/backend/src/leads-controller/`), not assumed from the old frontend's calls.

**Trace, controller by controller (all real, none stubbed):**
- **Active tab, List view** — `GET /leads-list` (`leads-list/leads-list.controller.ts`/`.service.ts`), the whole-board, cross-column, paginated list (distinct from the Kanban view's per-column `GET /lead-board/:boardId/column/:columnId`). **1-indexed** `page` (confirmed directly in the controller's `Math.max(1, Math.trunc(page))` — every other paginated endpoint in this feature is 0-indexed, this one genuinely isn't), response `{data, count, conversationCounts}` — `normalizePaginated()` already understands `{data,count}` natively, no adapter changes needed. `selectedColumns` (the old frontend's `BoardColumnFilter`) accepted by the endpoint but not wired this pass.
- **Sold tab** — `sold-leads-list.controller.ts`: `GET /sold-leads-list?boardId=` (bare array, no pagination/filters param at all — confirmed by reading the controller signature, matches the old frontend's own client-side-filtering workaround, not reproduced here), `PATCH /sold-leads-list/:id/restore`. Response rows carry `sold_at`/`sold_note`/`sold_by_profile` (confirmed in `sold-leads-list.service.ts`'s in-memory profile-join).
- **Rejected tab** — `rejected-leads-list.controller.ts`: `GET /rejected-leads-list?boardId=&page=&filters=` (**0-indexed** page, fixed 100/page server-side, no `pageSize` override param), `{leads, count}` (`normalizePaginated()` handles this shape too), `PATCH /rejected-leads-list/:id/restore`. `filters=` JSON uses the exact same field names as `LeadFilters` (confirmed directly in `leads-list-filters.util.ts`'s shared `LeadListFilters` type/`applyLeadListFilters()`, used by `leads-list`/`rejected-leads-list`/`archived-leads-list` alike) — reused the existing `LeadFiltersBar` as-is rather than building a second filter UI.
- **Archived tab** — `archived-leads-list.controller.ts`: same shape as Rejected (`GET /archived-leads-list?boardId=&page=&filters=`, `PATCH /archived-leads-list/:id/restore`). The controller also has `restore-multiple`/`archive-multiple`/`archive-all` — real endpoints, explicitly deferred to Phase 2c-3 (bulk actions), only single-lead restore wired here.
- **Trash tab** — **not** a separate controller/module (confirmed by grepping the whole backend for "trash" — it only appears in `leads-controller/leads/leads.controller.ts`/`.service.ts`): `GET /leads/trash?workspace_id=`, `POST /leads/:id/restore?workspace_id=`, `DELETE /leads/:id/permanent?workspace_id=`. **Workspace-scoped, not board-scoped** — a soft-deleted lead can be from any board, so this tab ignores the board switcher entirely (traced directly: the controller takes no `boardId` anywhere in this trio). Also added `leadsApi.deleteLead` (`DELETE /leads/:id`, soft-delete into Trash, admin/sales_manager-gated server-side) for a delete action on the Sold tab, matching the old frontend's `SoldLeadsList.tsx` "permanently delete" button (misleadingly named there — it's the same soft-delete-to-Trash as everywhere else, confirmed in `leads.service.ts`'s `remove()`).
- **UX note, a deliberate departure from the old frontend, not an oversight**: in the old app, Trash is a dialog opened from the sidebar nav (`AppSidebar.tsx` → `TrashDialog.tsx`), not a `/leads` page tab — confirmed by reading `TrashDialog.tsx`'s one caller. This slice's brief explicitly asked for Trash as a fifth page tab instead, which is what's built; the sidebar-dialog entry point from the old app was not reproduced (no such dialog/trigger exists in this app's sidebar).

**Built:**
- `src/features/leads/types.ts` — `LEAD_TABS`/`LeadTab` (fixed order: Active/Sold/Rejected/Archived/Trash — admin-configurable order/visibility, the old frontend's `leads-tab-config`, deliberately deferred, see Phase 2c-1's own scope line), `LeadViewMode` (`"board" | "list"`), and `LeadRow` extended with the sold/rejected/archived/trash metadata fields each respective endpoint's response actually carries (`sold_at`/`sold_note`/`sold_by_profile`, `rejected_at`/`rejected_reason`/`rejected_by_profile`, `archived_at`/`archived_note`/`archived_by_profile`, `deleted_at`/`deleted_by_profile`) — a given row only ever populates the subset relevant to the tab it came from.
- `src/services/api/leads.ts` extended — `leadsApi.getLeadsList`/`.deleteLead`, plus four new exports (`soldLeadsApi`, `rejectedLeadsApi`, `archivedLeadsApi`, `trashApi`), each with the traced contract in its own doc comment. `filtersToLeadListFilters()` — shared helper dropping this app's client-only sentinels (`null`/`"all"`/`""`) before sending `LeadFilters` as the `filters=` JSON param.
- `src/features/leads/hooks/` — `useLeadsListQuery` (Active tab List view), `useSoldLeads`/`useRejectedLeads`/`useArchivedLeads`/`useTrash` (query + restore mutation each, plus `useDeleteLeadMutation` for Sold's delete action), `invalidateAllLeadViews.ts` (shared predicate-based invalidation across every board-scoped list key plus Trash — mirrors the old frontend's own `invalidateLeadViews`/`invalidateLeadViewsAfterSoldChange` helpers: restoring/deleting a lead on any one tab can move it into or out of any other, so every mutation invalidates the full set rather than just its own tab).
- `src/features/leads/components/` — `LeadsTabs.tsx` (HeroUI `Tabs`/`Tabs.List` as a pure tab-strip selector — deliberately **not** wrapping content in `Tabs.Panel`, so only the active tab's query hooks actually mount/fire, keeping the project's network-flood-prevention rule intact instead of eagerly fetching all five tabs on page load), `LeadViewToggle.tsx` (Kanban/List icon toggle, Active tab only), `LeadsListTable.tsx` (Active tab's List view — the same TanStack Table v9 headless pattern `ConversationsTable` established: `useTable({features, columns, data})`, no `useReactTable`, plain semantic `<table>`, not HeroUI's react-aria-components `Table`), `SoldLeadsTable.tsx`/`RejectedLeadsTable.tsx`/`ArchivedLeadsTable.tsx`/`TrashLeadsTable.tsx` (plain `<table>` markup matching the same visual/DOM shape without a TanStack Table instance each, since none of these four has a client-side sort/filter surface to justify one — Rejected/Archived still get real server pagination via Prev/Next).
- `src/app/(protected)/leads/page.tsx` rewired — tab state + Active-tab-only view-mode state; the filter toggle/bar only render for Active/Rejected/Archived (Sold/Trash accept no `filters` param at all, traced directly, so no filter UI is offered where it would silently do nothing); "Manage columns"/"Add lead" (both still disabled placeholders) and the new view toggle only render on the Active tab. Every tab's own page number resets to its first page on board/filter change via the render-time "adjust state on prop change" pattern already established by `useConversationAudio` (not a `useEffect`). `LeadDetailsModal` (existing, unchanged) is reused as the row-click details view from every list tab except Trash (a soft-deleted lead's board/column context may not be navigable — matches the old frontend's `TrashLeadsList.tsx`, which doesn't open a details dialog on row click either).

**Explicitly deferred within this slice** (per the brief's own slice boundary, not oversights):
- Kanban/board view for Sold/Rejected/Archived (the old frontend's `SoldLeadsBoard.tsx`/`RejectedLeadsBoard.tsx` — a second, board-shaped rendering of the same data) — List view only for these three tabs; the brief only required List view "at minimum" for Active and "extend to other tabs if the same data/endpoint supports it cheaply," and these tabs have no natural Kanban shape without inventing marker-column UX.
- Bulk restore/archive (`restore-multiple`/`archive-multiple`/`archive-all` on the Archived tab) — real endpoints, traced, explicitly Phase 2c-3's scope.
- Admin-configurable tab order/visibility (`leads-tab-config`) — fixed order per the brief.
- `BoardColumnFilter` (per-tab column-scoping picker) on any of the five tabs — none of this pass's list views wire `selectedColumns`/`columnIds` even though several of the traced endpoints accept it.
- Sold-tab client-side filtering/search (the old frontend's own workaround for the endpoint's total lack of server-side filters) — not reproduced; Sold has no filter UI in this app.
- Trash-tab row click → lead details (see the UX note above).

**Verification**: `tsc --noEmit`, `eslint .` (full repo), `next build` all clean (zero errors/warnings) — one real fix made along the way (a duplicated `sold`/`rejected` field pair in `LeadRow`, caught immediately by `tsc`'s `TS2300`, resolved by keeping the richer/documented set and dropping the earlier duplicate declarations). `next build`'s route table still shows `/leads` prerendering alongside the other protected routes. `new-operatora-dev` (PM2, port 3033) restarted; `GET /leads` still correctly 307-redirects unauthenticated requests to `/login?next=%2Fleads`, which itself returns a clean 200 login page with no server crash or Next.js error overlay in the HTML — same standing verification bar as every prior phase (still no test account/workspace on `operatora_test` for authenticated click-through of the actual tabs/toggle/restore flows — same limitation flagged throughout this file).

### Leads — Create/Mark Sold/Mark Rejected dialogs (done 2026-08-15)

Phase 2c-2. Old-frontend reference read for UX only (`components/leads/{CreateLeadDialog,MarkLeadSoldDialog,MarkLeadRejectedDialog}.tsx`), never modified — every endpoint traced directly against the real backend (`/www/wwwroot/test.operatora.ai/app/backend/src/leads-controller/{add-lead,sold-leads-list,rejected-leads-list,lead-rejection-reasons}/`).

**Trace (all real, none stubbed):**
- **Create Lead** — `GET /add-lead/columns?boardId=` (`add-lead.controller.ts`/`.service.ts`) — the dialog's own column picker, distinct from the board's `lead-board` columns: excludes the locked Sold/Rejected marker columns server-side (`special_stage_kind` filter), traced directly. `POST /add-lead` — `CreateAddLeadDto` confirmed field-for-field (`first_name`/`column_id` are the only truly required fields server-side; `custom_fields` has no validator and defaults to `{}`, `created_by` is always taken from the authenticated caller and any DTO value is ignored). A `400` with `{code: 'DUPLICATE_LEAD', existingLead}` blocks creation on a duplicate phone within the workspace — surfaced via `ApiError.code`, shown as an inline error with the backend's own message; the old frontend's richer duplicate-lead resolution panel (open/delete the conflicting lead inline) is explicitly deferred, see below.
- **Mark Sold** — `PATCH /sold-leads-list/:id/mark-sold` (`sold-leads-list.controller.ts`/`.service.ts`), body `{note?}` (optional free text, server-trims/caps at 2000 chars). Same deadline/required-field automation gate as a regular column move (`assertRequiredFieldsForSold` → `FIELD_REQUIRED:...` message on a `400`), confirmed directly in the service — surfaced as a generic inline error, not the old frontend's guided `RequireFieldDialog` follow-up (same deferred-scope call the Kanban board MVP already made for the column-move gate).
- **Mark Rejected** — `PATCH /rejected-leads-list/:id/mark-rejected` (`rejected-leads-list.controller.ts`/`.service.ts`), body `{reason}`, mandatory (`400` if empty/whitespace, confirmed in the service). Same required-field gate as Mark Sold.
- **Rejection reasons** — a genuinely separate, real controller (`lead-rejection-reasons.controller.ts`/`.service.ts`), **not** a dedicated table: persisted under `workspaces.settings.leadRejectionReasons`. `GET /lead-rejection-reasons?workspace_id=` **always returns a non-empty array** — a workspace that hasn't customized its list yet gets the backend's own built-in Uzbek defaults (`DEFAULT_REASONS`, confirmed directly in the service), never `[]`. `PUT /lead-rejection-reasons` replaces the whole list (no per-item CRUD endpoint exists), body `{workspace_id, reasons}`, server-side sanitized (trimmed, deduped, capped at 30 items/200 chars each).

**Built:**
- `src/services/api/leads.ts` extended — `leadsApi.getAddLeadColumns`/`.createLead` (+ `CreateLeadPayload` type), `soldLeadsApi.markSold`, `rejectedLeadsApi.markRejected`, new `leadRejectionReasonsApi.list`/`.set` export.
- `src/features/leads/schema.ts` (new) — `createLeadSchema`/`markSoldSchema`/`markRejectedSchema`/`addRejectionReasonSchema` (Zod), matching `CreateAddLeadDto`'s real required-field set.
- `src/features/leads/leadActionError.ts` (new) — shared inline-error formatter for every lead-mutating action, consolidated out of `LeadDetailsModal.tsx`'s original one-off `actionErrorMessage` (that file now imports the shared helper instead) so the three new dialogs don't reimplement the same `ApiError` classification logic; also branches on `error.code === 'DUPLICATE_LEAD'`.
- `src/features/leads/hooks/` — `useCreateLeadMutation`/`useMarkSoldMutation`/`useMarkRejectedMutation` (added to `useLeadMutations.ts`, each invalidates via the existing `invalidateAllLeadViews` per the brief — no optimistic patch, since the server resolves fields — duplicate checks, `display_order`, deadline gates — that can't be predicted client-side), `useAddLeadColumnsQuery.ts` (new), `useLeadRejectionReasons.ts` (new: `useLeadRejectionReasonsQuery` + `useSetLeadRejectionReasonsMutation`).
- `src/features/leads/components/` — `CreateLeadDialog.tsx` (HeroUI `Modal`, RHF+Zod — first name/last name as two plain fields rather than the old frontend's single "full name" input that splits on the last space, since that's a UI workaround for the old app's layout, not a backend requirement, and the two real DB columns map directly; phone/age/marital/academic status/pipeline-stage `Select`), `MarkSoldDialog.tsx` (confirm + optional note `TextArea`), `MarkRejectedDialog.tsx` (mandatory reason `Select`, sourced from `useLeadRejectionReasonsQuery`, with a collapsible "Manage reasons" section), `RejectionReasonsManager.tsx` (new — minimal add/remove list, each action sends the full next array to the replace-all `PUT` endpoint since no per-item CRUD exists; embedded inside `MarkRejectedDialog` rather than a standalone Settings section, since that's the only place this app gates on the list existing).
- `src/features/leads/components/LeadDetailsModal.tsx` — added "Mark sold"/"Mark rejected" buttons to the footer, gated by a data-driven `canMarkOutcome = !lead.sold && !lead.rejected && !lead.deleted_at` check (not tab-based, since this modal is reused from every list tab except Trash) rather than an already-sold/rejected lead offering to be re-marked. Marking either closes both the mark dialog and the details modal (the lead has left the view it was opened from).
- `src/app/(protected)/leads/page.tsx` — the previously-disabled "Add lead" toolbar button (Active tab only) now opens `CreateLeadDialog`.

**Trigger points wired (per the brief's own fallback structure)**: `LeadCard` has no per-card menu in this codebase yet (confirmed by reading the current file before assuming one existed) — the brief explicitly allowed `LeadDetailsModal` as the fallback trigger surface when that's the case, which is what's built; a per-card quick-action menu is a nice-to-have left for a future pass, not required by this slice. The Kanban board also has no virtual/marker Sold/Rejected drop-columns to hook a drag-and-drop trigger into (confirmed directly in `KanbanBoard.tsx`/`KanbanColumn.tsx` — only real board columns render, no marker columns) — the brief flagged this as possibly-not-wired-yet, and it isn't; building that drop-zone UX would be a `KanbanBoard` restructuring more appropriately scoped with Phase 2c-5 (column/board management), not bundled into this dialogs-focused slice.

**Explicitly deferred within this slice (not oversights):**
- Custom-field rendering on Create Lead (required-per-stage custom fields, `GET /add-lead/custom-fields`) — the custom-field type/engine isn't built anywhere in this app yet (Phase 2c-6); the DTO's `custom_fields` isn't server-validated, so omitting it entirely is safe, not a broken create flow.
- The old frontend's duplicate-lead resolution panel (inline mini-card with Open/Delete actions for the conflicting lead) — this pass surfaces the real `DUPLICATE_LEAD` error message inline but doesn't fetch/render the conflicting lead or offer to delete it from within the dialog.
- The guided `RequireFieldDialog`/deadline-prompt follow-up UX for `FIELD_REQUIRED:...` gate failures on Mark Sold/Mark Rejected — generic inline error only, matching the Kanban board MVP's existing deferred-scope call (tracked together under Phase 2c-5).
- Per-card quick-action menu on `LeadCard` and Kanban drag-onto-virtual-column triggers for Mark Sold/Mark Rejected — see the trigger-points note above.
- A standalone Settings-section rejection-reasons manager — the minimal manager built here lives only inside `MarkRejectedDialog`, the one place this app currently gates on the list existing.

**Verification**: `tsc --noEmit`, `eslint .` (full repo), `next build` all clean (zero errors/warnings) — no fix-ups needed this pass; the `Select`'s `placeholder` prop location (on `Select` itself, not `Select.Value`, confirmed against `EditMemberModal.tsx`'s existing usage before writing new call sites) was the one thing double-checked against precedent rather than guessed. `next build`'s route table still shows `/leads` prerendering alongside the other protected routes. `new-operatora-dev` (PM2, port 3033) restarted; `GET /leads` still correctly 307-redirects unauthenticated requests to `/login?next=%2Fleads`, which itself returns a clean 200 login page with no server crash or Next.js error overlay in the HTML — same standing verification bar as every prior phase (still no test account/workspace on `operatora_test` for authenticated click-through of the actual create/mark-sold/mark-rejected/reason-manager flows — same limitation flagged throughout this file).

### Leads — bulk actions (done 2026-08-15)

Phase 2c-3. Old-frontend reference read for UX only (`components/leads/{BulkOperationsDialog,FilteredBulkActionsDialog}.tsx` and `LeadsList.tsx`'s row-selection state), never modified — every endpoint traced directly against the real backend (`/www/wwwroot/test.operatora.ai/app/backend/src/leads-controller/{right-board-controller,archived-leads-list,leads-list,leads}/`), not assumed from the old frontend's calls.

**Trace — row-selection bulk actions (id-array endpoints), all already bulk-capable, none needed a new endpoint:**
- `POST /right-board-controller/change-column` / `assign-operator` / `delete-leads` — confirmed directly in `right-board-controller.controller.ts`/`.service.ts` that `selectedLeads` was **already a real array parameter server-side** (the Phase 2b MVP's single-lead callers just never sent more than one id) — no backend change needed, only new client-side wrappers (`leadsApi.moveLeadsBulk`/`assignOperatorBulk`/`deleteLeadsBulk`) alongside the existing single-lead ones (left unchanged, still used by drag-and-drop and the details modal).
- `PATCH /archived-leads-list/archive-multiple` (body `{leadIds, note?}`, excludes already-sold leads server-side) and `PATCH /archived-leads-list/restore-multiple` (body `{leadIds}`) — real, already-built bulk endpoints (confirmed in `archived-leads-list.controller.ts`, not something this pass had to add).
- **Sold/Rejected/Trash tabs have no bulk endpoint of any kind** (single-lead `PATCH .../:id/restore` / `POST /leads/:id/restore` / `DELETE /leads/:id/permanent` only, confirmed by reading every controller in scope) — row-selection bulk actions for these three tabs are **explicitly deferred**, not silently dropped: building "bulk" there would mean a client-side loop of N sequential/parallel single-id calls with its own partial-failure UX to design, which is a real scope item on its own, not a quick add to this pass.

**Trace — filtered bulk actions (act on every lead matching current filters), a real dedicated endpoint pair, not client-side id-resolution:**
- `POST /leads-list/bulk/preview` → `{count}` and `POST /leads-list/bulk/execute` → `{affected}` (`leads-list.controller.ts`/`.service.ts`) — confirmed these already exist and already do exactly what the brief asked for: the backend resolves every matching lead id **server-side** (paginating internally, capped at 50,000 rows) and applies one of `archive | delete | move | assign | moveBoard` in a single call. No client-side "fetch every page of ids, then call the array endpoint" was needed — the endpoint takes `filters` directly, same shape `LeadFilters`/`filtersToLeadListFilters()` already produces for `leads-list`/`rejected-leads-list`/`archived-leads-list`.
- Base query for both endpoints is scoped to `archived=false, sold=false, rejected=false, deleted_at is null` (confirmed directly in `leads-list.service.ts`'s `buildFilteredIdQuery()`/`baseLeadsSelect()`) — i.e. exactly the Active tab's own filter scope. This means the filtered-bulk dialog only makes sense from the Active tab; Sold/Rejected/Archived/Trash have no equivalent filtered-bulk endpoint of their own, confirmed by reading each controller, not assumed.
- **Cross-board move (`moveBoard` + `stageMapping`) is real and was checked, not assumed same-board-only**: `right-board-controller.service.ts`'s `moveLeadsToBoard()` requires a per-source-column mapping (`{sourceColumnId, targetColumnId | (createNew, name, color)}`) for every distinct stage a moved lead currently sits in. Rather than reproducing the old frontend's full "preserve stage names automatically, per-stage matching preview" UX, this pass builds the simpler "single target stage" mode only: every column on the current board gets mapped to one chosen destination column on the target board (`columns.map(c => ({sourceColumnId: c.id, targetColumnId}))`) — safe even for source columns that happen to have no currently-matching leads, since the backend only consults a mapping for stages the moved leads are actually in. The old frontend's second mode ("preserve stage names, auto-match by name, create missing ones") is explicitly deferred — noted below, not silently dropped.
- `delete` on this endpoint resolves to the same soft-delete-to-Trash as everywhere else (`rightBoard.deleteLeads(ids)` internally, confirmed in `leads-list.service.ts`'s `executeFilteredBulk()`), so the confirm UX is worded accordingly ("moves to Trash"), not "permanently delete."

**Built:**
- `src/services/api/leads.ts` extended — `leadsApi.moveLeadsBulk`/`.assignOperatorBulk`/`.deleteLeadsBulk` (id-array wrappers around the already-bulk-capable `right-board-controller` endpoints), `archivedLeadsApi.archiveMultiple`/`.restoreMultiple`, and a new `leadsListBulkApi.preview`/`.execute` export with `FilteredBulkAction`/`StageMoveMapping`/`FilteredBulkParams`/`FilteredBulkExecuteParams` types (mirroring the backend's own `right-board-controller.service.ts` `StageMoveMapping` shape).
- `src/features/leads/hooks/useLeadSelection.ts` (new) — row multi-select as genuinely local UI state (`Set<string>`, not TanStack Query), per this project's state-separation rule; `toggleAll()` is additive against a given visible-id list (e.g. the current page) so a multi-page selection survives pagination instead of being silently clobbered.
- `src/features/leads/hooks/useLeadMutations.ts` extended — `useBulkMoveColumnMutation`/`useBulkAssignOperatorMutation`/`useBulkArchiveLeadsMutation`/`useBulkDeleteLeadsMutation`, same full-set `invalidateAllLeadViews` pattern as every other cross-view mutation in this file (no optimistic patch — a bulk action can span multiple columns/pages at once, unlike the single-lead drag-and-drop move).
- `src/features/leads/hooks/useArchivedLeads.ts` extended — `useBulkRestoreArchivedLeadsMutation`.
- `src/features/leads/hooks/useFilteredBulkActions.ts` (new) — `useFilteredBulkPreviewQuery`/`useFilteredBulkExecuteMutation`.
- `src/features/leads/components/RowCheckbox.tsx` (new) — plain native `<input type="checkbox">` with `indeterminate` support, same pattern already established by `AmocrmBoardsModal.tsx`'s `BoardCheckbox` (avoids guessing at an unverified HeroUI `Checkbox` compound anatomy for a one-off tri-state "select all on this page" control) — reused across every row-selection surface this pass touches.
- `src/features/leads/components/BulkActionsBar.tsx` (new) — row-selection toolbar (assign/move/archive/delete), appears once ≥1 lead is selected on the Active tab; double-submit guarded (`pending` check before every action), inline error via the shared `leadActionErrorMessage`, `window.confirm` before archive/delete.
- `src/features/leads/components/FilteredBulkActionsDialog.tsx` (new) — live preview count (re-fetched whenever `filters` change), panel-per-action UI (assign/move/archive/delete), `window.confirm` before archive, **type-the-match-count-to-confirm** before delete (matching the old frontend's `FilteredBulkActionsDialog.tsx` safety pattern for this exact kind of mass, filter-scoped mutation), move panel supports both same-board (single `columnId`) and cross-board (`moveBoard` + `stageMapping`, single-target-stage mode) via a target-board `Select` sourced from the existing `leadsBoardsApi`/`useLeadsBoardsQuery`/`useLeadsBoardColumnsQuery` (no new boards data layer needed).
- `LeadsListTable.tsx`/`ArchivedLeadsTable.tsx` — added an optional checkbox column (header select-all/indeterminate against the current page's visible ids, per-row checkbox), gated behind an optional `selection` prop so both tables stay usable unselected elsewhere.
- `LeadCard.tsx`/`KanbanColumn.tsx`/`KanbanBoard.tsx` — added an optional per-card checkbox (`selected`/`onToggleSelect`, both optional) threaded down from `KanbanBoard`'s new optional `selection` prop, so Kanban cards and the List table share one selection set on the Active tab.
- `src/app/(protected)/leads/page.tsx` — one shared `useLeadSelection()` instance for the Active tab (survives a Kanban/List view-mode toggle), a new "Bulk actions" toolbar icon button (Active tab only, opens `FilteredBulkActionsDialog`), `BulkActionsBar` rendered above the content area whenever the selection is non-empty. Selection (and every tab's own page state) is cleared via the existing render-time "adjust state on prop change" pattern whenever the board, filters, **or active tab** change (the tab dependency is new this pass — previously only board/filters reset page state) — a selection made on one tab/board/filter combination stops making sense the moment any of those change.

**Explicitly deferred within this slice (not oversights):**
- Sold/Rejected/Trash tabs' row-selection bulk actions — no bulk endpoint exists for any of the three (see the trace above); would need a client-side multi-call loop with its own partial-failure UX, scoped as a follow-up rather than folded into this pass.
- The old frontend's "preserve stage names" cross-board move mode (auto-match by name, create missing target columns) — this pass only builds the simpler "map every stage to one chosen target stage" mode; `StageMoveMapping.createNew` is typed and supported by the backend but never sent by this dialog.
- `selectedColumns` (per-tab column/stage scoping) on the filtered-bulk preview/execute calls — accepted by both endpoints but not wired, consistent with every other list view in this feature already omitting it (see the 2c-1 writeup).
- A toast/notification system for bulk-action success — this feature still has none (see `FilteredBulkActionsDialog.tsx`'s inline comment); success is surfaced by the dialog closing and the affected views refetching, same as every other mutation dialog in this feature (`CreateLeadDialog`, `MarkSoldDialog`, etc.).
- Kanban board-wide "select all" (there's no single flat list of "every visible lead" in the Kanban view the way there is in `LeadsListTable`, since each column paginates independently) — only per-card and per-list-page select-all are built; a board-wide select-all would need its own affordance design, not bundled into this pass.

**Verification**: `tsc --noEmit`, `eslint .` (full repo), `next build` all clean (zero errors/warnings) — one real fix made along the way (an initial `@gravity-ui/icons` `UserPlus` import didn't exist in this icon set; verified via a direct Node `require()` probe of the package before picking `PersonPlus` as the real available equivalent, rather than guessing a name from the old frontend's `lucide-react` icon). `next build`'s route table still shows `/leads` prerendering alongside the other protected routes. `new-operatora-dev` (PM2, port 3033) restarted; `GET /leads` still correctly 307-redirects unauthenticated requests to `/login?next=%2Fleads`, which itself returns a clean 200 login page with no server crash or Next.js error overlay in the HTML — same standing verification bar as every prior phase (still no test account/workspace on `operatora_test` for authenticated click-through of the actual row-selection/filtered-bulk flows — same limitation flagged throughout this file).

### Leads — details panel expansion (done 2026-08-15)

Phase 2c-4. Old-frontend reference read for UX/field reference only (`components/leads/{LeadComments,LeadTimeline,LeadTagPicker,LeadTagChips,LeadAdditionalPhones,LinkConversationDialog}.tsx`, `hooks/{useLeadTags,useLeadLifecycleEvents,useLeadPhones}.ts`, `lib/leadLifecycleEvents.ts`), never modified — every tab's backend support traced directly against the real backend (`/www/wwwroot/test.operatora.ai/app/backend/src/leads-controller/`, `src/tasks/`, `src/db-proxy/table-registry.ts`, `prisma/schema.prisma`), not assumed from the old frontend's calls. `LeadDetailsModal.tsx` (previously a single-view Phase 2b MVP panel) is now a HeroUI `Tabs`-driven panel — Info (existing fields, now plus additional phone numbers) + Comments/Timeline/Tags/Conversations/SMS/Tasks/Stats, each its own component under `features/leads/components/Lead*Tab.tsx`, not one large file. Every non-Info tab's data hook is `enabled` only while that tab is actually selected (`isActive` prop threaded through) — opening the drawer never fires all eight tabs' requests at once, matching `useConversationAudio`'s established "fetch on demand, not on mount" rule.

**Trace per tab:**
- **Comments** — `leads-comments.controller.ts`/`.service.ts`, a real, already-built `lead_comments` CRUD surface: `POST /leads-comments/list` (`{leadId}` in the body, not a `GET` — confirmed directly, not assumed REST-ful), `POST /leads-comments/create` (`{leadId, content, imageUrls}`), `PATCH /leads-comments/:id`, `DELETE /leads-comments/:id`, `POST /leads-comments/upload` (multipart, 5 MB cap, returns `{url, publicUrl}`). Full create/delete + file-attachment upload built; edit (`PATCH`) is real but deliberately not wired — a low-value affordance, delete + re-post covers it.
- **Timeline/Activity** — `lead_lifecycle_events`, an immutable, DB-triggered journal registered `readOnly: true` in `table-registry.ts` (all mutations happen inside Postgres itself, confirmed directly — no write path exists or is expected). Read via the db-proxy, field set and event-type switch ported from the old frontend's `lib/leadLifecycleEvents.ts` (English strings, not i18next — this app has no lead-feature i18n layer). `actor_id` resolves to a name via the already-fetched Team Members list.
- **Tags** — `lead_tags` (catalog) + `lead_tag_assignments` (M2M), both registered `scope: 'workspace', writeRoles: ALL_APP_ROLES` — the same catalog table `features/lead-automations/`'s tag picker already reads via `dbProxyQuery` (not duplicated, a sibling read of the same table). Get-or-create by case-insensitive name match + instant toggle-assign, matching the old frontend's `useLeadTagMutations` semantics exactly.
- **Additional phone numbers** (Info tab) — confirmed real, dedicated (not generic-proxy) endpoints: `GET/POST /leads/:id/phones`, `PATCH/DELETE /leads/:id/phones/:phoneId` (`leads.controller.ts`), which normalize/validate and refuse a number already claimed by another lead in the workspace — logic only reachable through these endpoints, not the generic `lead_phones` proxy registration (which also exists, for other backend-internal callers).
- **Conversations** — no dedicated "conversations for lead X" endpoint exists; linking is done entirely via `conversations.entities` (jsonb array of `{lead_id}` objects), confirmed directly in the old frontend's `LinkConversationDialog.tsx`. Read/write both go through the `conversations` db-proxy registration (`scope: 'workspace', writeRoles: ALL_APP_ROLES`). **Deliberately did not use the proxy's `contains`/`cs` filter op** to query "entities contains this lead" server-side — read `db-proxy.service.ts`'s filter builder directly and found the `cs`/`contains` case pushes the raw JS array as the SQL parameter with **no `::jsonb` cast** (`${col} @> $idx`, `params.push(f.value)`), unlike that same file's own `values` insert/update path (`bindValue()`), which explicitly JSON-stringifies and casts specifically to avoid an "invalid input syntax for type json" error. A real, traceable landmine, not a guess. Built around it: a bounded most-recent-300 client-side scan + filter (same shape as `LinkConversationDialog.tsx`'s own `.limit(100)` + client-filter query), plus link/unlink (which safely use the `values` write path, confirmed cast-safe). **Known limitation, documented in the component's own doc comment**: a conversation linked long ago that has since scrolled outside the most-recent 300 workspace-wide won't surface. No per-conversation deep-link route exists in this app (`/conversations` has no id-addressable detail URL, confirmed by reading its `page.tsx` — the detail panel is pure client state) — the tab links to `/conversations` only, not a fabricated per-id URL.
- **SMS** — read-only sent-message log. Deliberately reads the local `lead_sms_messages` table (via db-proxy, `scope: 'workspace', writeRoles: ALL_APP_ROLES`; columns confirmed directly in `prisma/schema.prisma`: `status`/`sent_at`/`delivered_at`/`error_message`/`provider_message_id`/`message_body`/`phone_number` — exactly the delivery-status shape needed) rather than `GET /sms/messages?lead_id=` (`sms.controller.ts`): that endpoint proxies to an external, per-workspace-configured SMS gateway microservice (`resolveGatewayForUser()`), whose response shape is outside this repo's control and untraceable from it, and which 400s when no gateway/channel port is configured. `lead_sms_messages` is the same table the backend's own AI tooling (`chat-tools.service.ts`) reads for the equivalent "history for this lead" purpose. Compose is explicitly out of scope (Phase 2c-8).
- **Tasks** — `tasks.controller.ts`/`tasks.service.ts` (`src/tasks/`), a real, already-live `operator_tasks` CRUD surface this app had no data layer for yet (only `features/tasks-settings/` for the separate workspace-automation *config*). `GET /tasks?lead_id=` returns the lead's full task history regardless of assignee/status (confirmed directly in the controller's own summary). Built list + inline create (title/type/due date/assignee) + mark-complete (with the server-mandatory `closure_comment`, confirmed in `TasksService.complete()`'s own validation). Assigning to someone other than yourself 403s server-side unless the caller is a manager — surfaced as a normal inline error, not pre-blocked client-side.
- **Stats** — `lead-stats.controller.ts`/`.service.ts`, `GET /lead-stats/:leadId?workspace_id=`, a real live-aggregated-counters endpoint (`days_in_pipeline`, `calls_count`, `tasks_closed`/`tasks_overdue`, `notes_count`, `client_chats_count`, `internal_mentions_count`, `checklist_total`/`checklist_filled`) — every number rendered is exactly what this endpoint returns, nothing computed or fabricated client-side.

**Built:**
- `src/features/leads/types.ts` extended — `LeadComment`, `LeadStats`, `LeadPhone`, `LeadTag`, `LeadLifecycleEvent`/`LeadLifecycleEventType` + `describeLeadLifecycleEvent()`, `LeadTask`/`LeadTaskType`/`LeadTaskStatus`, `LeadSmsMessage`/`LeadSmsStatus`, `LeadLinkedConversation`.
- `src/services/api/leads.ts` extended — `leadCommentsApi`, `leadStatsApi`, `leadPhonesApi`.
- New: `src/services/api/{leadTags,leadLifecycleEvents,leadSms,leadTasks,leadConversationLinks}.ts`.
- `src/features/leads/schema.ts` extended — `leadCommentSchema`, `leadNewTagSchema`, `leadAdditionalPhoneSchema`, `leadTaskSchema`/`LEAD_TASK_TYPES`, `leadTaskCompleteSchema`.
- New hooks: `src/features/leads/hooks/{useLeadComments,useLeadTags,useLeadTimeline,useLeadSms,useLeadTasks,useLeadConversationLinks,useLeadStats,useLeadPhones}.ts`.
- New components: `src/features/leads/components/{LeadCommentsTab,LeadTimelineTab,LeadTagsTab,LeadConversationsTab,LeadSmsTab,LeadTasksTab,LeadStatsTab,LeadAdditionalPhones}.tsx`.
- `src/features/leads/components/LeadDetailsModal.tsx` rewired — HeroUI `Tabs`/`Tabs.List`/`Tabs.Tab` inside the existing `Drawer`, widened `max-w-md` → `max-w-2xl` to fit the tab strip; Info tab content unchanged except for the appended `LeadAdditionalPhones`.

**Explicitly deferred within this slice (not oversights):**
- Comment editing (`PATCH /leads-comments/:id` is real and traced, just not wired — see Comments trace above).
- Conversations tab's completeness beyond the most-recent 300 workspace-wide conversations (documented landmine in the jsonb-filter proxy op — see trace above); no dedicated backend "conversations for lead X" endpoint exists to fix this without a backend change, which is out of this frontend's scope.
- Per-conversation deep link (no id-addressable `/conversations` detail route exists yet in this app).
- SMS compose (Phase 2c-8, templates + compose + this same log).
- Tags color picker for a newly created tag (auto-assigned from the same palette as the old frontend's `TAG_COLORS`, not user-choosable this pass).

**Verification**: `tsc --noEmit`, `eslint .` (full repo), `next build` all clean (zero errors/warnings) — one real fix made along the way (a Zod `.default([])` + `.refine()` combination on `leadCommentSchema` produced an input/output type mismatch against `zodResolver`'s generic; fixed by dropping the default and always passing `imageUrls: []` explicitly via `useForm`'s `defaultValues`, and a `LeadTagsTab` first draft using `useForm`'s `watch()` for a live filter triggered a React Compiler "cannot memoize" warning — replaced with plain `useState`, since it was a single search input with no real multi-field form validation to justify `react-hook-form` there). `next build`'s route table still shows `/leads` prerendering alongside the other protected routes. `new-operatora-dev` (PM2, port 3033) restarted; `GET /leads` still correctly 307-redirects unauthenticated requests to `/login?next=%2Fleads`, which itself returns a clean 200 login page with no server crash or Next.js error overlay in the HTML — same standing verification bar as every prior phase (still no test account/workspace on `operatora_test` for authenticated click-through of the actual tab content — same limitation flagged throughout this file).

### Leads — column/board management (done 2026-08-15)

Phase 2c-5: column CRUD + WIP limits, guided required-field/deadline gate dialog, board create/share/switcher polish. Old-frontend reference read for UX only (`components/leads/{ShareBoardDialog,PipelineSettingsDialog}.tsx`), never modified — every endpoint traced directly against the real backend (`/www/wwwroot/test.operatora.ai/app/backend/src/leads-controller/{columns,board}/`), not assumed.

**Trace (all real, none stubbed) — column CRUD:**
- `columns.controller.ts`/`.service.ts` (`/columns`, a dedicated controller distinct from the Kanban-rendering `lead-board`/`right-board-controller` endpoints): `GET /columns?boardId=` (every column, hidden included — unlike the board-rendering query), `POST /columns` (`CreateColumnDto`: `name`, `board_id`, `display_order?`, `color?`, `is_hidden?`, `description?`, `lead_limit?` — the WIP limit field, confirmed real, `null`/omitted = unlimited), `PATCH /columns/:id`, `DELETE /columns/:id`, `POST /columns/reorder` (`{board_id, column_id, direction: 'up'|'down'}`), `POST /columns/order` (full-array reorder — not wired, see deferred list). Server-side rules confirmed directly in `ColumnsService`: the two locked Sold/Rejected `special_stage_kind` marker columns reject rename/edit/delete outright; `is_default`-seeded columns are editable but never deletable; delete additionally 400s if the column still has active (non-archived/non-sold/non-deleted) leads.
- `LeadBoardColumn` (`features/leads/types.ts`) extended with `lead_limit`/`special_stage_kind` — both were already present on every `GET /lead-board/:boardId` response (`select('*')` server-side) and on `/columns`, just not typed until this pass needed them; no new query was required to read them for the board's own WIP-badge rendering.

**Trace — WIP limit enforcement (already real, confirmed, not built this pass):** `lib/column-wip-limit.ts`'s `assertColumnHasCapacity()` — called from `right-board-controller.service.ts`'s `changeColumn()` before every move — blocks a move that would push a column's active-lead count over `lead_limit`. This pass only adds the **display** (per-column `count/limit` badge, `warning`-colored when at/over limit) — the gate itself was already enforced server-side since before this pass, per the Kanban board MVP's own trace.

**Trace — board CRUD/share:**
- `board.controller.ts`/`.service.ts` (`/boards`): `POST /boards` (`{name}` — pre-seeds the default 6-column pipeline + Sold/Rejected markers server-side, so a new board is immediately usable), `PATCH /boards/:id` (rename), `DELETE /boards/:id` (blocked server-side if it's the workspace's default board — first by `display_order`/`created_at` — surfaced via `leadActionErrorMessage`, not pre-guessed client-side), `GET/PATCH /boards/:id/share` (`{enabled, hasPassword, token, expiresAt}` / `{enabled?, password?, expiresAt?}` — `password: null` clears it, `expiresAt: null` makes the link permanent), `POST /boards/:id/share/rotate` (issues a new token, invalidating the old link).
- `public-board.controller.ts` (`/public/boards/:token`, `@Public()`, unauthenticated): `GET` with an optional `x-board-password` header — 401 means password required/incorrect, 404 means no enabled link (deliberately indistinguishable from "never existed", confirmed in the service's own comment). Wired as `publicLeadBoardApi` + a new standalone page, see below.

**Trace — the required-field/deadline gate, and a real backend bug found while tracing it (not assumed from the brief):**
- `automation.service.ts`'s `assertDeadlineBeforeColumnMove()` throws a plain `Error('FIELD_REQUIRED:field1,field2')` — there is **no `DEADLINE_REQUIRED` constant anywhere in the current backend** (grepped the whole repo); a missing deadline is just `FIELD_REQUIRED:deadline`, one name among possibly several. The brief's own framing ("a 400 DEADLINE_REQUIRED/FIELD_REQUIRED...") was the starting assumption, corrected by reading the actual code.
- **Mark Sold/Mark Rejected are clean**: `sold-leads-list.service.ts`'s `assertRequiredFieldsForSold()` and `rejected-leads-list.service.ts`'s equivalent both re-throw the gate failure as `new BadRequestException(msg)` with nothing wrapping it further — this reaches the client as a real, parseable `400 {message: "FIELD_REQUIRED:..."}`. Confirmed by reading `markAsSold()`/`markAsRejected()` end-to-end — no outer catch reprocesses the exception.
- **The raw Kanban column-move endpoint is not clean — a real, traced backend bug**: `right-board-controller.service.ts`'s `changeColumn()` wraps its *entire* body (every step: workspace/board consistency checks, the deadline/field gate, the WIP-limit check, the actual update) in one `try { ... } catch (error: any) { throw new Error(error?.message || 'Failed to change column'); }`. That outer catch runs even when the inner gate check already converted the failure into a `BadRequestException` — re-wrapping it into a **plain `Error`, which is not an `HttpException`**. The backend's global `AllExceptionsFilter` (`common/filters/all-exceptions.filter.ts`) only preserves a thrown exception's real status/message when `exception instanceof HttpException`; for a plain `Error` it reports a generic `500 {message: "Internal server error"}` instead. Net effect: a blocked column move (missing required field/deadline, WIP limit reached, or even the earlier cross-board/cross-workspace guards) currently reaches this frontend as an **undifferentiated 500 with no usable detail**, not the 400 the brief assumed. This also affects the row-selection bulk move (`moveLeadsBulk`, same endpoint) and the filtered-bulk `move`/`moveBoard` actions (`leads-list.service.ts`'s `executeFilteredBulk()` calls `this.rightBoard.changeColumn()`/`moveLeadsToBoard()` directly). **Never modified the backend to fix this** (out of scope, per the standing rule) — see how this was handled below.
- `deadline.controller.ts`/`.service.ts` (`PATCH /deadline/:leadId`, `{deadline}`) — the real, dedicated endpoint for setting/clearing a lead's deadline, confirmed clean (no wrapping bug). `leads.controller.ts`'s generic `PATCH /leads/:id` forwards its whole body straight to Supabase's `.update()` with no server-side whitelist stripping (confirmed directly in `LeadsService.update()`), so any real `leads` column — not just `UpdateLeadDto`'s narrower declared set — can be patched this way too; used for every non-deadline required field.

**Built:**
- `src/services/api/leadsColumns.ts` (new) — `columnsApi.list/create/update/remove/reorder`.
- `src/services/api/leadsBoards.ts` extended — `leadsBoardsApi.create/rename/remove/getShare/updateShare/rotateShare`, on top of the existing `list`/`columns` pickers (unchanged, still used by Google Sheets/Lead Automations/Tasks Settings).
- `src/services/api/leadsPublicBoard.ts` (new) — `publicLeadBoardApi.get(token, password?)`, using `apiFetch`'s existing `public: true` option (no new auth path needed).
- `src/services/api/leadDeadline.ts` (new) — `deadlineApi.update`.
- `src/services/api/leads.ts` extended — `leadsApi.patchLead` (generic `PATCH /leads/:id` wrapper).
- `src/features/leads/types.ts` extended — `LeadBoardColumn.lead_limit`/`.special_stage_kind`, `BUILTIN_LEAD_FIELD_META` (presentational-only field-name → input-widget map for the guided dialog — explicitly **not** a reimplementation of which fields are required for which column; that logic stays entirely server-side and is never duplicated here), `LeadBoardShareSettings`, `PublicLeadBoardSnapshot`.
- `src/features/leads/schema.ts` extended — `columnFormSchema`, `createBoardSchema`.
- `src/features/leads/hooks/{useColumnManagement,useBoardManagement}.ts` (new) — column/board CRUD + share mutations, all invalidating the right existing query keys (`lead-board`'s columns for the Kanban board, the shared `useLeadsBoardsQuery` list for the switcher — exported a `leadsBoardsQueryKey()` helper from `features/leads-boards/hooks/useLeadsBoards.ts` so this doesn't hardcode/duplicate that key).
- `src/features/leads/components/ManageColumnsDialog.tsx` (new) — column list (up/down reorder buttons, not a second `@dnd-kit/sortable` dependency — this feature already deliberately avoids `sortable`, see the Kanban board MVP's own doc comment) + inline create/edit form (name, color via a native `<input type="color">`, description, WIP limit, hidden toggle). Locked marker columns are excluded from the list entirely (editing/deleting them always 400s server-side); the default column's delete button is disabled with a tooltip instead of a round-trip.
- `src/features/leads/components/CreateBoardDialog.tsx`/`ShareBoardDialog.tsx` (new) — board creation (name only) and the full share-settings surface (enable toggle, copy/rotate link, expiry presets, password set/remove).
- `src/app/board/[token]/page.tsx` (new) — the public, unauthenticated share-link viewer itself, deliberately built (not left as a dead link): `ShareBoardDialog` copies a `{origin}/board/{token}` URL matching the old frontend's own `/board/:token` shape, and a share feature that copies a link to nowhere isn't a working feature. Outside `(protected)`/`ROUTES` (so `proxy.ts`'s auto-derived protected-prefix list — which the whole app relies on for new routes to be protected automatically — correctly leaves it public), password-gated via the `401`/`x-board-password` contract above, `retry: false` on the query (a wrong password is expected user input, never auto-retried). Minimal, clean read-only render (columns + name/phone cards) — not a copy of the old frontend's `PublicBoard.tsx` visual design.
- `src/features/leads/components/RequireFieldDialog.tsx` (new) — the guided fix-and-retry dialog, wired into `MarkSoldDialog`/`MarkRejectedDialog` only (both now take a `lead: LeadRow` prop instead of just `leadId`, needed for the custom-fields merge — the one caller, `LeadDetailsModal`, updated to match). Catches a `FIELD_REQUIRED:...` 400 via the new `parseFieldRequiredError()` helper (`leadActionError.ts`), renders one input per missing field (a native `datetime-local` input for `deadline` via `PATCH /deadline/:leadId`; a `Select` for `marital_status`/`academic_status` from the existing option constants; an operator `Select` for `assigned_operator_id`; free text for anything else, written into `custom_fields.<name>` since this app's custom-field-type engine isn't built yet, Phase 2c-6), saves once via `leadsApi.patchLead`, then retries the original mark-sold/mark-rejected call exactly once (no automatic loop).
- **Kanban column-move failures — upgraded honestly, not with a dialog that can't work.** Given the traced backend bug above, `leadActionErrorMessage()` gained a `context: "move"` parameter used by `KanbanBoard.tsx`'s drag-and-drop handler and `LeadDetailsModal.tsx`'s column `Select`: instead of the generic "something went wrong on our end" (or, for drag-and-drop specifically, previously **no visible message at all** — confirmed by reading `KanbanBoard.tsx` before this pass; only the optimistic-update rollback was visible, not an error), it now shows "This move was blocked by a workspace rule — most likely a required field or deadline needs to be set on this lead first, or the column's lead limit was reached. Open the lead to review it, or ask an admin." `KanbanBoard.tsx` also gained a dismissible inline error banner (it previously had none at all for a failed drag).
- `src/features/leads/components/KanbanColumn.tsx` — WIP limit display: the count chip becomes `count/limit` (vs. bare `count` for an unlimited column) and turns `warning`-colored at/over the limit; the column's border also gets a subtle warning tint. Purely a display upgrade — the gate itself was already enforced server-side.
- `src/app/(protected)/leads/page.tsx` — board switcher now always renders (previously only when a workspace already had >1 board, so board creation was undiscoverable), plus "New board" and "Share board" icon buttons next to it; "Manage columns" toolbar button (previously a permanently-disabled "coming soon" placeholder) now opens `ManageColumnsDialog`.

**Explicitly deferred within this slice (not oversights):**
- `POST /columns/order` (full-array bulk reorder) — real endpoint, not wired; the up/down single-step reorder (`POST /columns/reorder`) covers the same real capability without a `@dnd-kit/sortable` dependency this feature has already twice deliberately avoided.
- The guided `RequireFieldDialog` does **not** cover the raw Kanban column-move / row-selection bulk move / filtered-bulk move paths — not an oversight, a direct consequence of the traced backend bug above: those endpoints currently return an undifferentiated 500 with no parseable field list, so a dialog claiming to know which fields are missing would be fabricating data the response doesn't contain. Fixing this for real requires a backend change (removing/narrowing `changeColumn()`'s outer catch-and-rewrap so its own `BadRequestException`s survive) — flagged here as a precise, traceable finding for whoever owns that repo, not silently patched around and not silently ignored.
- Board CRUD's rename action (`leadsBoardsApi.rename`) has a real client method but no UI trigger yet — `ManageColumnsDialog`/`ShareBoardDialog` don't expose a board-rename field; the switcher itself has no inline rename. A small gap, left for a follow-up rather than bolted onto either existing dialog's unrelated scope.
- Custom field values entered into `RequireFieldDialog` for a non-builtin field name are always free text — no type-aware input (select/number/date/checkbox), since this app's custom-field-type engine doesn't exist yet (Phase 2c-6, explicitly upcoming).
- The public board page's read-only render omits `age`/`marital_status`/`academic_status`/`deadline`/`custom_fields`, even though `getPublicBoard()`'s response includes them — name + phone per card only, matching what a first-pass minimal public viewer needs; the backend response already carries the rest for a future richer pass without another trace.

**Verification**: `tsc --noEmit`, `eslint .` (full repo), `next build` all clean (zero errors/warnings) — one real fix made along the way (a HeroUI `Button` doesn't accept a plain `title` prop, caught by `tsc`; the one call site needing a conditional tooltip was switched to the existing `IconButton` wrapper, which does support one, instead of reaching for a native DOM attribute a component doesn't forward). `next build`'s route table shows the new `/board/[token]` route as dynamic (`ƒ`, expected — client-rendered per-token data, no static param set), alongside every other route unchanged. `new-operatora-dev` (PM2, port 3033) restarted; `GET /leads` still correctly 307-redirects unauthenticated requests to `/login?next=%2Fleads`; `GET /board/some-nonexistent-token` returns a clean `200` with no server crash or Next.js error overlay in the HTML (renders the "Link not available" empty state, since a real token was never exercised — no test workspace exists, same standing limitation as every phase). **Not verified** (same standing limitation as every phase since Phase 2a): interactive click-through of an authenticated session — still no test account/workspace on `operatora_test`. Recommend exercising, once a login exists: the full column CRUD/reorder/WIP-limit round trip against a real board; the required-field guided dialog against a workspace with a real automation rule requiring a field on a stage; board create/share/rotate/password/expiry against a real share link opened in an incognito tab; and confirming firsthand (via browser devtools, not just static tracing) that a blocked Kanban drag-and-drop move really does surface as a 500 as traced above — this was derived from careful static reading of `right-board-controller.service.ts`/`all-exceptions.filter.ts`, not an authenticated request/response pair, since none was available in this sandbox.

### Leads — custom fields engine + field visibility (done 2026-08-15)

Phase 2c-6. Old-frontend reference read for UX/data-shape only (`components/leads/LeadFieldVisibilityManager.tsx`, `lib/{leadCustomFields,leadFieldConfig,leadColumnFieldGate}.ts`, `hooks/{useLeadFieldConfig,useLeadCustomFields,useLeadFieldVisibility,useLeadDetailsFieldVisibility}.ts`), never modified — every endpoint traced directly against the real backend (`/www/wwwroot/test.operatora.ai/app/backend/src/leads-controller/{lead-custom-fields,lead-field-config,lead-search,preview-leads}/`, `db-proxy/table-registry.ts`, `prisma/schema.prisma`), not assumed from the old frontend's calls.

**Trace (all real, none stubbed):**
- **Field definitions** — `lead-custom-fields.controller.ts`/`.service.ts` (`/lead-custom-fields`): `GET` (list, workspace-scoped, ordered by `display_order`), `POST`/`PATCH` (validated per-type — the service's own `validate()` enforces the exact `field_options` shape per `field_type`, confirmed directly, not guessed), `DELETE`. `ALLOWED_FIELD_TYPES` (`lead-custom-fields.types.ts`) confirmed to list **all 19 types** the brief asked for: text, number, amount, date, select, checkbox, email, phone_number, url, multi_select, status, created_time, last_edited_time, created_by, last_edited_by, relation, rollup, formula, place — **none dropped**. `is_required`/`required_column_ids` (per-stage required config, item 2 of this slice) are real, validated columns on the same row — `required_column_ids` is a sanitized UUID array checked against a column-move automation gate server-side (`automation-gates.util.ts`'s `getMissingRequiredFields()`, which already merges `lead_custom_fields.required_column_ids` into its check — this slice only had to build the UI for a config surface the gate-evaluation code already consumed).
- **`relation` is real, not a client-only placeholder** — backed by a dedicated lookup pair confirmed in `lead-search.controller.ts`/`.service.ts`: `GET /lead-search?q=` (name/phone search) and `GET /lead-search/by-ids?ids=` (resolve ids → `{id,name,age,custom_fields}`, explicitly built "for chips + rollup aggregation" per that service's own doc comment). A relation value is an array of real lead ids stored in `custom_fields.<name>`, always resolvable through this real endpoint.
- **`rollup`/`formula` have no server-side computation engine** — confirmed by reading the whole backend: the service only validates+persists their *definition* (`{relationField,fn,targetField}` / `{expression,decimals,format}`), the aggregation/evaluation itself is client-side, same architecture the old frontend already used (`lib/customFieldTypes.ts`'s own `evaluateFormula()`/rollup logic, read for reference). This is legitimate, not a fake stand-in — the definition is real and round-trips through a real, validated endpoint, and what it operates over (other real custom-field values, or a real relation's server-resolved leads) is real data. `expr-eval` (`^2.0.2`, new dependency — a pure parser, no `eval`/`Function`, so a workspace-authored formula expression can never reach anything beyond its own variable map) added for `formula`, matching the old frontend's own safety rationale for the same library.
- **Per-stage required-field config (item 2)** — no new endpoint needed; `required_column_ids` was already a real, gate-consumed column on `lead_custom_fields` (see above). `ManageCustomFieldsDialog`'s per-field editor now includes a stage checklist (against the board's own real columns, `useBoardColumnsManageQuery`) that writes straight into this column — this is the data layer `RequireFieldDialog` (built in 2c-5) now reads from.
- **Kanban-card field visibility (item 3, half)** — `preview-leads.controller.ts`, a real, dedicated, per-user REST surface (`lead_field_visibility` table): `GET /preview-leads/field-settings` (bundle: saved visibility rows + the workspace's current custom-field defs, one round trip — the same bundle `LeadFieldVisibilityManager` uses), `POST /preview-leads` (replace-all save). Traced field-key convention directly from the old frontend's own dialog (`custom_field_<name>` namespacing for custom fields) since the backend just stores whatever key the client sends.
- **Details-panel field visibility (item 3, other half) — a genuinely different real store, not this app's own invention.** The old frontend's `useLeadDetailsFieldVisibility` calls Supabase directly against `user_preferences` (`preference_type = 'lead_details_visibility'`), not `preview-leads` — traced the equivalent real path in this backend instead of unifying the two into one store that wouldn't match either: `user_preferences` is registered `scope: 'user'` in `db-proxy/table-registry.ts` (auto-scoped to the caller's own `user_id` server-side from the JWT — confirmed in `db-proxy.service.ts`'s `scopeValue()`/`prepareValues()`, same "never send the scope column yourself" precedent `leadTags.ts` already established for a workspace-scoped table), with a real `@@unique([user_id, preference_type])` constraint (confirmed in `prisma/schema.prisma`) that makes the db-proxy's real `upsert` method (`onConflict: 'user_id,preference_type'`) safe to use for the save path — read the proxy's `upsert()` implementation directly to confirm it also auto-injects the scope column via the same `prepareValues(..., enforceScope=true)` path `insert()` uses, not assumed.

**Built:**
- `src/features/leads/customFieldTypes.ts` (new) — the field-type registry: `CUSTOM_FIELD_TYPES`/`getFieldType()`, per-type `coerceCustomFieldValue()`/`validateCustomFieldValue()` (client-side UX validation only, backend remains authoritative), amount/currency + date/time-inclusion helpers, `evaluateFormula()`/`formatFormulaResult()` (expr-eval), `computeRollup()` (count/sum/avg/min/max over already-resolved relation targets), `colorForOption()` (mirrors the backend's own deterministic status-color hash exactly, confirmed field-for-field against `lead-custom-fields.types.ts`).
- `src/services/api/{leadCustomFields,leadSearch,leadFieldVisibility}.ts` (new) — the three real data-layer files traced above.
- `src/features/leads/hooks/{useLeadCustomFields,useLeadSearch,useFieldVisibility}.ts` (new) — list/CRUD query+mutation hooks, `buildCardVisibilityItems()` (merge saved rows + standard-field defaults + current custom fields, ported from the old frontend's `buildRowsFromBundle()`), `useVisibleCardFieldKeys()`.
- `src/features/leads/components/{CustomFieldValue,CustomFieldInput}.tsx` (new) — the type-aware read-only renderer and editable-widget dispatcher every other new/updated component below is built on; `CustomFieldInput` includes an inline `RelationFieldInput` (debounced search + result list + selected chips — a plain list, not a HeroUI `ComboBox`, whose exact compound anatomy wasn't worth verifying for this one narrow control, same "avoid guessing an unverified anatomy" precedent `RowCheckbox` already set).
- `src/features/leads/components/ManageCustomFieldsDialog.tsx` (new) — field CRUD (name/type/type-specific options editor: list options, status options, amount currency, date include-time, formula expression+decimals+format, rollup relation-field+function+target-field) + the per-stage required-field checklist (item 2).
- `src/features/leads/components/LeadFieldVisibilityManager.tsx` (new) — two-tab dialog (Kanban card / Details panel) matching the real backend split documented above, each with its own save + (card tab only, since it's the only one with a documented "old view" default set) a reset-to-default action.
- `src/features/leads/components/LeadCustomFieldsSection.tsx` (new) — `LeadDetailsModal`'s Info tab custom-fields block, rewritten from the earlier Phase 2b/2c-5 generic key/value dump into a type-aware read view (`CustomFieldValue`) with a batch Edit→Save toggle (`CustomFieldInput` per non-computed field, one `PATCH /leads/:id` for the whole set — same batch-write shape `RequireFieldDialog` already established, not a per-field round trip). Custom-field writes now also stamp `custom_fields.__last_edited_by_id` (the old frontend's own write convention for the `last_edited_by` computed type, ported exactly since it's a client-side bookkeeping key, not a backend contract).
- `CreateLeadDialog.tsx` — added a "Custom fields" section rendering every non-computed field via `CustomFieldInput`, client-validated against `is_required` before submit, sent as `custom_fields` on `POST /add-lead` (the DTO field isn't server-validated but is real — simply stored as-is, confirmed directly; corrects this file's earlier "custom_fields is intentionally never sent" comment, which was accurate only until this slice built the engine it was waiting on).
- `RequireFieldDialog.tsx` (2c-5) — extended exactly per the brief: a `FIELD_REQUIRED:<field>` name not in `BUILTIN_LEAD_FIELD_META` is now looked up against the real `lead_custom_fields` list and rendered with `CustomFieldInput` (validated via `validateCustomFieldValue`), not free text — free text is now only the last-resort fallback when no matching definition exists at all (e.g. a stale automation-rule field reference).
- `LeadCard.tsx` — renders every card-visible custom field (per the new visibility bundle) as a compact `label: value` line below the existing content, using `CustomFieldValue`'s `compact` mode; the visibility query is the same shared cache key across every mounted card (one fetch per board render, not one per card).
- `src/app/(protected)/leads/page.tsx` — two new Active-tab toolbar buttons: "Manage custom fields" (opens `ManageCustomFieldsDialog`) and "Field visibility" (opens `LeadFieldVisibilityManager`).
- `LeadRow.updated_at` (`features/leads/types.ts`) — typed for the first time (was always present on every `select('*')` read, confirmed in `prisma/schema.prisma`, just unused until the `last_edited_time` computed type needed it).

**Deliberately scoped down / dropped, with reasoning:**
- **Nothing was dropped from the 19-type allowlist** — every type traced as real is built with a real input/display path. `place`'s input is a plain address text field (matching the backend's own bare-string-or-`{address}`-object coercion) — no maps/geocoding integration, since no such endpoint exists to trace.
- **Built-in-field (not custom-field) hidden/required config** (`lead-field-config.controller.ts`, `workspace-wide` hidden/required flags for `first_name`/`phone_number`/etc.) — traced, confirmed real, but **not built this slice**: it's a workspace-wide admin setting distinct from both this slice's per-stage custom-field requirement and the field*visibility* manager the brief actually asked for; building an unrequested admin surface would be scope creep. Flagged here so a future slice doesn't have to re-discover it.
- **Formula/rollup client-side computation has the same inherent limitation the old frontend's does**: a rollup's target leads are resolved via `GET /lead-search/by-ids` (real, but not paginated beyond 200 ids server-side) and a formula only sees the current lead's own field values — no cross-lead formula inputs, matching the backend's own design (the backend doesn't support that either).
- **`ManageCustomFieldsDialog`'s rollup editor requires at least one existing `relation` field** to pick from (real UX constraint, not a bug) — creating a rollup before any relation field exists shows an inline "Create a Relation field first" hint rather than a broken picker.
- **No column-level required-field indicator UI** (e.g. a badge on `ManageColumnsDialog`'s column rows showing which custom fields gate that stage) — the checklist lives on the field's own editor (where the real data lives), a reverse "which fields require this column" view on the column side would be a nice-to-have cross-reference, not built this pass.
- **Comment editing / bulk custom-field edit across multiple leads** — out of scope, not requested.

**Verification**: `tsc --noEmit`, `eslint .` (full repo), `next build` all clean (zero errors/warnings) — fixes made along the way: a standalone (non-`TextField`-wrapped) HeroUI `Input` doesn't accept `isInvalid` (only `Select` does outside a `TextField` context) — caught by `tsc`, six `CustomFieldInput` branches corrected to drop the prop rather than wrapping every native-typed input in an extra `TextField` just to carry it; a `react-hooks/incompatible-library` warning on `ManageCustomFieldsDialog`'s `watch()` call — switched to `useWatch`, same fix already precedented in the Settings epic's Brand section; an `react/no-unescaped-entities` lint error on a literal quoted word in JSX copy — escaped to `&ldquo;`/`&rdquo;`. `next build`'s route table unchanged (`/leads` still prerendering alongside every other protected route — no new route added this slice). `new-operatora-dev` (PM2, port 3033) restarted; `GET /leads` still correctly 307-redirects unauthenticated requests to `/login?next=%2Fleads`, which itself returns a clean `200` login page with no server crash or Next.js error overlay in the HTML — same standing verification bar as every prior phase (still no test account/workspace on `operatora_test` for authenticated click-through of the actual field-CRUD/visibility-toggle/relation-search/rollup-computation flows — same limitation flagged throughout this file). Recommend exercising, once a login exists: creating one of each field type end-to-end (especially `relation`→`rollup` together, since that's the two-type interaction most likely to surface an edge case), the per-stage required checklist actually blocking a column move via Mark Sold/Mark Rejected's `RequireFieldDialog`, and both visibility-manager tabs saving/reloading correctly against the two real, distinct backing stores.

### Leads — duplicate detection (done 2026-08-15)

Phase 2c-7. Old frontend's `components/leads/DuplicateLeadsDialog.tsx` read for UX/flow reference only, never modified — the real contract was traced directly against a dedicated backend module, `leads-controller/duplicated-leads/{duplicated-leads.controller.ts,.service.ts}`.

**Trace — a real, dedicated, server-side merge endpoint exists; nothing was client-orchestrated:**
- `GET /duplicated-leads?boardId=` — scans every non-deleted lead on the board and groups by matching phone digits (7+ digits after stripping non-digits) or matching lowercased full name (>3 chars); a lead already grouped by phone is excluded from a name group (phone match takes priority). Returns `DuplicateGroupDto[]` (`{key, leads, duplicateBy: 'Phone Number' | 'Full Name'}`), `leads` being full `leads` rows.
- `DELETE /duplicated-leads` — `{leadIds}`, soft-deletes (same `deleted_at`/Trash mechanism as every other delete in this feature) and unlinks the given leads from their chats first (`unlinkLeadsFromChats`). Returns `{deletedCount}`.
- `POST /duplicated-leads/merge` — `{primaryLeadId, duplicateLeadIds}`. **This is the important finding**: confirmed by reading `DuplicatedLeadsService.mergeLeads()` line-by-line that this single endpoint atomically-per-request (all writes happen server-side within one HTTP request/response cycle, not a client-driven sequence that could be interrupted mid-flight by a network drop) relinks `telegram_chats`/`instagram_conversations` and `lead_comments` onto the survivor, merges phone numbers (deduped against what the survivor already has, extra ones dropped rather than violating the one-lead-per-number constraint), merges `custom_fields` (survivor wins on any key conflict), and sets `sold: true` on the survivor if *any* lead in the group (survivor or a duplicate) was sold — then soft-deletes the merged-away leads. Works even when a duplicate is sold, per the controller's own doc comment. Every field the brief asked to verify (phones/comments/chats absorption, sold-status carry-over) matched exactly — nothing had to be guessed or faked client-side.
- Both `DELETE`/`merge`/`GET` are workspace-scoped server-side via `assertBoardBelongsToCallerWorkspace`/`assertLeadsBelongToCallerWorkspace` before the service runs — no `workspace_id` sent by the client.

**Built:**
- `src/services/api/duplicateLeads.ts` (new) — `duplicateLeadsApi.list/removeMany/merge`, doc comment carries the atomicity trace above so a future reader doesn't have to re-derive it.
- `src/features/leads/hooks/useDuplicateLeads.ts` (new) — `useDuplicateLeadsQuery` (only fires while the dialog is open, no polling — a point-in-time scan, not a live view), `useDeleteDuplicateLeadsMutation`/`useMergeDuplicateLeadsMutation` (both invalidate the full `invalidateAllLeadViews` set plus the dialog's own scan query).
- `src/features/leads/components/DuplicateLeadsDialog.tsx` (new) — grouped-cluster list (matched-by chip, leads-per-group count), star-to-pick-primary per group (defaults to the sold lead in the group if any, else the first lead — matches the old frontend's own `primaryForGroup` default exactly), per-group "Merge into starred" button with a confirm describing exactly what the merge does (phones/comments/chats absorbed, sold status kept, rest moved to Trash), cross-group checkbox selection + "Delete N selected" as the bulk-delete alternative to merging, Rescan button, loading/error/empty states via the shared `LoadingState`/`ErrorState`/`EmptyState` components. Rebuilt on this app's own HeroUI `Modal`/`Chip`/`RowCheckbox` patterns — not a visual copy of the old frontend's shadcn-based dialog.
- Double-submit guarding: both the group-merge and bulk-delete actions bail immediately if either mutation is already pending (a single shared `busy` flag disables every action button while either is in flight, since a merge and a delete both touch the same underlying lead rows and shouldn't be allowed to race each other), and each destructive action requires a `window.confirm` naming exactly what will happen before firing — matching this feature's established destructive-action pattern (`ManageColumnsDialog`'s delete, `FilteredBulkActionsDialog`'s type-to-confirm delete).
- Wired into `src/app/(protected)/leads/page.tsx` — new Active-tab toolbar icon button ("Duplicate leads", `CodeMerge` icon) opening `DuplicateLeadsDialog`, alongside the existing Manage columns/Manage custom fields/Field visibility/Bulk actions buttons.

**Deliberately deferred (not oversights):**
- Admin-configurable duplicate-detection rules (e.g. choosing which fields count as a match, fuzzy-matching thresholds) — the backend's matching logic is fixed (phone-digits-equal or exact-lowercased-full-name-equal), not configurable server-side, so there's nothing to build a settings UI for.
- A cross-board or workspace-wide duplicate scan — `GET /duplicated-leads` is `boardId`-scoped only, matching the old frontend's own scope (opened per-board, same as this pass).

**Risk note**: none beyond what's already inherent to any destructive action — the merge is a real, atomic-per-request backend operation (see trace above), not a multi-call client sequence, so there's no partial-failure/inconsistent-state risk on this frontend's side to flag. The bulk-delete path is the existing, already-proven soft-delete-into-Trash mechanism used everywhere else in this feature.

**Verification**: `tsc --noEmit`, `eslint .` (full repo), `next build` all clean (zero errors/warnings) — one real fix made along the way (HeroUI's `Modal.Container` doesn't accept a `size="xl"` value, only up to `lg`/`full`/`cover`; caught immediately by `tsc`, switched to `lg`). `next build`'s route table unchanged (`/leads` still prerendering alongside every other protected route — no new route added this slice). `new-operatora-dev` (PM2, port 3033) restarted; `GET /leads` still correctly 307-redirects unauthenticated requests to `/login?next=%2Fleads`, which itself returns a clean 200 login page with no server crash or Next.js error overlay in the HTML — same standing verification bar as every prior phase (still no test account/workspace on `operatora_test` for authenticated click-through of the actual scan/star-primary/merge/bulk-delete flows — same limitation flagged throughout this file). Recommend exercising, once a login exists: a real duplicate cluster (same phone across two leads) going through both the merge path (confirm phones/comments/chats really land on the survivor and sold-status carries over) and the bulk-delete path, plus the Rescan button after either action to confirm the group list updates.

### Leads — SMS templates + compose (done 2026-08-15)

Phase 2c-8. Old frontend reference read for UX only (`components/leads/{LeadSMSTemplatesDialog,SendSMSDialog,ComposeSMSDialog}.tsx`), never modified. Traced two candidate real backends before building anything, and picked one deliberately rather than following the brief's literal wording — see below.

**The critical finding, verified not assumed**: the brief (and the old frontend's `LeadSMSTemplatesDialog.tsx`/`SendSMSDialog.tsx`) pointed at `lead_sms_templates`/`lead_sms_messages` (Prisma models, registered read/write in the generic db-proxy — `table-registry.ts`: `lead_sms_messages` scope `workspace`/`writeRoles: ALL_APP_ROLES`, `lead_sms_templates` scope `workspace`/`writeRoles: MANAGER_ROLES`). Grepped the **entire** backend for any service that reads from or writes to `lead_sms_messages` beyond the existing 2c-4 read-only tab and `chat-tools.service.ts`'s AI-context read — there is none. No controller, no queue worker, no cron, nothing processes a row inserted there; a message written via the db-proxy would sit at `status: "queued"` forever with no gateway ever actually contacted. Writing to it from a "Compose SMS" feature would be a fake send — the operator believes a message went out and it never does. Rejected building on it for that reason (never fabricate delivery), even though its shape (`variables: string[]`, `reason_tag`, `language`) matches the brief's ask almost exactly.

The real, traceable, end-to-end pipeline is **Eskiz** (`eskiz.controller.ts`/`eskiz.service.ts`, `/eskiz/*`) — confirmed by reading `sendSms()`/`sendBulkToLeads()` line-by-line: it actually POSTs to the Eskiz gateway API, tracks delivery status via a real provider webhook (`eskiz_messages.status`), balance-gates real sends against a synced `eskiz_accounts.balance_uzs`, and is exactly what the old frontend's own `ComposeSMSDialog.tsx` (bulk, from the Kanban board — the more recently-written of the two old dialogs) is already wired to, not `lead_sms_templates`. Built the whole slice on this instead. Consequences of that choice, stated explicitly:
- **No `name`/`language`/`variables`/`reason_tag` fields on a template** — `eskiz_templates` only has `content` (Eskiz moderates the literal string) + `status` (`moderation`/`approved`/`rejected`). Dropped the language filter and any rejection-reason association entirely — verified, per the brief's own instruction, that `reason_tag` (where it exists at all, only in the dead `lead_sms_templates`) is a fixed 5-value enum (`no_answer`/`info_request`/`payment_reminder`/`follow_up`/`other`), **not** the rejection-reasons table from 2c-2 (`leadRejectionReasonsApi`) — so that reuse the brief floated was correctly not force-fit.
- **No update/delete endpoint** — a template's approved text is immutable once submitted. "Copy as new" and "edit" both resolve to prefilling the submit box with existing content and submitting fresh (real UX, not a workaround).
- **A real external precondition**: sending requires the workspace to have connected an Eskiz account first (`POST /eskiz/connect`, workspace-owner-only) — that connect flow lives in Settings → Eskiz, which is still an unbuilt placeholder section in this app. Every entry point built this slice handles the resulting 404 with an explicit `EmptyState` pointing at `/settings?section=eskiz` rather than a raw error, since there was nothing else honest to do without also building the Eskiz connect flow (out of scope for this slice).
- **The 2c-4 SMS tab's "feeds 2c-4's SMS tab" checklist line is now literally true, via a real second read**: `LeadSmsTab` merges the original (still-present, unchanged) `lead_sms_messages` log with a real Eskiz-messages read for the one chat (if any) linked to this lead. Eskiz has no `?lead_id=` filter on messages, so this is genuinely two real requests client-side (`GET /eskiz/chats` filtered by `linked_lead_id`, then `GET /eskiz/chats/:id/messages`), not a single fabricated shortcut. After a successful single-lead send, the resulting chat is auto-linked to the lead (`PATCH /eskiz/chats/:id/link-lead`) so this feed picks it up without the operator doing anything extra.

**Built:**
- `src/services/api/eskizSms.ts` (new) — `eskizSmsApi.{getAccount,getGuidance,listTemplates,submitTemplate,resubmitTemplate,syncTemplates,send,sendBulk,listChats,linkChatToLead,listChatMessages,getMessagesForLead}`. Full contract trace and reasoning in its header comment.
- `src/features/leads/types.ts` — `EskizTemplate`/`EskizTemplateStatus`/`EskizAccount`/`EskizGuidance`/`EskizMessage`/`EskizMessageStatus`/`EskizBulkSendResult`, plus `SMS_OVERRIDE_VARIABLES`/`resolveSmsVariables()` — the old frontend's confirmed `{{lead_first_name}}`/`{{lead_last_name}}`/`{{lead_phone}}`/`{{operator_name}}`/`{{company_name}}` token set, kept only as client-side "insert variable" convenience buttons on the single-lead compose override field (Eskiz itself doesn't interpret `{{...}}` syntax — it structurally validates the final text against the approved template and rejects a mismatch, surfaced as a normal error).
- `src/features/leads/schema.ts` — `smsTemplateSubmitSchema`, `composeSmsSchema` (React Hook Form + Zod, matching this feature's established pattern).
- `src/features/leads/hooks/useEskizSms.ts` (new) — query/mutation hooks per endpoint above, `isAccountMissing()` classifying the expected "no account connected" 404 so every consumer renders the same `EmptyState` instead of a generic error. Single-lead send auto-links the resulting chat and invalidates both the lead's SMS feed and the account balance (a send spends real money).
- `src/features/leads/components/SmsTemplatesManager.tsx` (new) — list (status chip, moderation/approved/rejected) + submit + resubmit-if-rejected + manual "refresh statuses" (`POST /eskiz/templates/sync`) + copy-as-new. No manager-role gate — confirmed the real `submitTemplate`/`resubmitTemplate` endpoints carry no such server-side restriction (only `send/bulk` does), so none was added client-side either.
- `src/features/leads/components/ComposeSmsDialog.tsx` (new) — single-lead compose: phone (defaults to the lead's, editable), approved-template picker, optional override text with variable-insert buttons + live preview, cost estimate + balance from `/eskiz/guidance` + `/eskiz/account` (both real, nothing invented), double-submit guarded.
- `src/features/leads/components/BulkComposeSmsDialog.tsx` (new) — bulk compose from the Kanban toolbar: approved-template picker, stage chips (none checked = every column, matching the old frontend), created-date range, real cost/balance gating. Recipient **count preview** reuses 2c-3's existing `leads-list.controller` `bulk/preview` endpoint (`useFilteredBulkPreviewQuery`, same hook `FilteredBulkActionsDialog` uses) scoped to the chosen stages/dates — an accurate estimate, not the authority; the actual send is the single real `POST /eskiz/send/bulk` call (server re-resolves recipients and re-checks balance itself, paces the batch in the background) — **not** a client-orchestrated N-call loop, so the same double-submit guard already used everywhere else in this feature (`isPending` bail) is sufficient; there's no idempotency risk from a client-side retry loop because there is no such loop. Confirmed `POST /leads-list/bulk/execute` (2c-3's own filtered-bulk action set) has no `sms` action type — this really is a separate endpoint, not a force-fit reuse. Gated to `MANAGER_ROLES` in the UI, matching the real server-side 403 the endpoint itself enforces.
- `src/features/leads/components/LeadSmsTab.tsx` (extended, not rebuilt) — now merges both sources (see above) into one time-ordered feed, plus a "Compose SMS" button opening `ComposeSmsDialog`.
- `src/features/leads/components/LeadDetailsModal.tsx` — threads `lead`/`operatorName` (from the session user) into the now-extended `LeadSmsTab`.
- `src/app/(protected)/leads/page.tsx` — two new Active-tab toolbar icon buttons: "SMS templates" (everyone) and "Compose SMS" (bulk, `MANAGER_ROLES`-only, hidden entirely for other roles rather than shown-then-403'd).

**Deliberately deferred / dropped, not oversights:**
- Language filter, `variables: string[]` on a template, rejection-reason association — dropped, no backing field on the real `eskiz_templates` table (see finding above).
- Template edit/delete — no such endpoint exists; immutable-once-submitted is the real backend behavior, not a missing feature.
- The Eskiz account **connect** flow itself (`POST /eskiz/connect`, credentials form) — belongs to Settings → Eskiz, a whole separate still-unbuilt settings section; out of scope for a Leads-feature slice. Every dialog here degrades to an explicit `EmptyState` + link there instead of a broken flow.
- A dedicated "delivery-status log tab" as its own new UI — not built as a *separate* tab; folded into the existing `LeadSmsTab` instead (the brief's own checklist line frames it as feeding that tab, not replacing it), since building a second, near-duplicate tab next to the one already there would be a worse UX for no real benefit.

**Verification**: `tsc --noEmit`, `eslint .` (full repo), `next build` all clean (zero errors/warnings). `new-operatora-dev` (PM2, port 3033) restarted; `GET /leads` still correctly 307-redirects unauthenticated requests to `/login?next=%2Fleads`, which itself returns a clean 200 login page with no server crash or Next.js error overlay in the HTML — same standing verification bar as every prior phase (still no test account/workspace on `operatora_test`, and this slice additionally needs a *connected Eskiz account* with at least one *approved* template to exercise the actual send paths, which is a further precondition beyond just having a login — flagging this explicitly since it's a new category of blocker this slice introduces). Recommend exercising, once both exist: template submit → (real Eskiz moderation delay) → approved → single-lead send → confirm the chat auto-links and the lead's SMS tab shows it with the webhook-updated delivery status; the bulk-compose stage/date scoping against a real board; and the balance-insufficient path actually blocking a send client-side before the server would reject it anyway.

### Leads — import/export (Google Sheets connect: reused, not rebuilt) (done 2026-08-15)

Phase 2c-9. Old frontend reference read for UX/field-shape only (`components/leads/{LeadsExportDialog,LeadsImportDialog,ConnectLeadsToBoardDialog}.tsx`, `lib/{leadsExport,leadsImport}.ts`), never modified. Every endpoint traced directly against the real backend before writing any UI, per this slice's own instruction — one real finding materially changed scope (Google Sheets), documented below rather than silently followed the brief's literal wording.

**Export** — real, already-built endpoint: `GET /leads-list/export` (`leads-controller/leads-list/{leads-list.controller,leads-list.service}.ts`, `leadsToCsv()`/`csvEscape()` in `leads-list-filters.util.ts`). Confirmed by reading the service directly that `selectedColumns` on this endpoint (and on `leads-list`'s own paginated list, and the Phase 2c-3 filtered-bulk endpoints) means exactly what it means everywhere else in this codebase: board **pipeline-stage (column) ids** to scope to (`resolveSelectedColumnIds()`), *not* a picker of which fields appear in the CSV output — the server's CSV header row is fixed (`id/first_name/last_name/phone_number/age/marital_status/academic_status/stage/operator/deadline/created_at/updated_at/custom_fields`, the last one always one raw JSON blob), with no server-side output-field selection at all. The old frontend's own `LeadsExportDialog.tsx`/`BoardColumnFilter` confirms the same reading — its "selectedColumns" is the stage picker too, not a field picker. Built accordingly, not per the brief's literal "selected-columns picker (standard fields + custom fields)" phrasing: a real pipeline-stage scope picker (matches server + old frontend exactly) **plus** a genuine client-side value-add layered on top — a standard-field checkbox picker and a "expand custom fields into columns" toggle that reshapes the real, filtered, server-returned CSV (parsed and re-serialized client-side, never re-derived from a second data fetch) rather than fabricating a server capability that doesn't exist. Respects the board's currently-active filter state as-is (passed in from `LeadsPage`, not re-editable inside the dialog — the brief's literal "respecting the currently-active filters", not the old frontend's larger re-filter-inside-the-dialog UX with quick-presets, which duplicates the whole filter bar for no real gain here).

Files: `src/lib/csv.ts` (new — `parseCsv`/`toCsv`/`csvEscape`, the last mirroring the backend's own `csvEscape()` byte-for-byte so a parse→reshape→re-stringify round-trip never changes quoting), `src/services/api/leadsExportImport.ts` (new — `leadsExportApi`/`leadsImportApi`), `src/features/leads/leadExportCsv.ts` (new — `buildExportCsv()`, the reshape; reuses the Phase 2c-6 custom-field formatters — `formatAmount`/`formatNumberFieldValue`/etc. — so an exported amount/multi-select reads the same as it does in the UI, and never silently drops a custom-field key that doesn't match a known definition, e.g. a deleted field or the id-keyed import quirk noted below — it's preserved in a trailing "Custom fields (raw)" column instead), `src/features/leads/hooks/useLeadExport.ts`, `src/features/leads/components/LeadsExportDialog.tsx`. `services/api/leads.ts`'s previously-private `filtersToLeadListFilters()` was exported (visibility only, no behavior change) so export reuses the exact same filter-serialization convention as every other `leads-list` call rather than a second copy of it.

**Import** — real, already-built endpoint pair: `GET /leads-import/sample` (ready-made `.xlsx` template, seeded with the workspace's real importable custom-field columns) and `POST /leads-import/bulk` (`leads-controller/leads-import/{leads-import.controller,leads-import.service,leads-import.util}.ts`; multipart, `.xlsx`/`.xls`/`.csv`, 5 MB cap, max 2000 rows server-enforced). **Real finding that reshaped this half of the slice**: the endpoint has no client-configurable column-mapping parameter at all — confirmed by reading the controller/service/DTO directly (`bulkImport()` only accepts `file`/`board_id`/`column_id`/`workspace_id`/`operator_id`). It does its own multilingual header auto-detection server-side (`NAME_HEADERS`/`PHONE_HEADERS`/`STAGE_HEADERS`/etc. synonym sets in `leads-import.util.ts`) against whatever text is in row 1 of the uploaded file — and the **old frontend's own** `LeadsImportDialog.tsx` confirms this is genuinely how it has always worked: no mapping UI there either, just a sample-download + file-picker + target-column/operator pickers. So "column-mapping UI" was built as a real, functional client-side step rather than a fabricated server contract: for `.csv` uploads only, the file is parsed in-browser, each header is matched against the exact same synonym sets the backend uses (copied value-for-value into `src/features/leads/importFieldMatch.ts`, not reinvented) plus the workspace's registered custom field names, shown to the user as an editable mapping table, and — only for headers the user explicitly reassigns — the CSV's header row is rewritten to the canonical text the backend's matcher expects before upload (data rows untouched). `.xlsx`/`.xls` uploads skip this preview and go up as-is (the server's identical auto-detection still runs on those; parsing binary Excel client-side would need a new dependency this app doesn't otherwise need, so it wasn't added for a preview-only convenience). File validation matches this app's MIME+ext+size rule: extension whitelist is authoritative (`.csv` MIME reporting is inconsistent across browsers/OS — often empty), backed by a non-empty-MIME check when the browser does report one, plus the same 5 MB cap the server enforces.

**Real backend bug found, not touched (read-only reference)**: `leads-import.service.ts`'s row-insert loop writes custom-field values as `custom_fields[def.id]` (the field definition's UUID), while every other write path in this app's Leads feature — and the read path `CustomFieldValue.tsx` — uses `custom_fields[def.field_name]`. A lead imported via a file with a matched custom-field column will have that value stored under a key nothing else in the product ever reads by, so it won't display anywhere. This is a genuine backend inconsistency, not something this frontend can or should paper over (backend is read-only in scope) — flagging it here so it's not mistaken for this slice's own bug if a real import test surfaces "my custom field didn't show up."

Files: `src/features/leads/importFieldMatch.ts` (new), `src/features/leads/hooks/useLeadImport.ts` (new — invalidates every board-scoped lead view plus `add-lead-columns`, since a successful import can both add leads and create new pipeline columns via unmatched `Stage` values, matching `LeadsBulkImportResult.columnsCreated`), `src/features/leads/components/LeadsImportDialog.tsx` (new).

**Google Sheets connect — go, but reused rather than rebuilt (not a new OAuth flow).** Per this slice's own instruction, confirmed live OAuth credentials exist server-side *before* writing any UI: `GOOGLE_CLIENT_ID`/`GOOGLE_CLIENT_SECRET`/`GOOGLE_REDIRECT_URI` are real, non-placeholder values in `app/backend/.env`, and `google-sheets.service.ts`'s `oauth-url`/`oauth-callback` handlers are fully implemented (not stubs). That's a genuine "go" — but per PROGRESS.md's existing Settings epic entry ("CRM Integrations, Google Sheets, Google Calendar"), this exact workspace-level Google Sheets integration — **including real board-scoped import (with per-board/per-column config, field mapping, ignored columns, and an existing-rows import-all/skip-existing gate) and export (create/link a spreadsheet, one-time full-board sync, auto-append new leads)** — was already fully built in this codebase (`src/features/google-sheets/`, `src/services/api/googleSheets.ts`, `src/app/google-sheets-callback/page.tsx`, wired at `/settings?section=google-sheets`). Building a second, parallel OAuth-connect UI inside the Leads feature would be exactly the kind of duplicate data layer this slice's own instructions said to check for and avoid ("check `src/features/settings/*` for reusable OAuth/connection data layer before building a parallel one"). So this slice adds a single small, honest entry point instead of a rebuild: a link from `LeadsExportDialog` ("Need an ongoing sync instead of a one-time file? Connect Google Sheets in Settings →") pointing at the existing real panel. No new OAuth code, no new `features/leads-google-sheets/` folder, no second `google-sheets.service.ts` client. Checklist line marked `[x]` (not `[~]`) since the real capability exists and is reachable from Leads — "dropped" would misdescribe a deliberate reuse decision as a gap.

**Deliberately deferred / out of scope, not oversights**:
- A pre-selected-board variant of the Settings Google Sheets panel (opening it from Leads with the current board already chosen, instead of the user re-picking it there) — would touch `GoogleSheetsSettingsPanel.tsx`'s internals (currently self-contained, no props) for a minor convenience; flagged as a small, low-risk follow-up, not done here to avoid scope creep into a different feature's settings component during an import/export-focused pass.
- `.xlsx`/`.xls` column-mapping preview — see above; direct upload only, relying on the server's own (identical) auto-detection.
- A true two-way Google Sheets "connect" *from the Leads feature specifically* (as opposed to the real one already reachable via the link above) — not built, per the reuse decision above.

**Verification**: `tsc --noEmit`, `eslint .` (full repo), `next build` all clean (zero errors/warnings) — two real lint fixes made along the way: `react/no-unescaped-entities` (apostrophes/quotes in JSX text, escaped) and confirming HeroUI's `Modal.Container`/`Select` don't actually support `size="xl"`/`size="sm"` (checked the installed `@heroui/styles` source directly rather than guessing — `Modal` sizes are `xs/sm/md/lg/full/cover` only, `Select` has no `size` variant at all) before they'd have been silent no-ops or type errors. `next build`'s route table unchanged (no new routes — both dialogs mount from the existing `/leads` page). `new-operatora-dev` (PM2, port 3033) restarted; `GET /leads` still 307-redirects unauthenticated requests to `/login?next=%2Fleads`, itself a clean 200 with no server crash or Next.js error overlay in the HTML — same standing check as every prior phase. **Not verified** (same standing limitation as every phase since Phase 2a): interactive click-through of an authenticated session — still no test account/workspace on `operatora_test`. Recommend exercising, once available: the export reshape against a board with real custom-field data (both the id-keyed-import-bug leftover case and the normal field_name-keyed case), a real `.csv` upload with at least one unrecognized header to exercise the remap-and-rewrite path end-to-end, an `.xlsx` upload via the sample template, and the Google Sheets settings link.

### Leads — board automations (done 2026-08-15)

Phase 2c-10. Old frontend reference read for UX/rule-shape only (`components/leads/{LeadAutomationsDialog,LeadAutomationsPanel}.tsx`), never modified.

**Traced first, per this slice's own instruction, and confirmed this is reuse, not new backend surface.** `features/lead-automations/` (built in the Settings epic, "Lead Automations, Task Management, Billing & Usage" writeup above) already covers the *entire* backend contract: the old frontend's `LeadAutomationsPanel.tsx` is one component with `boardId`/`variant`/`kind` props — the Settings section renders it `boardId={undefined} variant="settings" kind="automation"` (workspace-wide), and the old frontend's `LeadAutomationsDialog.tsx` (never built in this app before this slice) is a one-file `Dialog` wrapper rendering the exact same component `boardId={board} variant="dialog"`. Both read/write the same `automation_rules` table via the same `POST /db/:table/query` compat proxy (`db-proxy/table-registry.ts`: `{ table: 'automation_rules', scope: 'workspace', writeRoles: MANAGER_ROLES }`) — a `board_id` column already exists on the row and was already carried end-to-end in this app's `types.ts`/`AutomationRuleRow`, just never read from or written with a real value (`buildRulePayload()` hardcoded `board_id: null`). So this slice is genuinely **"surface the existing settings-section automations UI/data with a board-scoped entry point from `/leads`"**, exactly the smaller scope the brief called out as the likely outcome — no second automations system, no parallel data layer, no new `services/api/*` file.

**What changed to make it board-aware** (all in the already-existing `features/lead-automations/` — nothing new stood up):
- `types.ts` — `buildRulePayload(form, workspaceId, boardId?)` now stamps the real `boardId` (previously always `null`) onto both `board_id` and `trigger_config.board_id` when creating/editing from the board dialog; new `ruleMatchesBoard(row, boardId)` — a rule with no `board_id` (workspace-wide) matches every board, a rule scoped to a specific board only matches that one — client-side filter, mirroring the old frontend's own `.filter()` logic exactly (the `automation_rules` table has no server-side board filter; the compat proxy only scopes by workspace via the JWT).
- `components/LeadAutomationsPanel.tsx` — now takes optional `boardId`/`variant="settings" | "dialog"` props (matching the old frontend's own prop names). `boardId` set: rules list filtered via `ruleMatchesBoard`, and the column picker (both trigger `lead_column_change`/action `move_to_column` selects, and rule descriptions) is narrowed to that board's own columns (via the existing workspace-wide `useAllLeadsBoardColumnsQuery`, filtered client-side by `board_id` — no new query). `variant="dialog"` skips `SettingsSectionShell`'s card chrome (the wrapping `Modal` in the new dialog component already supplies title/description) and renders an inline "New rule" button above the list instead of a header action — implemented as a plain `renderShell()` function (not a component defined during render) to avoid an eslint `react-hooks/static-components` violation the first draft tripped.
- `app/(protected)/settings/page.tsx` — the registry's `"lead-automations": LeadAutomationsPanel` entry became `() => <LeadAutomationsPanel />` (both props optional, but the registry's `Record<string, () => ReactElement>` type wants exactly zero-arg function values, not "all params optional").

**New file**: `features/leads/components/LeadBoardAutomationsDialog.tsx` — thin `Modal` wrapper (HeroUI `Modal.Container size="lg"`, matching `ManageColumnsDialog`'s dialog-sizing precedent) around `<LeadAutomationsPanel boardId={boardId} variant="dialog" />`, 1:1 with the old frontend's own thin-wrapper `LeadAutomationsDialog.tsx`. Entry point wired into `app/(protected)/leads/page.tsx`'s Active-tab toolbar (`GearPlay` icon — already used inside the rule-list rows, reused for visual consistency rather than picking a new icon) alongside the other board-management icon buttons (Manage columns, Manage fields, etc.); no role gate on opening it (same as the existing Settings panel — mutations already 403 client-side-surfaced via `ApiError.isForbidden` → `mutationErrorMessage()` for non-managers, per `writeRoles: MANAGER_ROLES`, so gating the open button too would just be a second, redundant check).

**Deliberately deferred / not in scope for this slice** (unchanged from the Settings-epic build — nothing new deferred here beyond what that pass already noted): Notification Rules' distinct richer feature (personal Telegram account-link flow, `/notification-rules/*`) — separate Settings section, not automations.

**Verification**: `tsc --noEmit`, `eslint .` (full repo), `next build` all clean (zero errors/warnings). `next build`'s route table unchanged (no new route — dialog mounts from the existing `/leads` and `/settings` pages). `new-operatora-dev` (PM2, port 3033) restarted; `GET /leads` and `GET /settings` both still 307-redirect unauthenticated requests to their respective `/login?next=...`, themselves clean 200s with no server crash or Next.js error overlay — same standing check as every prior phase, and dev-server logs show no new errors post-restart. **Not verified** (same standing limitation as every phase since Phase 2a): interactive click-through of an authenticated session (create a board-scoped rule, confirm it doesn't appear when a different board's dialog is opened, confirm a workspace-wide rule created from Settings still appears in every board's dialog) — still no test account/workspace on `operatora_test`.

### Leads — AI features (done 2026-08-15)

Phase 2c-11. Old frontend reference read for UX/field reference only (`components/leads/{LeadAIAssist,AiLeadDistributionDialog}.tsx`, `lib/leadSignalsDisplay.ts`), never modified. All three in-scope items traced to real, non-fabricated backend surfaces — nothing dropped for lack of a traceable endpoint.

**1. AI Assist panel — real, `/fn/lead-ai-assist` (not SSE).** Traced exactly like Conversations' AI assistant (`useConversationAssistant.ts` precedent): `FunctionsController.dispatch('lead-ai-assist')` -> `FunctionsHandlersService.aiChatHandler('lead-assist', body, user)` -> the same unified `AiChatService.chat()` backing every other `/fn/*` AI chat mode, plain JSON request/response. `aiChatHandler()` special-cases `mode === 'lead-assist'` server-side to wrap the plain `{reply}` into `{response: {talk_track, follow_up_question, sms_follow_up}, thread_id, ...}` — confirmed directly in `functions.handlers.ts`; `follow_up_question`/`sms_follow_up` are always empty strings today (never actually implemented server-side beyond the envelope shape), so only `talk_track` is rendered. Insights/next-best-action are backed by the real `GET /signals/lead/:leadId` (`SignalsWorkerController`), the actual `lead_signals` row a background worker (`signals-worker/signal-extractor.service.ts`) computes from call/message history — not fabricated. New: `services/api/leadAiAssist.ts` (`leadAiAssistApi`, `leadSignalsApi`), `features/leads/leadSignals.ts` (display helpers ported from the old frontend's `lib/leadSignalsDisplay.ts` — score bands, channel/updated-at formatting, suggested-prompt list), `features/leads/hooks/{useLeadSignalsQuery,useLeadAiAssistChat}.ts` (the latter mirrors `useConversationAssistant`'s local-non-persisted-chat-state reasoning exactly — synchronous request/response, no thread GET to resume from, resets when the lead changes), `features/leads/components/LeadAiAssistTab.tsx` (new "AI Assist" tab in `LeadDetailsModal`, wired only-while-active per the modal's established per-tab fetch-on-demand rule). Copy-draft-to-clipboard implemented (mirrors `MessageBubble.tsx`'s `CopyButton` pattern). Composer guards empty input + double-submit. UI is a clean rebuild (plain bordered cards matching `LeadStatsTab`'s existing convention), not a copy of the old purple-gradient Figma layout.

**2. AI lead distribution — real, deterministic backend, not a stub.** `AiLeadDistributionController` (`/leads-ai-distribution`) + `.Service`: `POST /:boardId/plan` parses a natural-language instruction (OpenAI) into a structured `DistributionPlan` and runs a dry-run `preview()` (greedy assignment across unassigned leads, `even` or `backlog_aware` mode — real logic, read directly in `ai-lead-distribution.service.ts`, not guessed); `POST /:boardId/apply` actually assigns; `GET`/`PATCH /:boardId/settings` toggle a recurring background sweep. Every endpoint requires `isWorkspaceOwnerOrAdmin` server-side. New: `services/api/aiLeadDistribution.ts`, `features/leads/components/AiLeadDistributionDialog.tsx` (chat-style plan → preview → confirm flow + recurring-toggle switch, clean rebuild of the old dialog's layout using this app's Modal/Switch/Input conventions, not its styling). Entry point: new `MagicWand` icon button in the Active-tab toolbar (`app/(protected)/leads/page.tsx`), gated by `hasAnyRole(roles, MANAGER_ROLES)` — an approximation of the backend's workspace-role-based owner/admin check (this app doesn't fetch `workspace_users` role client-side yet, same standing gap noted in Phase 2b), documented inline as UX-only; the backend remains the real authorization boundary and a 403 surfaces via the dialog's own error banner, not silently.

**3. Sort-by-AI-score toggle — real score field, page-local sort (documented limitation, not silently wrong).** Confirmed `buying_intent_score` is a real, queryable column (`lead_signals` table) but the board's column-leads endpoint (`GET /lead-board/:boardId/column/:columnId`) has no server-side sort param and doesn't join `lead_signals` into its response (read `lead-board.controller.ts`'s `buildFilters()`/`lead-board.service.ts`'s query directly to confirm, not assumed) — so a true cross-page global sort isn't possible without a new backend endpoint, out of scope here. Implemented as a client-side toggle (`BarsDescendingAlignLeftArrowDown` icon in the toolbar, board view only) that re-sorts each column's *currently loaded page* by score (desc, unscored leads last). New: `features/leads/hooks/useColumnLeadSignalsQuery.ts` — one batched `lead_signals` read per column page (`lead_id IN (...)`) via the existing `db-proxy` compat endpoint (`table-registry.ts` already lists `{table: 'lead_signals', scope: 'workspace', writeRoles: ADMIN_ROLES}` with no `readRoles` restriction — its own comment says exactly "frontend read-only, for showing intent on lead cards", i.e. built for this), fired only while the toggle is on so leaving it off costs zero extra requests (flood-prevention: 1 request per column per page, not 1 per card). Since the same batched fetch is already paid for, added the score badge to `LeadCard` too (only rendered while sorting is active) rather than wiring the sort with no visible indicator — the "cheap to add, so add it" case the brief called out.

**Explicitly and deliberately left out of this slice, per the brief — do not build without a separate scoping conversation first**: real-time call guidance (live transcription + Gemini suggestions streamed during an active call). Confirmed the reason it's out of scope is real, not just deferred-by-default: it depends on live call-recording/streaming infrastructure (`real-time-guidance` function exists in the registry as `'inline'` but wraps a *different* concern — Asterisk/live-call audio pipeline — that hasn't been traced at all in this pass) whose contract needs its own investigation and a scoping conversation with the user before any UI or data-layer work starts. Nothing was stubbed for it — no dead tab, no disabled button referencing it.

**Files touched**: new — `src/services/api/{leadAiAssist,aiLeadDistribution}.ts`, `src/features/leads/leadSignals.ts`, `src/features/leads/hooks/{useLeadSignalsQuery,useLeadAiAssistChat,useColumnLeadSignalsQuery}.ts`, `src/features/leads/components/{LeadAiAssistTab,AiLeadDistributionDialog}.tsx`. Edited — `src/features/leads/components/LeadDetailsModal.tsx` (new "AI Assist" tab), `src/features/leads/components/{LeadCard,KanbanColumn,KanbanBoard}.tsx` (optional `aiScore`/`sortByAiScore` prop threading), `src/app/(protected)/leads/page.tsx` (sort toggle + AI distribution button/dialog + gating).

**Verification**: `tsc --noEmit`, `eslint .` (full repo), `next build` all clean (zero errors/warnings) — one real fix along the way (HeroUI's bare `Input` component takes `disabled`, not `isDisabled`, unlike the `TextField`-wrapped `Input` used elsewhere in this codebase; caught by `tsc`, not guessed). `next build`'s route table unchanged (no new routes — both new panels mount from the existing `/leads` page/modal). `new-operatora-dev` (PM2, port 3033) restarted; `GET /leads` still 307-redirects unauthenticated requests to `/login?next=%2Fleads`, itself a clean 200 with no server crash or Next.js error overlay, and PM2 logs show no new errors post-restart — same standing check as every prior phase. **Not verified** (same standing limitation as every phase since Phase 2a): interactive click-through of an authenticated session — still no test account/workspace on `operatora_test`. Recommend exercising, once available: an actual AI Assist reply round-trip, the distribution plan→preview→apply flow against real unassigned leads, and the sort toggle against a column with a mix of scored/unscored leads.

### Leads — cross-feature integration points (done 2026-08-15)

Phase 2c-12, the final slice of the Leads full-parity plan. Scope: small entry-point dialogs that live in *other* feature folders (conversations, AI chat, dashboard, messages) but read/write leads. Old frontend reference read for UX/flow only (`components/{conversations/LinkMeetRecordingLeadDialog,conversations/LinkLeadDialog,conversations/CreateLeadFromMeetingDialog,dashboard/DashboardAttachLeadDialog,ai-chat/MultipleLeadsCard,ai-chat/EmbeddedLeadCard,messages/CreateLeadFromChatDialog,messages/AutoLeadCreateDialog,messages/LinkChatToLeadDialog}.tsx`), never modified. Per this slice's own instructions, each of the 4 items was checked against whether its *host* feature already exists in this rebuild before writing anything — two do, two don't.

**1. Link lead to conversation — built.** Host exists (`/conversations`'s `ConversationDetailPanel`). Reused Phase 2c-4's already-traced mechanism (`conversations.entities` jsonb, written via the cast-safe `values` proxy path — see that slice's own writeup for the `cs`/`contains` landmine this avoids) and its exact `leadConversationLinksApi.link(conversationId, leadId)`/`.unlink(conversationId)` primitives verbatim — no new write path invented. New: `getLinkedLeadId()` exported from `services/api/leadConversationLinks.ts` (narrows a conversation's own already-fetched `entities` field, no extra request), `features/conversations/hooks/useConversationLeadLink.ts` (mirrors 2c-4's `useLeadConversationLinkMutations`, just invalidates the conversation's own detail query instead of a lead's linked-conversations list), `features/conversations/components/LinkLeadDialog.tsx` (search/select/unlink dialog). The lead picker uses the real `lead-search` endpoint (`leadSearchApi.search`/`byIds`, Phase 2c-6's `relation`-field lookup) rather than duplicating the old frontend's own ad-hoc Supabase `ilike` query or 2c-4's bounded 300-conversation scan (wrong direction — that one searches conversations, this needs to search leads) — a genuinely better real search than the old dialog had (server-side name/phone match, no row cap). Wired into `ConversationDetailPanel`'s header chip row: a "Link lead" button when unlinked, a clickable "Lead: name" chip when linked (resolves the display name via the same `lead-search/by-ids` call). `app/(protected)/conversations/page.tsx`'s `ConversationDetailPanel` mount gained a `key={selected.id}` so any open dialog/local state cleanly resets when the operator selects a different conversation row (it previously had none — a real, if minor, latent bug this slice's own dialog state made worth fixing).

**2. Create lead from meeting / link meeting recording to lead — skipped, no host.** Confirmed a real backend module exists (`meet-recordings.controller.ts`/`.service.ts`, and the old frontend's `MeetRecordingsSection.tsx` renders it as a sub-section of its Conversations page) — so this is a real, traceable backend capability, not a fabricated one. But this rebuild's `/conversations` feature (`src/features/conversations/`) has no meet-recordings list/section at all yet — only `conversation`/`play-back`/`fn/conversation-ai-assistant` were ever wired (Phase 2b). Building `LinkMeetRecordingLeadDialog`'s equivalent, or `CreateLeadFromMeetingDialog`'s equivalent, would require first building the meet-recordings list UI itself — a real new feature surface, not a small entry-point dialog into an existing one. Per this slice's own scope instruction ("if a host surface doesn't exist, skip that sub-item rather than scope-creeping into building it"), both sub-items are skipped. Flagged here, not silently dropped, so a future "meet recordings" slice has a clear starting trace (`meet-recordings.controller.ts`, `hooks/useMeetRecordings.ts` + `lib/meetRecordingsApi.ts` in the old frontend).

**3. Create lead from chat / auto-lead-create from chat — skipped, no host.** All three old-frontend files for this item (`CreateLeadFromChatDialog`, `AutoLeadCreateDialog`, `LinkChatToLeadDialog`) live under `components/messages/` — the Telegram/Instagram unified chat inbox (old frontend's `pages/Messages.tsx`). Confirmed this rebuild has no Messages feature at all: `constants/sitemap.ts` lists a `messages` nav item, but `src/app/(protected)/messages/` doesn't exist — it 404s today, exactly as documented in Phase 2b's nav-shell writeup ("nav links to not-yet-built routes ... 404 for now, on purpose"). The AI Chat feature (`src/features/chat/`, `/dashboard`) is a genuinely different feature (AI assistant threads, not a Telegram/Instagram inbox) and was checked too: its existing `lead`/`leads` chat-card kinds (`ChatCards.tsx`) are read-only displays of leads the AI already resolved by id, with no "create from this chat" affordance to extend, and no `/ai-chat/*` endpoint was found for creating a lead from a thread specifically. Skipped per the same host-doesn't-exist rule as item 2 — building a Messages inbox as a side effect of this Leads slice would be exactly the scope creep the brief warned against.

**4. Dashboard attach-lead — built, but re-scoped to what the old dialog actually does.** Traced `DashboardAttachLeadDialog`'s actual call site (`Dashboard.tsx`'s `leadPickerOpen`/`leadRefChip`/`buildMessageWithContext()`) before building anything: it is **not** a lead-creation dialog and does not write anything — it's a lead *picker* that attaches a removable "context chip" to the AI chat composer, and on send the old frontend prepends a plain-text `[Lead context: name (id: id)]` line to the message before it reaches the AI (confirmed directly — `/ai-chat/v2` takes one message string, there's no structured "context" parameter to trace). Host exists and is exactly `/dashboard`'s `ChatComposer.tsx`. Built accordingly, with the same text-prepend convention, not a fabricated backend "attach" endpoint: `features/chat/components/AttachLeadDialog.tsx` (new — search dialog, reuses `leadSearchApi` again rather than the old dialog's own ad-hoc Supabase query), `ChatComposer.tsx` extended with a `Target`-icon "Attach a lead" button, a removable chip shown above the input while a lead is attached, and `handleSubmit()` prepending the context line and clearing the chip after send (matches the old app's own clear-on-send behavior, confirmed by reading every `setLeadRefChip(null)` call site in `Dashboard.tsx`). No changes needed to `useChatController`/`services/api/chat.ts` — the composed string is just what `onSend(query)` already accepted.

**Files touched**: new — `src/features/conversations/hooks/useConversationLeadLink.ts`, `src/features/conversations/components/LinkLeadDialog.tsx`, `src/features/chat/components/AttachLeadDialog.tsx`. Edited — `src/services/api/leadConversationLinks.ts` (`getLinkedLeadId()` export, no behavior change), `src/features/conversations/components/ConversationDetailPanel.tsx` (linked-lead chip + dialog wiring), `src/app/(protected)/conversations/page.tsx` (`key={selected.id}`), `src/features/chat/components/ChatComposer.tsx` (attach-lead button, chip, context-prepend on send).

**Verification**: `tsc --noEmit`, `eslint .` (full repo), `next build` all clean (zero errors/warnings) — two real fixes made along the way: HeroUI's `Modal` compound has no `Modal.Description` sub-component (only `Modal.Heading` inside `Modal.Header`, confirmed directly in the installed `@heroui/react` `.d.ts`, not guessed) — both new dialogs' description text moved to a plain `<p>`; and `Button` has no `color` prop (only `variant`, including a real `"danger-soft"` value) — the conversation-unlink button's `variant="secondary" color="danger"` was replaced with `variant="danger-soft"`. Also caught and fixed a `react-hooks/set-state-in-effect` violation in `LinkLeadDialog.tsx`'s first draft (an effect that only cleared a redundant error state already cleared at the top of every mutation handler) — removed rather than worked around, since the effect wasn't accomplishing anything the handlers didn't already do. `next build`'s route table unchanged (no new routes — both dialogs mount from existing `/conversations` and `/dashboard` pages). `new-operatora-dev` (PM2, port 3033) restarted; `GET /dashboard`, `GET /conversations`, and `GET /leads` all still correctly 307-redirect unauthenticated requests to their respective `/login?next=...`, themselves clean 200s with no server crash or Next.js error overlay in the HTML, and PM2 logs show no new errors post-restart — same standing verification bar as every prior phase. **Not verified** (same standing limitation as every phase since Phase 2a): interactive click-through of an authenticated session — still no test account/workspace on `operatora_test`. Recommend exercising, once available: linking/unlinking a conversation to a lead and confirming it shows up on that lead's Phase 2c-4 Conversations tab (and vice versa), and attaching a lead in the AI chat composer, sending a message, and confirming the assistant's reply reflects the injected lead context.

## Phase 2c — Leads full parity (planned 2026-08-15)

Old frontend's `/leads` is ~26,000 lines / 55 components — the Phase 2b MVP covers board+columns+DnD+realtime+filters+a scoped details view only. Full inventory taken from `/www/wwwroot/dev.operatora/app/src/{pages/Leads.tsx,components/leads/*}` (55 files) plus cross-feature integration points in conversations/dashboard/ai-chat/messages. Breaking the remainder into ordered slices, same contract-tracing rigor as every phase above (backend controllers/services read directly, never guessed from the old frontend alone). Each slice below becomes its own dated `### Leads — <slice>` entry when done, same pattern as the Settings epic.

- [x] **2c-1 Tabs & List view** — Active/Sold/Rejected/Archived/Trash tabs (admin-configurable order/visibility deferred to a later governance slice — build fixed order first), List (table) view toggle alongside the existing Kanban view, reusing `TanStack Table` per the `ConversationsTable` precedent. (done 2026-08-15)
- [x] **2c-2 Mark Sold / Mark Rejected / Create Lead dialogs** — including rejection-reason gating (reason must be configured before a lead can be rejected) and a minimal reason-list manager. (done 2026-08-15)
- [x] **2c-3 Bulk actions** — row multi-select (assign/move/archive/delete) and filtered bulk actions (act on every lead matching current filters, with board-move stage-mapping) — both real backend endpoints already partly traced via `right-board-controller`. (done 2026-08-15)
- [x] **2c-4 Lead details panel expansion** — Comments (with attachments), Timeline/activity feed, Tags (picker + chips), additional phone numbers, linked Conversations tab, SMS log tab, Tasks tab, Stats tab. `LeadDetailsDialog.tsx` (3,442 lines) is the old-frontend reference; build as separate tab components, not one file. (done 2026-08-15 — see "Leads — details panel expansion" writeup above; comment editing and SMS compose real-but-deferred, Conversations tab has a documented bounded-scan limitation, no tab was fabricated without a traced backend source)
- [x] **2c-5 Column/board management** — create/edit/delete/reorder columns, WIP limits, required-field/deadline gating dialogs, board create/share/multi-board switcher polish. (done 2026-08-15 — see "Leads — column/board management" writeup below; guided `RequireFieldDialog` only reaches Mark Sold/Mark Rejected, not raw Kanban column-move, due to a traced backend bug — documented in detail below, not silently worked around)
- [x] **2c-6 Custom fields engine + field visibility manager** — field type system (text/number/amount/select/multi-select/status/date/checkbox/email/phone/url/relation/rollup/formula/place/computed), per-stage required-field config, per-user card/detail field visibility toggles. (done 2026-08-15 — see "Leads — custom fields engine + field visibility" writeup below; all 19 types real, none dropped)
- [x] **2c-7 Duplicate detection** — phone/name-based scan, merge-with-primary-selection flow. (done 2026-08-15 — see "Leads — duplicate detection" writeup above; real atomic server-side merge endpoint found and used, no client-orchestrated multi-call sequence)
- [x] **2c-8 SMS** — templates manager (CRUD, variables, language/reason tagging) + per-lead compose + bulk compose + delivery-status log tab (feeds 2c-4's SMS tab). (done 2026-08-15 — see "Leads — SMS templates + compose" writeup above; built on the real Eskiz gateway pipeline instead of the brief's assumed `lead_sms_templates`/`lead_sms_messages`, which traced to a dead end with no delivery pipeline at all — variables/language/reason-tag dropped accordingly, not fabricated)
- [x] **2c-9 Import/Export** — CSV import/export respecting active filters + selected columns; Google Sheets two-way connect (OAuth) — confirm live OAuth app credentials exist before starting, else scope down to CSV-only first. (done 2026-08-15 — see "Leads — import/export (Google Sheets connect: reused, not rebuilt)" writeup above; OAuth credentials confirmed real/live, but the workspace-level Google Sheets integration was already fully built in the Settings epic — reused via a link, not duplicated)
- [x] **2c-10 Lead-board automations dialog** — trigger×condition×action rule builder scoped to a lead board (distinct from the already-built `features/lead-automations/` *settings*-section variant — do not duplicate that data layer, extend it if the contract allows). (done 2026-08-15 — see "Leads — board automations" writeup above; confirmed same underlying `automation_rules` table/engine as the settings-section variant, so this slice extended that data layer with real `board_id` scoping instead of building a parallel one)
- [x] **2c-11 AI features** — AI Assist panel (insights/next-best-action/ask-AI chat), AI lead distribution, sort-by-AI-score toggle. **Real-time call guidance (live transcription + Gemini suggestions) explicitly needs a scoping conversation before starting** — depends on live call-recording infra whose contract hasn't been traced yet; do not assume parity here. (done 2026-08-15 — see "Leads — AI features" writeup above; all three items real-backend-backed, real-time call guidance intentionally excluded per the brief, not stubbed)
- [x] **2c-12 Cross-feature integration entry points** — link-lead-to-conversation, create-lead-from-meeting/-chat, link-meeting-recording-to-lead, dashboard attach-lead — small dialogs that live in *other* features' folders but write to leads; sequence after 2c-4 since most reuse its data layer. (done 2026-08-15 — see "Leads — cross-feature integration points" writeup above; 2 of 4 built (link-to-conversation, dashboard attach-lead-as-context), 2 skipped because their host features don't exist yet in this rebuild at all (meet recordings section, Messages/Telegram-Instagram inbox) — not built as a side effect, per this slice's own scope rule)

**Phase 2c is now complete — all 12 slices done.** `/leads` has reached real feature parity with the old app's ~26,000-line/55-component implementation across board+columns+DnD+realtime, filters, tabs/list view, all core dialogs, bulk actions, a fully tabbed details panel (comments/timeline/tags/conversations/SMS/tasks/stats/AI-assist), column/board management with sharing, a 19-type custom-fields engine with per-stage requirements and field visibility, duplicate detection with real server-side merge, SMS (rebuilt on the real Eskiz pipeline instead of the dead `lead_sms_templates` path the brief assumed), import/export (CSV plus a reused Google Sheets connect), board-scoped automations, AI assist/distribution/sort, and now the small cross-feature entry points that reuse this data layer from Conversations and AI Chat. What's genuinely still missing or intentionally out of scope, for a future session that wants a single pointer rather than re-deriving it by re-reading all 12 writeups:
- **Real-time call guidance** (live transcription + Gemini suggestions during an active call) — 2c-11 explicitly excluded this; it depends on live call-recording/streaming infrastructure that has never been traced in this rebuild and needs its own scoping conversation before any work starts.
- **The raw Kanban column-move / row-selection bulk move / filtered-bulk move paths' error messaging is honest but not field-specific** — 2c-5 found and documented a real backend bug (`right-board-controller.service.ts`'s `changeColumn()` rewraps a clean `BadRequestException` into a generic 500) that this frontend cannot work around without a backend change; only Mark Sold/Mark Rejected get the guided required-field dialog.
- **Meet recordings** (list, link-to-lead, create-lead-from-meeting) and a **Messages inbox** (Telegram/Instagram unified chat, create/auto-lead-create/link-to-lead) — both are real, traced backend capabilities with zero frontend surface in this rebuild yet; 2c-12 deliberately did not build either as a side effect of a Leads-focused slice.
- Smaller, individually-noted deferrals across slices (comment editing, built-in-field workspace-wide hidden/required config distinct from per-stage custom-field requirements, `.xlsx`/`.xls` import column-mapping preview, board rename UI trigger, a pre-selected-board variant of the Settings Google Sheets panel, `POST /columns/order` bulk reorder) — see each slice's own writeup above for specifics; none block core Leads usage.

Execution note: each slice gets its own backend trace (read the real controller/service, not the old frontend's assumptions) before implementation, per [[frontend_rules]]. Update this file with a dated writeup (files touched, what's deferred within the slice, verification run) as each lands — do not batch multiple slices into one entry.

### Tasks page (`/tasks`) (done 2026-08-17)

Standalone Tasks app page — the real `operator_tasks` list/board operators work from day to day, distinct from Settings > Tasks (`features/tasks-settings/`, the automation-*rules* config for when the app should prompt/require/auto-create a task). PROGRESS.md's Open Questions section had already confirmed `/tasks` maps to a real `TasksController`/`operator_tasks` table (raw Supabase client, not in Prisma schema) — re-verified directly against `/www/wwwroot/dev.operatora/app/backend/src/tasks/{tasks.controller,tasks.service}.ts` rather than trusting that note at face value. Old frontend reference read for UX only (`pages/Tasks.tsx`, `components/tasks/{TaskRow,CreateTaskDialog,TaskDetailsDialog}.tsx`, `lib/{operatorTasksApi,taskPermissions}.ts`), never modified.

**Real contract, confirmed directly in the controller/service (not assumed from the old frontend)**: `GET /tasks?scope=mine|team` (workspace derived purely from the JWT, no `workspace_id` param anywhere on this controller), `GET /tasks/assignees`, `POST /tasks`, `PATCH /tasks/:id/complete` (server-mandatory `closure_comment`). **No edit/cancel/delete endpoint exists** — confirmed by reading the whole controller, not assumed; the create/complete/list surface above is the entire real contract, and the UI doesn't offer anything beyond it.

Files: `src/features/tasks/{types,permissions}.ts`, `src/services/api/tasks.ts`, `src/features/tasks/hooks/{useTasksQuery,useTaskMutations}.ts`, `src/features/tasks/components/{TaskRow,TaskStatCard,CreateTaskDialog,TaskDetailsDialog,CompleteTaskDialog}.tsx`, `src/app/(protected)/tasks/page.tsx` (new route — `constants/sitemap.ts` already had the `/tasks` nav entry pointing at this not-yet-built page, wired now).

**Reused rather than duplicated**: `features/tasks-settings/hooks/useTaskModuleSettingsQuery` gates the whole page behind the workspace's `enabled` toggle (module-disabled empty state with a "Open settings" link for managers, matching the old frontend's own gate exactly — `isTaskModuleEnabled(settings)`); `features/leads/hooks/useLeadSearch`'s `useLeadSearchQuery` (Phase 2c-6's `relation`-field lookup) powers the create dialog's lead picker instead of a second lead-search implementation; `features/leads/types`'s `formatLeadName` for the row/detail display. `canManageTeamTasks` (`features/tasks/permissions.ts`) is a 1:1 port of the old frontend's `lib/taskPermissions.ts`, but — unlike the earlier approximation Phase 2c-11's AI Lead Distribution entry point had to settle for (`hasAnyRole(roles, MANAGER_ROLES)` only, no workspace-role data available yet) — this reproduces the backend's real two-part check exactly, since `useMyWorkspacePermissionsQuery` (Settings' Roles & Permissions section, `GET /workspace-rbac/me`) is now a real, already-built data source for `workspace_role`.

**Explicitly deferred, with reasoning (not oversights)**:
- **Deep-linking a task's lead into `/leads`'s `LeadDetailsModal`** — that modal requires a `boardId`/`columns` prop this page has no way to resolve for an arbitrary lead id without first building a new board-resolving lookup (a lead's board isn't known from the task row). Rather than link to something that would silently 404 or need guessed board context, the linked lead renders as plain, non-interactive text (name + phone, exactly what `GET /tasks` already returns inline).
- **`applyColumnChangeTaskRules`/`applyLeadCreatedTaskRules`-equivalent wiring into the Leads feature** (old frontend's `lib/applyTaskRules.ts` — auto-create/prompt a task when a lead's column changes/is created/assigned, driven by the Settings > Tasks rules this app already has real CRUD for). Confirmed via `grep` that nothing in this app's `features/leads/` currently calls anything task-rule-related — building this now would be new Leads-feature integration work triggered from lead lifecycle events, not part of a standalone task-list page. Flagged here as a real, traceable follow-up (the rules config and the `POST /tasks` create endpoint both already exist and are wired; only the "call it from Leads' column-change/create/assign handlers" glue is missing), not silently dropped.
- **`TaskColumnChangePrompt`-equivalent guided dialog** — depends on the deferred item above (it's the UI for the interactive prompt/require rule outcome); nothing to build without the trigger wiring.

**Verification**: `tsc --noEmit`, `eslint .` (full repo), `next build` all clean (zero errors/warnings) — one real lint fix along the way (`react-hooks/set-state-in-effect` in the create dialog's due-preset reset, originally a `useEffect`; moved into the preset-button click handler directly instead, no effect needed). `next build`'s route table shows `/tasks` prerendering as static content alongside every other protected route. `new-operatora-dev` (PM2, port 3033) restarted; `GET /tasks` → 307 → `/login?next=%2Ftasks` → clean 200 login page, no server crash, no Next.js error overlay in the HTML or PM2 logs — same standing verification bar as every prior phase. **Not verified** (same standing limitation as every phase since Phase 2a): interactive click-through of an authenticated session — still no test account/workspace on `operatora_test`. Recommend exercising, once available: the mine/team scope toggle and its manager gating, create (with and without a linked lead/assignee), complete with the mandatory closure comment, the overdue/today/upcoming bucketing against real `due_at` values across timezones, and the module-disabled empty state against a workspace that hasn't enabled Task Management in Settings.

### AI Dashboards page (`/dashboards`) (done 2026-08-17)

Old frontend reference read for UX only (`pages/AIDashboards.tsx`, `components/dashboards/{DashboardGrid,DashboardCreator,DashboardView,DashboardWidget}.tsx`, `hooks/useCustomDashboards.ts`, `lib/customDashboards/customDashboardsApi.ts`), never modified. Traced directly against the real backend (`/www/wwwroot/dev.operatora/app/backend/src/custom-dashboards/{custom-dashboards.controller,custom-dashboards.service}.ts`, `custom_dashboards` table) rather than assuming the old frontend's calls were still accurate — one real finding: the read routes (`GET /meta`, `GET`, `GET /:id`) are now gated by `@RequireModulePermission('ai_dashboards', 'view')` (`RequireModulePermissionGuard`, resolves the caller's real RBAC permission matrix), confirming this page's RBAC module key exactly matches `constants/sitemap.ts`'s existing `ai_dashboards` nav-gate key — not something the old frontend's own code shows (it only checks `workspaceRole === 'owner'` client-side), so this is a newer server-side addition worth relying on directly rather than re-deriving.

**Full real contract, confirmed directly in the controller/service**: `GET /custom-dashboards/meta` (owner flag + plan quota — `isOwner` is server-computed via `isWorkspaceOwner()`, not re-derived client-side), `GET /custom-dashboards` (list), `GET /custom-dashboards/:id` (resolved — spec's widgets re-run against live data server-side on every fetch, not cached in the spec), `POST /custom-dashboards/generate` and `POST /custom-dashboards/:id/edit` (AI-driven, both additionally `assertWorkspaceOwner`-gated *inside the service*, independent of and in addition to the RBAC module gate above), `PUT /custom-dashboards/:id` (manual save — confirmed this one route has **no** owner or module decorator at the controller level, so technically any workspace member who already cleared the class-level `JwtAuthGuard` could call it; this app doesn't expose a manual-editor UI to non-owners either, matching the old frontend's own owner-gated `DashboardView` panel exactly, so the gap is dormant, not exploited by anything built here), `DELETE /custom-dashboards/:id` (owner-only).

Files: `src/features/dashboards/types.ts` (hand-written mirror of the real `WidgetSpec`/`DashboardSpec`/`WidgetData`/`CustomDashboardRow`/`DashboardMeta` shapes, plus `CATEGORY_ACCENT`/`spanClass`/`formatWidgetValue` ported 1:1 from the old frontend's `DashboardWidget.tsx`), `src/services/api/customDashboards.ts`, `src/features/dashboards/hooks/useDashboards.ts` (`useDashboardMetaQuery`/`useDashboardListQuery`/`useDashboardQuery`/`useGenerateDashboardMutation`/`useEditDashboardMutation`/`useDeleteDashboardMutation`), `src/features/dashboards/components/{DashboardWidgetCard,DashboardGrid,DashboardCreator,DashboardView}.tsx`, `src/app/(protected)/dashboards/page.tsx` (new route — `constants/sitemap.ts` already had the `/dashboards` nav entry, gated in `AppSidebar` via the pre-existing `isAdmin()` approximation from Phase 2b; left unchanged, see note below).

**New dependency: `recharts@^3.10.1`** — the old frontend's own choice for the `series` (bar/line) widget charts, and no chart-rendering capability already exists in this codebase to reuse instead. Confirmed React 19 compatibility (`peerDependencies: react ^19.0.0`) before installing; `npm audit` after install shows the same pre-existing critical/high findings as before (all from `@heroui-pro/react`'s `tar`/`expr-eval` transitive deps, unrelated to this change — confirmed by diffing the audit output before/after).

**Language handling**: `generate`/`edit` take a `lang` param controlling what language the AI writes titles/summaries in (old frontend sends the user's active `uz`/`ru`/`en` i18n locale). This app has no i18n layer (ARCHITECTURE.md) and its UI copy is English-only, so `lang` is hardcoded to `"en"` in `useDashboards.ts` rather than guessing a locale — documented inline so a future i18n pass knows exactly where to wire a real value in.

**Explicitly deferred, with reasoning (not oversights)**:
- **`AppSidebar`'s `ai_dashboards` nav gate still uses the Phase 2b `isAdmin()` global-role approximation**, not the real workspace-level `ai_dashboards`/`view` RBAC permission this page's own backend actually enforces (confirmed above) — the same standing gap Phase 2b's own writeup already flagged ("a deliberate, commented-in-code simplification… since `workspace_users` role data isn't fetched client-side yet"). That gap is now stale (Settings' Roles & Permissions section has fetched real RBAC permission data via `GET /workspace-rbac/me` since that slice landed), but fixing `AppSidebar`'s gate is a nav-shell change outside this page-build brief's scope — flagged here as a real, ready-to-fix follow-up (the data source already exists) rather than silently left as before or fixed as a drive-by. Worst case today: a non-permitted user sees the nav icon and gets a clean "you don't have access" `EmptyState` on click (built into this page, see below) instead of a 403 with no explanation — not a security gap, since the backend remains the real boundary, just an extra click.
- **Manual spec editing (drag/resize widgets, a widget picker, raw title/description/category form fields via `PUT /:id`)** — the old frontend's own `DashboardView.tsx` doesn't expose this either (only the AI edit chat, which calls `/edit` not `/:id` directly); not built here either, matching old behavior exactly rather than adding a new capability neither app has ever had.
- **`shared` flag toggle UI** — real column (`custom_dashboards.shared`), rendered read-only (the grid card's "Shared" badge), but no toggle control exists in the old frontend's `DashboardView.tsx` to port either.

**Verification**: `tsc --noEmit`, `eslint .` (full repo), `next build` all clean (zero errors/warnings) on the first attempt. `next build`'s route table shows `/dashboards` prerendering as static content. `new-operatora-dev` (PM2, port 3033) restarted; `GET /dashboards` → 307 → `/login?next=%2Fdashboards` → clean 200 login page, no server crash, no Next.js error overlay in the HTML or PM2 logs — same standing verification bar as every prior phase. **Not verified** (same standing limitation as every phase since Phase 2a): interactive click-through of an authenticated session — still no test account/workspace on `operatora_test`. Recommend exercising, once available: the RBAC 403 empty-state (a non-`ai_dashboards`-permitted workspace member), dashboard generation end-to-end against the real AI service, the edit-copilot chat + suggestion chips, delete-confirm, the plan-limit 403 (`code: 'plan_limit'`) surfacing a real message on the creator screen, and every `WidgetData` kind's rendering (`kpi`/`series` bar+line/`breakdown`/`breakdown`+funnel/`table`) against real resolved data, not just the empty-state fallbacks exercised so far.

**Self-review pass (no code-reviewer subagent available in this session — no agent-dispatch tool was exposed) caught and fixed one real bug before considering this done**: `listQuery.isError` had no dedicated branch — a failed `GET /custom-dashboards` would fall through `loading === false` straight into the `dashboards.length === 0` check (an empty array on error, since `.data` is `undefined`) and silently render the "create a dashboard" screen instead of an error state, hiding the failure entirely. Fixed by adding an explicit `listQuery.isError` branch (matching the pattern `detailQuery.isError` already used) before the empty/creator check. Re-ran `tsc`/`eslint`/`next build`/PM2 restart after the fix — all still clean.

### Ads page (`/ads`) + Meta OAuth callback (`/ads-callback`) (done 2026-08-17)

Old frontend reference read for UX only (`pages/{Ads,AdsCallback}.tsx`, `components/ads/{AdsCampaignRow,AdsCopilotPanel,CreativeDetailModal,AdAccountSelectPanel,NewTargetDialog}.tsx`, `lib/adsApi.ts`), never modified. Traced directly against the real backend (`/www/wwwroot/dev.operatora/app/backend/src/ads/{ads.controller,ads.service,ads.types}.ts`, `ads_connections`/`ads_campaigns` tables, live Meta Graph API calls) rather than assuming the old frontend's calls were still accurate.

**Real, RBAC-gated contract, confirmed directly in the controller/service**: `@RequireModulePermission('ads', 'view'|'manage')` on every route (matches this app's existing `ads` RBAC module key in `features/roles/types.ts`'s `PermissionModule`) — `GET /ads/status`, `GET /ads/campaigns` need `view`; `POST /ads/{connect,oauth-callback,oauth-connect,budget,audience,schedule,status-set,creative-status-set}` need `manage`, and are *additionally* workspace-owner/admin-only **inside the service** (`assertMoneyRole`) independent of the RBAC gate. The whole module is provider-abstract (`AdsProvider` interface) but only `meta` is implemented; real API access requires Meta's Advanced Access review (`isMetaAdsEnabled()`/`META_ADS_ENABLED`) — until granted, `listCampaigns()` returns **sample data** (`sample: true`, every row `isSample: true`) merged with any locally-edited rows, and this UI surfaces that honestly (a "You're viewing sample data" banner + a "Sample" chip per campaign/post), never pretending it's live. Budget changes and campaign/post *activation* (not pausing) require server-mandatory `confirm: true` (400 `code: 'confirmation_required'` otherwise, via the shared `AllExceptionsFilter`'s `{...extra, statusCode, message}` envelope, same pattern `ApiError.code` already parses) — this app always sends `confirm: true` after its own inline (not `window.confirm`) confirm UI, matching every other money-affecting confirm pattern already established in this codebase (e.g. Team Members' type-to-confirm delete).

**OAuth callback (`/ads-callback`, standalone, outside `(protected)`)**: same reasoning already established for `/instagram-callback`/`/google-sheets-callback`/`/google-calendar-callback` (no `AppShell` chrome wanted), deliberately at `/ads-callback` rather than the old frontend's `/auth/ads/callback` — this app already diverged from that `/auth/x/callback` convention for all three prior OAuth callbacks, so this one follows the same established pattern rather than reintroducing the old prefix. **One real behavioral difference from Instagram's callback, confirmed by reading both old-frontend flows**: Ads' connect is always a full-page redirect (`window.location.href = authorizeUrl`, old frontend's own `Ads.tsx` never opens a popup) — no `window.opener`/popup branch needed, unlike `google-sheets-callback`'s popup-or-redirect dual handling. Handles both outcomes of `POST /ads/oauth-callback`: immediate success (`managed` billing mode), or an ad-account selection step (`own` mode, `requiresSelection: true` + a 15-minute `selectionToken`) — the selection panel is a straight rebuild of the old frontend's `AdAccountSelectPanel.tsx`.

Files: `src/features/ads/types.ts`, `src/services/api/ads.ts`, `src/features/ads/hooks/{useAds,useAdsCopilot}.ts`, `src/features/ads/components/{AdsCampaignRow,CreativeDetailModal,ManageCampaignDialog,ActivateCampaignDialog,AdsCopilotPanel}.tsx`, `src/app/(protected)/ads/page.tsx`, `src/app/ads-callback/page.tsx` (new route, `constants/routes.ts` gained `adsCallback` so `proxy.ts`'s auto-derived protected-prefix list covers it too, same as the three prior OAuth-callback routes).

**Ads Copilot — reused existing infrastructure, not new plumbing.** Traced the old frontend's `components/ads/AdsCopilotPanel.tsx` → `lib/aiChatStream.ts`'s `streamAdminAiChat()` → confirmed it's a thin wrapper around the exact same `POST /ai-chat/v2` SSE endpoint the AI Chat feature (`/dashboard`) already streams from. `features/ads/hooks/useAdsCopilot.ts` reuses `services/api/chat.ts`'s `streamChatMessage`/`ChatStreamHandlers` directly (no new streaming client written) — the only difference from `/dashboard`'s usage is `threadId` is never sent, confirmed via `ai-chat.controller.ts` that persistence branches are strictly `threadId ? appendMessage(...) : (skip)`, so an omitted `threadId` runs the same AI pipeline without writing anywhere server-side — matching the old panel's own "ephemeral, page-local conversation" behavior exactly. A campaign-summary context line is prefixed onto each question (ported from the old panel's `contextLine()`), and `react-markdown` (already an installed dependency, from the AI Chat feature) renders assistant replies.

**Explicitly deferred, with reasoning (not oversights)**:
- **`POST /ads/schedule`** — a real, traced endpoint (campaign start/end date + day-of-week scheduling), but the old frontend's own `pages/Ads.tsx` "manage" dialog has no UI for it either (only budget + audience) — not built here either, matching old behavior exactly rather than adding a capability neither app has ever exposed.
- **`NewTargetDialog`'s "new target" wizard** — read in full before deciding: it calls **zero backend endpoints**. Step 3 ("preview") is a client-only summary that closes without writing anything — the old frontend's own deliberate design (an honest "here's what would happen once Advanced Access lands" walkthrough, explicitly not a fake "created" confirmation, per its own code comment). Reproducing a fully decorative multi-step wizard with no real effect wasn't worth the surface area for this pass given everything else in scope — flagged clearly here rather than silently dropped, in case a future pass wants to build it purely for onboarding/UX value.
- **`CreativeDetailModal`'s activate/pause action is real** (`POST /ads/creative-status-set`) but disabled whenever `sample: true` (mirrors the old frontend's own `disabled={busy || sample}` — no such action exists against sample data, only real Meta-provider rows).

**Self-review pass (no code-reviewer subagent available in this session)**: caught two real gaps before considering this done — the "pause campaign" toggle had no double-submit guard (rapid clicks could fire overlapping `POST /ads/status-set` calls) and neither it nor the connect-mode buttons surfaced a failure if the mutation errored (silent no-op on failure, violating this project's error-state rule). Fixed: `handleToggle` now no-ops while `setStatus.isPending`, and both `setStatus.isError`/`connectMutation.isError` render an inline `role="alert"` banner using the same `ApiError`-aware message pattern every other mutation error in this codebase already uses.

**Verification**: `tsc --noEmit`, `eslint .` (full repo), `next build` all clean (zero errors/warnings) — no new lint violations (the copilot panel's `stop()`-on-close effect and the OAuth callback's run-once effect both matched patterns already established elsewhere in this codebase, e.g. `ChatComposer`'s Stop wiring and `google-sheets-callback`'s run-once mount effect). `next build`'s route table shows both `/ads` and `/ads-callback` prerendering as static content. `new-operatora-dev` (PM2, port 3033) restarted; `GET /ads` → 307 → `/login?next=%2Fads`, `GET /ads-callback?code=x&state=y` → 307 → `/login?next=%2Fads-callback`, both following through to a clean 200 login page, no server crash, no Next.js error overlay in the HTML or PM2 logs — same standing verification bar as every prior phase. **Not verified** (same standing limitation as every phase since Phase 2a): interactive click-through of an authenticated session — still no test account/workspace on `operatora_test`. Recommend exercising, once available: the sample-data banner/badges against a genuinely unconnected workspace, both connect modes' `disabled`/`message` responses (Meta Advanced Access is very unlikely to be live on `operatora_test`), the full OAuth round-trip (requires a real Meta App registered with `ADS_OAUTH_REDIRECT_URL` pointing at this app's deployed `/ads-callback`, same standing infra caveat as Instagram's callback), budget/audience save + the `confirmation_required` 400 surfacing correctly, campaign/post activate-confirm and pause, and the copilot's streamed reply against a real `/ai-chat/v2` run.

### Messages (`/messages`) (done 2026-08-17)

Old frontend reference read for UX/flow only, never modified: `pages/Messages.tsx`, `components/messages/{MessagesCustomerInbox,MessagesTeamChat}.tsx`, `lib/messagesDeepLink.ts`, `locales/en/messages.json`.

**Important, surprising finding before any code was written**: the three files this feature's own brief named as the reference (`components/{TelegramMessages,InstagramMessages,SmsMessages}.tsx`, ~2,112/480/615 lines) are **dead code** — confirmed by grepping the entire old frontend for any remaining import of those component names outside their own files: none exist. `pages/Messages.tsx` actually imports `MessagesCustomerInbox.tsx`, which renders `TelegramChannelPanel.tsx` (4,573 lines), `InstagramChannelPanel.tsx` (2,621 lines) and `EskizChannelPanel.tsx`/`SmsChannelPanel.tsx` (894/559 lines) — the real, currently-wired implementations, each explicitly commented in its own source as the successor ("replace the legacy TelegramMessages component without losing functionality", "identical wire format to legacy InstagramMessages", "mirrors the legacy SmsMessages call"). The brief's own size estimate ("~5,300 lines of old-frontend logic") was built from the dead files; the real `components/messages/` folder this feature actually needed to trace against is **23,934 lines** across 33 files, including an entire "agentic" AI auto-reply/automation subsystem (11 files, ~4,600 lines) that doesn't exist anywhere else in this app. Traced and built against the real, currently-wired components, not the brief's assumed ones — flagged here explicitly rather than silently building against dead code.

**Scope call, made directly (no architect/code-reviewer subagent tool was exposed in this session, same standing limitation as the Dashboards/Ads passes)**: given the real size, this pass builds a genuine, fully real-backend-backed **core inbox** — read + send text, per-chat unread state, lead linking, realtime — for all four channels the old frontend's own channel rail exposes (Telegram, Instagram, SMS, Team Chat), and explicitly does not port the long tail of channel-specific power features below. Every deferred item is a real, traced backend capability (not a fabricated one), each individually larger than most whole single-page features already built in this app — reproducing all of it in one pass would have meant either silently compressing scope on the core inbox or ballooning this single dated entry into its own multi-slice epic like Leads (Phase 2c) got. Flagged here, not silently dropped, exactly like every prior phase's deferral list:
- **Telegram's linked-user-account ("userbot") mode** — contacts list/add/edit, start-chat-by-username, last-seen, history sync (`POST :id/sync-history`). This pass only builds the bot/business inbox (`mode` param omitted, which is what the real `findAll()` defaults to).
- **Media messages** (images/video/files/stickers/GIFs) on every channel — `POST .../send-media`/`send-file`, `PhotoLightbox.tsx`, `StickerGifPicker.tsx`. A non-text message renders as a plain "(non-text message)" placeholder in the thread rather than being hidden or crashing.
- ~~**Message-level actions**: edit, delete, forward, reactions, reply-to-quote, multi-select bulk actions (`TelegramMessageContextMenu.tsx`/`ForwardTelegramMessageDialog.tsx`/`StickerPackDialog.tsx`).~~ **Built for Telegram** in a later pass — see the dated "Messages — Telegram message context menu" entry below. (`StickerPackDialog.tsx` itself — browsing a sticker *pack* — is unrelated to the context menu and stays out of scope, since sending stickers at all is still not built here.)
- **The entire "agentic" AI subsystem** (`components/messages/agentic/*`, 11 files) — AI auto-reply mode, blacklist, knowledge base, targeting, catch-up sheet, copilot panel, approvals queue, away-replies, setup wizard. A materially different, self-contained feature domain with its own settings surface, not part of a "read + send" inbox.
- **Canned responses** (`CannedResponsesMenu.tsx`), **Instagram groups/automations** (`InstagramGroupsDialog.tsx`/`InstagramAutomationsDialog.tsx`), **Instagram AI "suggest reply"** (`POST conversations/:id/suggest`).
- **Channel connect/disconnect flows** (`AddTelegramBotDialog.tsx`/`ConnectTelegramAccountDialog.tsx`/`ConnectBusinessBotDialog.tsx`/`InstagramIntegration.tsx`) — deliberately not duplicated here; these already live in Settings (Telegram/Instagram/Eskiz sections, built in the Settings epic) and this page reuses their connected-state, not a second connect UI.
- **WhatsApp** — the old frontend itself only ever shipped a "coming soon" placeholder for this channel (no real backend integration exists); reproduced as the same honest placeholder here, not a fabricated fourth working channel.
- **Client-side plan-tier channel gating** (`useBillingFeatures().isChannelAllowed` — old frontend blocks Instagram/SMS on lower plans). This rebuild has no `useBillingFeatures`-equivalent hook yet (checked directly — nothing beyond the Settings billing-section display exists). RBAC module gating (`telegram`/`instagram` view permission, real and server-enforced) is reproduced; the plan-tier restriction is a real backend-enforced-elsewhere gap this frontend doesn't currently reproduce — flagged, not silently dropped, since a future pass building a billing-features hook can wire it straight in.

**Real contracts, confirmed directly in each controller/service (not assumed from the old frontend)**:
- **Telegram** — `telegram-chats.controller.ts` (`GET /telegram-chats?workspace_id=&search=&limit=&offset=`, `PATCH :id/read`, `PATCH :id/link-lead`) + `telegram-meassages.controller.ts` (`GET /telegram-meassages?chat_id=`, `POST /telegram-meassages/send` — `telegram`/`create` RBAC-gated server-side).
- **Instagram** — `instagram.controller.ts` (`GET /instagram/conversations`, `GET conversations/:id/messages`, `POST send-message` — `instagram`/`create` RBAC-gated, `POST conversations/:id/link-lead`). Confirmed no mark-as-read endpoint exists on this controller (read it in full) — unlike Telegram, Instagram unread counts here are display-only from the list response, not a fabricated read receipt.
- **SMS** — built entirely on the already-real `eskizSmsApi` (`src/services/api/eskizSms.ts`, Phase 2c-8's Leads SMS slice: `GET /eskiz/account`, `GET /eskiz/templates`, `GET /eskiz/chats`, `GET /eskiz/chats/:id/messages`, `POST /eskiz/send`, `PATCH /eskiz/chats/:id/link-lead`) — zero new SMS API surface needed, just a new inbox UI over the same real pipeline. Confirmed by reading `eskiz.service.ts` in full: there is **no inbound-SMS concept anywhere in this module** (no `direction`/`is_inbound` field, no webhook route for incoming messages) — Eskiz here is send-only (delivery-status updates via `eskiz:message-status-updated`, not customer replies). Every message this panel renders is therefore `direction="outbound"` by construction, which is a real backend limitation, not a bug in this UI — same "eskiz_messages" pipeline the Leads SMS tab already relies on.
- **Team Chat** — `messages-page/group-chat.controller.ts` (`GET channels`, `GET ?channel_id=` returning `{messages, profiles}`, `POST` to send). One genuine oddity confirmed by reading the controller directly: it does not use `JwtAuthGuard`/`@CurrentUser()` like every other controller in this app — it manually decodes the Bearer token and expects `workspace_id` as an explicit param rather than deriving it from the JWT. Confirmed compatible with this app's standard `apiFetch` (Bearer header either way); every call here passes `workspace_id` explicitly.

**Realtime — extended `services/realtime/subscriptions.ts` with four new entries**, the file's fifth+ additions since Phase 2b. One real backend difference worth documenting: Telegram/Instagram/SMS(Eskiz) do **not** go through the generic `RealtimeGateway`'s `workspace:{id}` topic/`channel:{topic}` envelope that every prior entry in this file uses — each has its own dedicated gateway class (`TelegramGateway`/`InstagramGateway`/`EskizGateway`) bound to the same default socket.io namespace, auto-joining the same `workspace:{workspaceId}` room on connection and emitting raw, unprefixed event names (`telegram:new-message`, `instagram:conversation-updated`, `eskiz:message-status-updated`, etc.) carrying the full row directly — confirmed by reading all three gateway classes, not assumed from the old frontend's own `socket.io-client` usage (though it independently confirms the same event names). `subscribeToTelegramEvents`/`subscribeToInstagramEvents`/`subscribeToEskizEvents` reflect this: no `subscribe`/`unsubscribe` emit needed (the room join is automatic per-connection), just `.on(eventName, …)` on the already-connected shared socket, and the feature hooks patch the React Query cache directly with the emitted row for the open thread (mirroring the old frontend's own "apply straight to local state" behavior) rather than forcing a refetch — with the chat-list view still refetched on a debounce for previews/unread counts. Team Chat, by contrast, *does* use the generic `RealtimeService`/`messages:{workspaceId}` topic (already present in `RealtimeGateway`'s `ALLOWED_PREFIXES`) with the standard `{event, table: 'messages'}` envelope, so `subscribeToTeamChatMessages` follows the exact same subscribe/debounce-invalidate pattern as the four prior entries in this file.

**Cross-feature integration revisit (per this slice's own brief)**: Phase 2c-12 deliberately skipped `CreateLeadFromChatDialog`/`AutoLeadCreateDialog`/`LinkChatToLeadDialog` because this host page didn't exist yet. Now that it does: **`LinkChatToLeadDialog`'s equivalent is built** — one shared `features/messages/components/LinkLeadDialog.tsx`, parametrized by link/unlink mutation callbacks, reused across Telegram/Instagram/SMS (all three have a real, traced `link-lead` endpoint), mirroring `features/conversations/components/LinkLeadDialog.tsx`'s exact structure (Phase 2c-12 precedent) rather than three near-duplicate dialogs. Linked-lead display uses the same "chip + name resolved via `lead-search` by-ids, no in-place lead-details modal" pattern `ConversationDetailPanel.tsx` already established, for the same reason: this app's `LeadDetailsModal` needs a `boardId`/`columns` context this page has no way to resolve for an arbitrary chat's lead. **`CreateLeadFromChatDialog`/`AutoLeadCreateDialog` remain deferred** — both would need a new "create a lead from this chat, pre-filling name/phone" endpoint trace (a genuinely new integration surface, not just wiring an already-built one, given the scope this pass already carries) — flagged as a real, ready-to-pick-up follow-up rather than silently dropped.

Files: `src/features/messages/types.ts`, `src/services/api/{telegramMessages,instagramMessages,teamChat}.ts` (SMS reuses the existing `services/api/eskizSms.ts` as-is — no new SMS API file), `src/features/messages/hooks/{useTelegramInbox,useInstagramInbox,useSmsInbox,useTeamChat}.ts`, `src/features/messages/components/{ChannelRail,ConversationListItem,MessageBubbleRow,TextComposer,SmsComposer,LinkLeadDialog,LinkedLeadChip,TelegramPanel,InstagramPanel,SmsPanel,TeamChatPanel}.tsx`, `src/app/(protected)/messages/page.tsx` (new route — `constants/sitemap.ts` already had the `/messages` nav entry pointing at this not-yet-built page, wired now), `src/services/realtime/subscriptions.ts` (extended, see above).

**Design**: three-pane shell (channel rail → conversation list → thread + composer) restructured from the HeroUI Pro email template supplied as a structural reference (`email-shell.tsx`/`email-sidebar.tsx`/`email-list-item.tsx`/`email-detail.tsx`) — folders became channels with unread counts, list items became conversation previews (avatar + name + last-message snippet + timestamp + unread badge), the email reader became a message-bubble thread + composer. Rebuilt on plain `@heroui/react` + Tailwind, not the template's `@heroui-pro/react` `Sidebar` compound component, per this codebase's own already-established precedent (`ChatThreadList.tsx`'s doc comment: no Pro license at runtime here). Message bubbles/composer follow this app's own AI Chat feature's (`features/chat/components/{MessageBubble,ChatComposer}.tsx`) established bubble/spacing conventions rather than porting the old frontend's channel-specific styling — a clean rebuild, not a visual port, per this feature's own brief. `@gravity-ui/icons` (this app's icon set) has no Telegram/Instagram/WhatsApp brand marks — channels are distinguished by accent color + a semantically-close generic icon instead of a fabricated brand logo.

**Verification**: `tsc --noEmit`, `eslint .` (full repo), `next build` all clean (zero errors/warnings) — one real lint fix along the way (`react-hooks/refs`: three realtime hooks were writing `ref.current` directly during render instead of inside an effect; moved into a dedicated `useEffect`). `next build`'s route table shows `/messages` prerendering as static content alongside every other protected route. `new-operatora-dev` (PM2, port 3033) restarted; `GET /messages` → 307 → `/login?next=%2Fmessages` → clean 200 login page, no server crash, no Next.js error overlay in the HTML or PM2 logs — same standing verification bar as every prior phase. **Not verified** (same standing limitation as every phase since Phase 2a): interactive click-through of an authenticated session — still no test account/workspace on `operatora_test`. Recommend exercising, once available: all four channel panes against real connected accounts (Telegram bot, Instagram Business account, Eskiz account, and the auto-seeded `#general` Team Chat channel), the realtime append-to-open-thread path for each of the three raw-event channels plus Team Chat's debounced-refetch path, RBAC-locked Telegram/Instagram (a role without `view` permission), the SMS "not connected" empty state and the approved-templates-only composer gate, and the new `LinkLeadDialog` link/unlink round-trip on all three channels.

### Messages — Telegram message context menu (done 2026-08-27)

Full parity port of the old frontend's per-message context menu, reversing the "Message-level actions" deferral the initial Messages pass made (see above): copy, reply (quoted-reply send), forward, react (quick-emoji + remove), edit (own outbound text messages), delete, multi-select (with a bulk copy/forward/delete toolbar), and save image (for photo/sticker messages) — for Telegram only (Instagram/SMS keep their plain read+send bubbles; their own context-menu parity, if ever wanted, is a separate follow-up).

**Reference**: `TelegramMessageContextMenu.tsx` (the menu itself) + `TelegramChannelPanel.tsx` (wiring: `handleReplyTo`/`handleEditMessage`/`handleReaction`/`handleDeleteMessage`/`handleForwardTo`/`handleCopyMessage`/`handleSaveMessageImage`/bulk-action handlers) + `ForwardTelegramMessageDialog.tsx`, all read-only, never modified.

**Real endpoints traced directly against `telegram-meassages.controller.ts`** (not assumed from the old frontend): `POST :id/reaction` (`{ emoji: string | null }`), `POST :id/edit` (`{ text_content }`, outbound text messages only, `telegram`/`edit` RBAC), `POST :id/telegram-delete` (`telegram`/`delete` RBAC — deletes on Telegram itself, not just the local row), `POST /telegram-meassages/forward` (`{ source_chat_id, target_chat_id, message_ids[], sender_id }`), and `POST /telegram-meassages/send`'s existing `reply_to_message_id` field (Telegram's own numeric message id, not our row uuid) for quoted replies. "Save image" doesn't call a message-scoped endpoint at all — it resolves the message's `file_id` (added to `TelegramMessage`, backfilled server-side from `telegram_data` when absent) through the already-existing, public/unauthenticated `GET /telegram-media/:fileId?bot_id=|business=1` proxy (`telegram-media.controller.ts`) and downloads the streamed blob — same mechanism the chat-avatar `<img src>` already uses, just triggered as a download instead of rendered inline. All six are real, already-shipped backend capabilities; nothing was invented.

**HeroUI substitution**: HeroUI has no dedicated right-click "context menu" primitive (the old frontend used shadcn/radix `ContextMenu`). Used `Dropdown` (`@heroui/react`) with `trigger="contextMenu"` instead — a real, natively-supported `MenuTrigger` mode (`react-stately/useMenuTriggerState`'s `MenuTriggerType = 'press' | 'longPress' | 'contextMenu'`), not a hand-rolled `onContextMenu` + controlled-`isOpen` workaround. The quick-reaction "React" submenu uses `Dropdown.SubmenuTrigger` (a real nested-menu primitive) with each quick emoji as a plain `Dropdown.Item` rather than reproducing the old menu's custom emoji-grid layout — behavior (pick one of 10 fixed reactions, or remove), not pixel styling, is what was ported, per this feature's own "clean rebuild, not a visual port" precedent. `Dropdown.Trigger`'s default `<button>` chrome is neutralized (`className="contents"`) so the wrapped message bubble renders exactly as it does everywhere else.

**Files**: `features/messages/types.ts` (extended `TelegramMessage` with `telegram_message_id`/`file_id`/`bot_integration_id`/`is_edited`/`reply_to_message_id`/`metadata`, extended `TelegramChat` with `bot_integration_id`/`business_connection_id`, added `telegramMessageMediaUrl()`), `services/api/telegramMessages.ts` (added `react`/`edit`/`remove`/`forward` to `telegramMessagesApi`, extended `send()` with `senderId`/`replyToMessageId`, added standalone `downloadTelegramMedia()` — a plain `fetch`, not `apiFetch`, since the media endpoint is public and returns binary, not JSON), `features/messages/hooks/useTelegramInbox.ts` (added `useTelegramReactionMutation`/`useTelegramEditMutation`/`useTelegramDeleteMutation`/`useTelegramForwardMutation`, extended `useTelegramSendMutation`'s optimistic bubble with reply metadata), `features/messages/components/{TelegramMessageMenu,ForwardMessageDialog}.tsx` (new), `features/messages/components/MessageBubbleRow.tsx` (extended with optional `replyQuote`/`isEdited`/`reaction`/`selectionMode`/`selected`/`onToggleSelect`/`wrapBubble` props — all unused-by-default, so Instagram/SMS panels render identically to before; confirmed by reading both before changing the shared component), `features/messages/components/TextComposer.tsx` (added optional `initialValue`/`contextBanner`/`submitLabel` for the edit-prefill and reply/edit context strip — Instagram/Team Chat panels that also use this composer are unaffected, same reasoning), `features/messages/components/TelegramPanel.tsx` (wired reply/edit/selection/forward state, the per-message menu, the selection-mode bulk toolbar, following this app's own `LinkLeadDialog`/`linkDialogOpen` dialog-state pattern for the new `ForwardMessageDialog`).

**Verification**: `tsc --noEmit` and `eslint src/features/messages src/services/api/telegramMessages.ts` both clean; `next build` succeeds with `/messages` still prerendering as static content (route table unchanged). Not verified interactively — same standing limitation as the rest of this feature (no test account/workspace on `operatora_test` yet); recommend exercising, once available: reply-to-quote round-trip (including the quote strip resolving via `metadata.reply_preview` for realtime-delivered messages vs. local `reply_to_message_id` lookup for the sender's own optimistic bubble), edit on an outbound text message vs. the menu correctly hiding "Edit" for inbound/non-text messages, delete's Telegram-side removal, react/remove-react round-trip, forward to a second chat, multi-select bulk copy/forward/delete, and save-image against a real photo/sticker message.

## Phase 2b — Remaining feature work (not started — next slice)
- [~] Leads board/list — **core MVP + filters done** (see "Leads Kanban board" and "Leads — filters" writeups above); full parity now tracked as **Phase 2c** above, slice by slice.
- [ ] Workspace/session bootstrap polish (workspace switcher UI, `workspace_users` role/permissions fetch — currently only global `roles[]` is wired)
- [x] Extend `services/realtime/subscriptions.ts` beyond the single AI-Chat topic/table entry added in Phase 2b — now four entries (chat threads, workspace presence, Super Agent tasks, **lead board** — added this pass); notifications not yet added.
- [x] Settings section content — **all 24/24 sections real**, see "Settings page epic — complete" above.
- [x] Tasks (`/tasks`), AI Dashboards (`/dashboards`), Ads (`/ads` + `/ads-callback`) — done 2026-08-17, see their own dated writeups above.
- [x] Messages (`/messages`) — done 2026-08-17, see "Messages — customer inbox + team chat" writeup below.
- [ ] Remaining protected routes (operators, a standalone Billing/plan page distinct from the `billing` *settings* section, forms, gallery) — one folder per route, scoped individually
- [ ] Admin console pages (workspaces, users, billing, tariffs, etc. under `(admin)/admin/*`) — currently just a placeholder + role gate
- [ ] `components/ui/` HeroUI wrapper layer — not needed yet since only a handful of features consume HeroUI directly so far; add once patterns repeat enough to be worth extracting
- [ ] File upload helper (`uploadFile(bucket, file, opts)` per ARCHITECTURE.md) — not built; no feature needs it yet

## Phase 3 — Validation & Hardening
- [ ] Error/loading/empty/offline states across features
- [ ] Network-flood audit (dedupe, debounce, retry/backoff)

## Phase 4 — Review & Deploy
- [ ] code-reviewer pass
- [ ] PM2 production deployment config

## Open Questions (from architecture pass — see `ARCHITECTURE.md`)
- [x] Cookie vs Bearer auth for new frontend → **Bearer token** (decided 2026-08-12)
- [x] Whether `/tasks` maps to a real backend entity yet → **Yes, real entity** (confirmed 2026-08-12). `TasksController` (`/tasks`) backed by `operator_tasks` table via raw Supabase client (not in Prisma schema). Build a proper `tasksApi` service. See `ARCHITECTURE.md` Backend Findings.
- [x] Admin console: route group in same app, or separate deployable → **same app, route group** (decided 2026-08-12)
- [x] Confirm exact realtime event names/payloads for lead/board changes → **confirmed 2026-08-12**, see `ARCHITECTURE.md` Backend Findings. Key gap: no realtime event for lead creation or board/column CRUD — those need query invalidation, not socket subscription.
- [x] Confirm whether new/changed backend endpoints will expose OpenAPI/Swagger for type generation → **Yes, live in dev/staging** (`ENABLE_SWAGGER=true`, off by default in prod). 138/142 controllers decorated; ~11 controllers use untyped `@Body() body: unknown` (incl. Tasks create/settings) needing hand-authored types. See `ARCHITECTURE.md` Backend Findings.

All open questions resolved — Phase 1 architecture is fully closed out. Ready to begin Phase 2 scaffolding.

---
Last updated: 2026-08-27 (Messages — Telegram message context menu)
