import { ExtensionConnectView } from "@/features/extension/components/ExtensionConnectView";

export const metadata = { title: "Connect Extension — Operatora" };

export default function ExtensionConnectPage() {
  return <ExtensionConnectView />;
}
